---
phase: quick-260606-01h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/config/ConfigForm.tsx
  - src/features/config/FooterBar.tsx
  - src/features/config/BoxRow.tsx
  - src/features/config/BoxCatalogCard.tsx
  - src/features/config/defaults.ts
  - src/features/config/BoxRow.test.tsx
  - src/features/config/BoxCatalogCard.test.tsx
  - src/features/config/FooterBar.test.tsx
  - src/components/result/PlacementList.tsx
  - src/components/result/PlacementList.test.tsx
  - src/routes/ResultPage.tsx
  - src/components/viewer/CameraPresets.tsx
  - src/lib/camera-presets.ts
  - src/lib/camera-presets.test.ts
  - src/components/viewer/Boxes.tsx
autonomous: true
requirements: [QUICK-260606-01h]
must_haves:
  truths:
    - "The topbar shows brand + step-nav only; there is exactly ONE 'Run packing' button on the Configure screen (the footer CTA)."
    - "The footer is visually separated from page content by a solid background + top border, has real vertical padding, and its Run button does not touch the screen edge."
    - "The rotation control offers exactly two options ('Any orientation', 'Keep this side up'); 'Fixed' is gone from the UI but a persisted 'fixed' draft still validates and maps to API 'none' without crashing."
    - "Length, Width, and Height each have their own correctly-labelled field and sit together in ONE row; the length field keeps its fit-error and 'mm' unit."
    - "No box-type row shows a visible id/code chip; the hidden id input is still registered (swatch colour + request ids unaffected)."
    - "A newly-added box type defaults its label to 'Box type <N>' where N is the next number by additions; the seeded first type is 'Box type 1'."
    - "Every interactive control (Add box type, segmented control, switch, number steppers, result toggles/switcher/preset buttons) shows cursor:pointer on hover."
    - "DESYNC: the box-type rows displayed on the cards are EXACTLY the same set the footer/badge tally counts AND exactly the units in the built PackRequest, after ANY add/remove/edit sequence — one source of truth, always."
    - "Footer 'units'/'est. kg' and the catalog badge update on every change, including nested quantity/weight edits."
    - "Result placement cards read cleanly (less cramped, clear hierarchy) and the right rail panel is modestly wider; the <900px stack behavior is preserved."
    - "The ISO→TOP (and all) camera-preset transitions animate smoothly with no 'tilt-then-snap'; preset framings stay distinct and the active button toggles; camera is preserved across pallet switches."
    - "The 3D box white edge-outline lines are slightly thicker (still subtle)."
  artifacts:
    - path: "src/features/config/FooterBar.tsx"
      provides: "Bordered, padded, solid-bg sticky footer (no gradient shade)"
      contains: "border-t"
    - path: "src/features/config/defaults.ts"
      provides: "makeDefaultBoxType(n?) numbering + 'Box type 1' seed"
      contains: "Box type"
    - path: "src/features/config/BoxRow.tsx"
      provides: "L|W|H one-row layout, two rotation options, no id chip"
    - path: "src/components/result/PlacementList.tsx"
      provides: "De-densified placement cards"
    - path: "src/lib/camera-presets.test.ts"
      provides: "Regression coverage for smooth preset interpolation math"
  key_links:
    - from: "src/features/config/BoxCatalogCard.tsx"
      to: "src/lib/config-tally.ts"
      via: "rows rendered from the SAME live source the tally reads"
      pattern: "useWatch|fields"
    - from: "src/features/config/ConfigForm.tsx"
      to: "src/lib/request-builder.ts"
      via: "submit reads the same boxTypes the cards display"
      pattern: "buildPackRequest"
    - from: "src/components/viewer/CameraPresets.tsx"
      to: "src/lib/camera-presets.ts"
      via: "preset transition interpolates position+target+up together"
      pattern: "presetFromBbox"
---

<objective>
Land a 12-item batch of Configure-form, footer/topbar, Result-page, and 3D-viewer UI/UX fixes PLUS the box-state desync bug, grouped into three atomic, independently-committable tasks. Each task keeps the full gate green (test / typecheck / lint / build / code-split / e2e) and updates the tests that assert any changed behavior.

Purpose: The Configure form has duplicate CTAs, an ugly gradient footer, a mislabelled/misplaced dimensions row, a stale-counter / submit-desync data-integrity bug, and a noisy rotation option; the Result page is cramped and the camera transition is jarring. This batch makes the displayed state provably equal to the submitted state and cleans the surfaces the user touches most.

Output: Edited Configure-form files (Task A), a root-caused + regression-tested state-integrity fix (Task B), and Result-page + viewer polish (Task C).
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

# Configure-form surfaces
@src/features/config/ConfigForm.tsx
@src/features/config/FooterBar.tsx
@src/features/config/BoxRow.tsx
@src/features/config/BoxCatalogCard.tsx
@src/features/config/defaults.ts
@src/features/config/schema.ts
@src/types/config.ts

# State-integrity surfaces
@src/hooks/useLocalStorageAutosave.ts
@src/lib/config-tally.ts
@src/lib/request-builder.ts

# Result + viewer surfaces
@src/components/result/PlacementList.tsx
@src/routes/ResultPage.tsx
@src/components/viewer/CameraPresets.tsx
@src/lib/camera-presets.ts
@src/components/viewer/Boxes.tsx
</context>

<constraints>
- Runs on the MAIN working tree (node_modules present). A dev server is already on :5173 — DO NOT kill it; you may open it to reproduce.
- Full gate must stay green: `npm test`, `npm run typecheck`, `npm run lint`, `npm run build`, `node scripts/check-code-split.mjs` (entry chunk three-free: config/footer/row files must NOT import three/r3f/drei), `npm run test:e2e`.
- Preserve the code-split discipline: ConfigForm / FooterBar / BoxRow / BoxCatalogCard / defaults / config-tally / request-builder import NO three/r3f/drei.
- KEEP the `RotationMode` union ('free'|'uprightOnly'|'fixed') in src/types/config.ts and the `fixed→none` mapping in request-builder.ts (back-compat for persisted drafts). Only remove 'fixed' from the BoxRow UI options array. Leave `rotation = z.enum([...,'fixed'])` in schema.ts intact so a persisted 'fixed' draft still validates.
- Do NOT regress the Phase 6 result page or the LoadingPage StrictMode fix.
- Update/extend tests wherever behavior changes (esp. the #5/#8 regression test, and BoxRow/FooterBar/BoxCatalogCard/PlacementList/CameraPresets tests that assert old layout/strings).
</constraints>

<tasks>

<task type="auto">
  <name>Task A: Configure-form UI fixes (#1, #2, #3, #4, #6, #7, #9-config)</name>
  <files>src/features/config/ConfigForm.tsx, src/features/config/FooterBar.tsx, src/features/config/BoxRow.tsx, src/features/config/BoxCatalogCard.tsx, src/features/config/defaults.ts, src/features/config/BoxRow.test.tsx, src/features/config/BoxCatalogCard.test.tsx, src/features/config/FooterBar.test.tsx</files>
  <action>
Deliver six Configure-form fixes. (#1) In ConfigForm.tsx remove the topbar `Run packing` `<button onClick={onRun}>` block (the one after the `flex-1` spacer in the `<header>`); keep brand + step-nav. The `RunIcon` helper is now unused there — if no other caller remains, remove `RunIcon` to keep lint green; the FooterBar already owns the primary Run CTA via `onRun`.

(#2) In FooterBar.tsx replace the root `<div>` gradient classes (`bg-[linear-gradient(transparent,var(--color-bg)_38%)] pt-7`) with a cleanly-separated footer: keep `sticky bottom-0`, use a solid surface background (`bg-bg` or `bg-surface`), add a top border (`border-t border-border`), real vertical padding (e.g. `py-4`), horizontal padding so the Run button is inset from the screen edge (e.g. `px-6` plus a small bottom inset so it does not hug the viewport bottom — e.g. `pb-5`/`mb-2`), and remove the negative-feeling `mt-8` gradient reliance. The footer lives inside ConfigForm's `<main class="mx-auto max-w-[960px] px-6">`; make the footer read as a bordered bar separated from the cards above. Keep the Save-draft + Run buttons and their `cursor-pointer`.

(#3) In BoxRow.tsx remove `{ value: 'fixed', label: 'Fixed' }` from `ROTATION_OPTIONS` so only `'free'`/`'uprightOnly'` remain. Do NOT touch the `RotationMode` type, the schema enum, or the request-builder mapping. Keep the two existing labels.

(#4) In BoxRow.tsx restructure the body dims layout: make row 1 a `grid grid-cols-3 gap-7 max-[720px]:grid-cols-1` containing three separate `NumberField`s — `label="Length"` (bound to `boxTypes.${index}.length`, keeping `unit="mm"` and `error={lengthError}`), `label="Width"` (`...width`, `unit="mm"`), `label="Height"` (`...height`, `unit="mm"`). Delete the old single full-width `NumberField label="Dimensions" hint="length · width · height"`. Move the Weight field (`label="Weight / unit"`, `unit="kg"`, `step="0.1"`, `...weight`) into the next grid row alongside Quantity + Max-load (or its own row) — do NOT drop it. Preserve the responsive single-column collapse.

(#6) In BoxRow.tsx remove the visible id chip `<span ...>{id}</span>` (the mono code tag in the row head). KEEP the `<input type="hidden" {...register(`boxTypes.${index}.id`)} />` and the `useWatch` of `id` (still needed for the swatch colour). 

(#7) In defaults.ts change `makeDefaultBoxType()` to accept an optional sequence number and set the label to `Box type ${n}` (default n=1 so the seeded `DEFAULT_CONFIG` first type reads 'Box type 1'). In BoxCatalogCard.tsx `onAdd`, pass the next number based on current count: `append(makeDefaultBoxType(fields.length + 1))`. Numbering is by additions (current count + 1), NOT re-numbered on removal.

(#9-config) Add `cursor-pointer` (and `disabled:cursor-not-allowed` where a control can be disabled) to interactive Configure controls that lack it — at minimum the BoxCatalogCard `Add box type` button (the `onAdd` `<button>` lacks `cursor-pointer`). Sweep SegmentedControl, Switch, and NumberField stepper controls; add `cursor-pointer` to any clickable/role=button/switch element missing it. (Result-page controls are Task C.)

Update tests in the same task: BoxRow.test.tsx (rotation now has 2 options not 3; the Length field is labelled "Length" not "Dimensions"; the id chip is gone — drop/adjust any assertion that the visible id text renders), BoxCatalogCard.test.tsx (a newly-added type's default name is now 'Box type 2', and the seeded one is 'Box type 1'), FooterBar.test.tsx (assert the bordered/padded footer if it pins the old gradient class). Do NOT place fenced code in actions — write the test changes directly in the files.
  </action>
  <verify>
    <automated>npx vitest run src/features/config/BoxRow.test.tsx src/features/config/BoxCatalogCard.test.tsx src/features/config/FooterBar.test.tsx && npm run typecheck && npm run lint && node scripts/check-code-split.mjs</automated>
  </verify>
  <done>Exactly one Run button on the Configure screen; footer is bordered + padded with the Run button inset from the edge; rotation shows two options; Length/Width/Height are three correctly-labelled fields in one row with Weight moved out; no visible id chip (hidden id input retained); new box types default to 'Box type &lt;N&gt;' with the seed at 'Box type 1'; Add-box-type and swept controls show cursor:pointer; the named vitest files pass, typecheck/lint/code-split green.</done>
</task>

<task type="auto">
  <name>Task B: State-integrity desync bug (#5, #8) + regression test</name>
  <files>src/features/config/BoxCatalogCard.tsx, src/features/config/FooterBar.tsx, src/hooks/useLocalStorageAutosave.ts, src/features/config/BoxCatalogCard.test.tsx</files>
  <action>
Root-cause and fix the displayed-vs-submitted box desync (#8) and the stale-counter symptom (#5) — they are the SAME RHF field-array reactivity family. Do NOT patch the symptom; make the displayed rows, the tally, and the built request provably ONE source of truth.

Step 1 — Reproduce: write a failing component/integration test FIRST (in BoxCatalogCard.test.tsx or a new sibling) that drives a rapid add/remove/edit sequence (e.g. add 2 types, edit a quantity, remove the middle one, add another, edit a weight) and asserts the THREE views agree: displayed rows (count of `Remove` buttons) === the types in the footer/badge tally === the box-type count and total units that `buildPackRequest` produces from `getValues()`. Use a harness that mounts BoxCatalogCard + FooterBar inside one `useForm<PackConfig>` and reads `getValues('boxTypes')` through `buildPackRequest`/`tallyCatalog`. The assertion is: `displayedRows === tally.types` AND `sum(rowQuantities) === tally.units === new Set(buildPackRequest(values).idToType.values()).size`-style unit count. Confirm it FAILS (reproduces the drift) before fixing — if it already passes in jsdom, escalate the sequence (interleave append/remove at non-tail indices, which is where RHF `fields` vs live-values drift) until it reproduces, OR reproduce live on the :5173 dev server and capture the exact op sequence in the test.

Step 2 — Root-cause: the rows render from `useFieldArray`'s `fields` SNAPSHOT (BoxCatalogCard.tsx:67) while the badge (BoxCatalogCard.tsx:27 `useWatch('boxTypes')`), the footer (FooterBar.tsx:36 `useWatch('boxTypes')`), and the submit (ConfigForm `getValues`/resolver) read LIVE values. Investigate the three candidate drift sources: (a) `remove(i)` index handling vs the keyed `field.id` rows; (b) `useWatch('boxTypes')` nested-subscription staleness in RHF 7.77 (a nested quantity/weight edit not re-firing the array-level watch — the #5 dual); (c) the autosave/restore interaction in useLocalStorageAutosave.ts (the `form.subscribe({ formState: { values: true } })` callback and the debounced unconditional `writeConfig` — confirm restore-on-mount and the subscribe callback are not re-seeding or shadowing the live array). Identify which actually drives the drift.

Step 3 — Fix to one source of truth. Make the displayed rows and the counters/submit read a single reliable subscription. Preferred approach: drive the rendered row list from the live, subscribed array length/ids (keep stable keys) rather than the possibly-stale `fields` snapshot — e.g. iterate the `useWatch('boxTypes')` array (using each entry's stable `id` as the React key) so rows, badge, footer, and submit all derive from the SAME subscribed value; keep `useFieldArray`'s `append`/`remove` as the only mutators. If the true cause is (b) nested-watch staleness, ensure the array-level watch re-computes on nested edits (e.g. subscribe such that quantity/weight changes re-fire). If the true cause is (c) the autosave subscribe/restore path, fix the re-seed/shadow so the live form array is never replaced out from under the field array. Whatever the root cause, the invariant after the fix is: rows rendered === entries the tally counts === entries `buildPackRequest` expands, with NO path where they diverge. Preserve: stable swatch colours (keys/ids), the Add-row scroll/focus behavior, the empty-catalog state, the NaN-safe tally, and the code-split gate (NO three import).

Step 4 — Lock it: the regression test from Step 1 must now PASS and assert the displayed-rows ≡ tallied-units ≡ built-request-units invariant after the add/remove/edit sequence. Keep the existing BoxCatalogCard add/remove/badge tests green.
  </action>
  <verify>
    <automated>npx vitest run src/features/config/BoxCatalogCard.test.tsx src/lib/config-tally.test.ts src/lib/request-builder.test.ts && npm run typecheck && npm run lint && node scripts/check-code-split.mjs</automated>
  </verify>
  <done>A regression test drives an add/remove/edit sequence and asserts displayed-rows ≡ tallied units ≡ built-request units (types AND total units), failing on the pre-fix code and passing after; the fix makes rows + badge + footer + submit derive from one subscribed source (no `fields`-vs-live drift path); swatch stability, add-scroll/focus, empty state, NaN-safe tally and code-split gate preserved; named vitest files + typecheck/lint/code-split green.</done>
</task>

<task type="auto">
  <name>Task C: Result page + 3D viewer (#10, #11, #12, #9-result)</name>
  <files>src/components/result/PlacementList.tsx, src/components/result/PlacementList.test.tsx, src/routes/ResultPage.tsx, src/components/viewer/CameraPresets.tsx, src/lib/camera-presets.ts, src/lib/camera-presets.test.ts, src/components/viewer/Boxes.tsx</files>
  <action>
Deliver four Result/viewer fixes.

(#10 cards) In PlacementList.tsx de-densify the placement cards: increase per-card padding and inter-card gap, strengthen visual hierarchy (e.g. give the head row more breathing room, separate the type sub-line, and let the Size/Position/Support field grid breathe — larger gap, slightly larger value text, or stack labels more clearly). Keep ALL existing data (swatch, item_id, orientation tag, weight, typeId sub-line, Size/Position/Support — Support ALWAYS shown per DIAG-02) and the one-way hover (onMouseEnter/Leave → onHover) intact; render every API string as React text children only (no raw-HTML sink — V5/T-06-07). Keep `data-placement-card` and the `data-result-rail` hooks.

(#10 rail width) In ResultPage.tsx widen the rail column modestly: the root grid is `grid-cols-[1fr_384px]` (line ~161) with a `max-[900px]:grid-cols-1` stack. Increase the rail track a bit (e.g. `384px` → ~`440px`). Preserve the `<900px` single-column stack behavior and the `data-pallet-count` / `aria-label` rail hooks.

(#11 camera) Fix the ISO→TOP (and all preset) transition tilt-then-snap in CameraPresets.tsx + camera-presets.ts. Root cause: the transition lerps camera POSITION and target linearly while the camera's effective up/orientation changes nonlinearly between presets (TOP looks straight down, ISO is three-quarter), so the framing tilts gradually then the rotation resolves abruptly. Fix by animating orientation smoothly: either (a) interpolate the camera ORIENTATION via quaternion slerp (build from→to quaternions that look from `position` toward `target` with a consistent up, slerp them across the eased k alongside the position/target lerp), or (b) animate position+target+up together with matched easing so the look-direction sweeps smoothly. If using (a), add a pure helper in camera-presets.ts (e.g. a function returning the look-quaternion or up vector for a preset given the bbox) so the orientation math stays jsdom-testable and outside heavy three runtime where possible. Preserve: the `bboxRef`/`measureNonce` camera-preservation-on-pallet-switch guard (D-02 — a swap must NOT re-frame), the `__cameraState` test hook semantics (presets still produce DISTINCT settled camera positions), `presetFromBbox` framing distances, and OrbitControls clamps. Keep `ANIM_MS`/easing; the goal is a smooth sweep, not a slower one.

(#12 edges) In Boxes.tsx make the drei `<Edges>` white outline lines a bit thicker — set the `<Edges>` `lineWidth` prop to a modestly higher value (subtle, e.g. ~1.5–2). Keep the existing `edgeTint` colour + opacity.

(#9-result) Add `cursor-pointer` to any Result-page interactive control missing it that is touched here — the PlacementList cards are hover-only (no pointer needed unless they become clickable); ensure any preset/toggle/switcher buttons rendered via ViewerOverlay/PalletSwitcher that you touch carry `cursor-pointer` (these may already have it from Task A's shared primitives — only add where missing).

Update tests in the same task: PlacementList.test.tsx (if it pins exact spacing classes or card structure, adjust to the new layout while keeping the data-presence + Support-always-shown + `data-placement-card` assertions), and camera-presets.test.ts (add coverage for the new orientation/quaternion or up-interpolation helper — e.g. that the look-orientation for ISO ≠ TOP and that an interpolated mid-point is between the two, locking the smooth-sweep math). Do NOT place fenced code in actions — write the test changes directly in the files. The result-viewer e2e (`e2e/result-viewer.spec.ts`) must still assert distinct preset framings + active-button toggle; if your camera change alters settled positions, keep them distinct.
  </action>
  <verify>
    <automated>npx vitest run src/components/result/PlacementList.test.tsx src/lib/camera-presets.test.ts && npm run typecheck && npm run lint && node scripts/check-code-split.mjs</automated>
  </verify>
  <done>Placement cards read less cramped with all data + Support retained and one-way hover intact; the rail column is modestly wider with the &lt;900px stack preserved; ISO→TOP and all preset transitions sweep smoothly (no tilt-then-snap) with distinct settled positions and camera-preserved-on-switch intact; 3D edge lines are subtly thicker; touched Result controls show cursor:pointer; the named vitest files + typecheck/lint/code-split green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage draft → form | A persisted (possibly stale/foreign/'fixed'-rotation) config is restored into the form. |
| packing API JSON → Result render | Untrusted API strings (item_id, orientation.name, typeId, pallet_id) rendered in the rail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q01-01 | Tampering | Persisted 'fixed' rotation draft after UI removal (#3) | mitigate | Keep `RotationMode` union + schema enum + `fixed→none` request-builder mapping intact; only remove 'fixed' from the BoxRow UI options array so a restored 'fixed' draft validates and maps, never crashes. |
| T-q01-02 | Tampering | Box state desync (#8) — submitted data diverging from displayed | mitigate | Task B makes displayed rows ≡ tally ≡ built request one source of truth; regression test pins the invariant so a future change cannot silently re-introduce divergence. |
| T-q01-03 | Information disclosure / Injection | API strings rendered in de-densified placement cards (#10) | mitigate | Continue rendering item_id/orientation.name/typeId as React text children only — NO raw-HTML sink (V5/T-06-07); no change to the rendering channel. |
| T-q01-SC | Tampering | npm installs | accept | No new dependencies added in this batch (UI/logic edits only); existing pinned lockfile unchanged. |
</threat_model>

<verification>
After all three tasks, run the FULL gate from the repo root (the dev server on :5173 may stay running):

- `npm test` — all vitest suites green, including the new #5/#8 regression test and the updated BoxRow/BoxCatalogCard/FooterBar/PlacementList/camera-presets tests.
- `npm run typecheck` — `tsc -b --noEmit` clean (RotationMode totality + schema satisfies preserved).
- `npm run lint` — eslint clean (no unused `RunIcon`/imports after the topbar button removal).
- `npm run build` — `tsc -b && vite build` succeeds.
- `node scripts/check-code-split.mjs` — entry chunk three-free (config/footer/row files never import three/r3f/drei).
- `npm run test:e2e` — Playwright green: result-viewer preset framings still distinct + active toggle, config-persist + api-poll flows unbroken, LoadingPage StrictMode behavior intact.
</verification>

<success_criteria>
All 12 issues resolved and the full gate (test / typecheck / lint / build / code-split / e2e) green:
- Exactly one Run CTA; bordered/padded footer with inset Run button.
- Two rotation options (Fixed removed from UI; back-compat preserved).
- Length/Width/Height in one row, correctly labelled; Weight retained elsewhere.
- No visible id chip; hidden id retained.
- New types default to 'Box type &lt;N&gt;' (seed = 'Box type 1').
- cursor:pointer on all interactive controls (config + result).
- DESYNC FIXED with a regression test proving displayed-rows ≡ tallied-units ≡ built-request-units after any add/remove/edit sequence; counters always update.
- Placement cards less cramped + wider rail (<900px stack preserved).
- Smooth preset camera transitions (no tilt-then-snap), distinct framings, camera-preserved-on-switch.
- Slightly thicker box edge lines.
</success_criteria>

<output>
Create `.planning/quick/260606-01h-config-ui-results-3d-viewer-fixes-and-bo/260606-01h-SUMMARY.md` when done.
</output>
