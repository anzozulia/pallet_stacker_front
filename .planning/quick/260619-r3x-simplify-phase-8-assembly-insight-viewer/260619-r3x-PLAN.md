---
phase: quick-260619-r3x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/computeLayers.ts
  - src/components/viewer/Boxes.tsx
  - src/components/viewer/LayerControls.tsx
  - src/routes/ResultPage.tsx
  - src/components/result/PlacementList.tsx
  - src/components/result/PlacementList.test.tsx
  - e2e/assembly-insight.spec.ts
autonomous: true
requirements: [RESULT-07]

must_haves:
  truths:
    - "EXPLODE is a single toggle button (Assembled ⇄ Exploded), not a slider; on = full separation, off = byte-identical assembled stack"
    - "LAYERS is build-up only with + / − stepper buttons revealing layers floor-up; no Isolate mode anywhere"
    - "Clicking a placement-list row builds the stack UP TO that box's layer so the box becomes visible (not isolate)"
    - "The isolate-era persistent 'selected' card cue is gone"
    - "A pallet switch resets explode to assembled and layers to All, camera preserved (no snap)"
    - "CoG hides while exploded and returns when assembled; camera re-frames on explode toggle"
    - "three stays in the lazy /result chunk; computeLayers stays three-free"
  artifacts:
    - path: "src/lib/computeLayers.ts"
      provides: "EXPLODE_FIXED_UNIT bumped 350 → 500"
      contains: "EXPLODE_FIXED_UNIT = 500"
    - path: "src/components/viewer/LayerControls.tsx"
      provides: "Explode toggle button + Layers +/- steppers; no mode toggle, no sliders"
    - path: "src/components/viewer/Boxes.tsx"
      provides: "layerAppearance build-up only; GHOST_OPACITY + isolate branch removed"
    - path: "src/routes/ResultPage.tsx"
      provides: "binary explode toggle, build-up level state, row-click→build-up, reset-on-switch without focusMode/selectedId"
    - path: "src/components/result/PlacementList.tsx"
      provides: "row-click → build-up seam; selected-cue removed"
  key_links:
    - from: "src/routes/ResultPage.tsx"
      to: "src/components/viewer/Boxes.tsx"
      via: "explode (0|1) + focusIndex props"
      pattern: "explode=\\{explode\\}"
    - from: "src/components/result/PlacementList.tsx"
      to: "src/routes/ResultPage.tsx"
      via: "row-click callback → setFocusIndex(layer+1)"
      pattern: "onClick"
---

<objective>
Simplify the just-built Phase 8 assembly-insight viewer controls (RESULT-07). The current overlay bar is too bulky: an Explode range slider, a Build-up/Isolate mode toggle, and a Layers range slider, plus an isolate-driven persistent "selected" card cue. Replace with two compact controls — an Explode TOGGLE button and Layers +/− STEPPERS — and remove the Isolate mode entirely (build-up is the only layer mode). Re-target the placement-row click from "isolate that layer" to "build up to that box's layer".

This is a UI revision of existing, working code. Modify in place; do NOT rebuild from scratch. Reuse the same overlay anchoring, token classes, and r3f wiring already present.

Purpose: leaner, less-bulky controls that read better and drop the redundant Isolate mode the user no longer wants.
Output: simplified LayerControls + Boxes + ResultPage + PlacementList, with EXPLODE_FIXED_UNIT bumped 350→500, and all unit/e2e tests updated and green.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/lib/computeLayers.ts
@src/components/viewer/Boxes.tsx
@src/components/viewer/LayerControls.tsx
@src/routes/ResultPage.tsx
@src/components/result/PlacementList.tsx
@src/components/result/PlacementList.test.tsx
@e2e/assembly-insight.spec.ts

Key facts confirmed during planning (do not re-discover):
- NO golden test asserts the EXPLODE_FIXED_UNIT literal. `computeLayers.test.ts` only asserts base-z banding (`350` there is a box height / baseZ, NOT the explode unit). `camera-presets.test.ts` tests `inflateBboxForExplode` with its OWN `extraHeight=600` argument and never imports EXPLODE_FIXED_UNIT. So bumping 350→500 changes no existing assertion.
- Boxes already lifts each layer by `layerIndex * explode * EXPLODE_FIXED_UNIT`, so a binary `explode` of 0 or 1 is sufficient — no slider needed.
- The CoG-hide gate is `cogVisible = cogOn && explode === 0` and the explode camera re-frame uses `explodeNonce`. Both must be preserved by the toggle.
- The decision for the build-up top of range: **the maximum is "All"** (i.e. stepping + past N collapses to `null`/All), so the readout reads `All` at full and `Layers 1–k / N` below it. Clamp the build-up level to `[0..N]` where 0 = no boxes and the top (>=N) = All (null). Keep readout and behavior consistent with this.
- LayerControls/PlacementList stay three-free (code-split gate C-04). Do not add any three/r3f/drei import to PlacementList.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Explode → toggle button + bump EXPLODE_FIXED_UNIT</name>
  <files>src/lib/computeLayers.ts, src/components/viewer/LayerControls.tsx, src/routes/ResultPage.tsx, e2e/assembly-insight.spec.ts</files>
  <action>
In src/lib/computeLayers.ts change EXPLODE_FIXED_UNIT from 350 to 500 (update the surrounding doc comment's stated magnitude if it cites 350). This is the single shared explode unit Boxes and ResultPage both import; no test asserts the literal, so no golden update is needed.

In src/components/viewer/LayerControls.tsx replace the Explode `<input type="range">` (min 0 max 1 step 0.05) and its `{x}x`/`Assembled` readout span with a SINGLE toggle button. The toggle is binary: pressed = Exploded (explode 1), unpressed = Assembled (explode 0). Keep the bottom-center anchoring, the `pointer-events-none/auto` wrapper, the `font-mono text-xs` bar chrome, the divider, and the "Explode" label. Style the toggle as the same accent-on / neutral-off pill pattern the existing mode buttons used (`role="switch"`, `aria-checked`, accent `bg-accent text-white` when on; neutral `bg-[#1a2030] text-[var(--color-d-text-2)]` when off). Disable it when `layerCount === 0` (empty), readout/label still legible. Derive `exploded` from the incoming `explode` prop (`explode > 0`). The button's accessible name must remain discoverable as "Explode" so existing flows can target it; set `aria-label="Explode"`. Keep a visible text readout near it: `Exploded` when on, `Assembled` when off, `No boxes` when empty — so the e2e `getByText('Assembled')` still resolves and a new `Exploded`/`Assembled` toggle assertion can target text.

Update the LayerControls props: replace `onExplode: (value: number) => void` with a binary intent — keep the prop name `onExplode` but have it called with the NEW numeric explode (0 or 1) so ResultPage's existing `onExplode` handler (which bumps explodeNonce) needs no rename. The button's onClick calls `onExplode(exploded ? 0 : 1)`.

In src/routes/ResultPage.tsx: the existing `onExplode = (v) => { setExplode(v); setExplodeNonce(n=>n+1); }` already works for a 0/1 value — keep it AS IS (the nonce bump drives the camera re-frame on every toggle). Keep `cogVisible = cogOn && explode === 0` unchanged. No change to selectPallet here (it already sets explode 0; Task 2 handles the focus-state cleanup).

In e2e/assembly-insight.spec.ts: the explode helper currently does `explodeSlider(page).fill(String(value))`. Replace the `explodeSlider` locator + `setExplode` helper so it CLICKS the explode toggle (target by accessible name "Explode") to reach the desired binary state, then waits for `__cameraState.settled`. Add/adjust assertions so `assembled-default` checks the toggle reads `Assembled` (off) and `explode-gaps`/CoG/compose scenarios toggle it ON and assert `Exploded`. The `1.0x` text assertion is gone — replace with the `Exploded` text. Keep the CoG-hidden-while-exploded, camera-unchanged-on-switch, and compose-with-preset+heatmap scenarios working against the toggle.
  </action>
  <verify>
    <automated>grep -q "EXPLODE_FIXED_UNIT = 500" src/lib/computeLayers.ts && ! grep -n "type=\"range\"" src/components/viewer/LayerControls.tsx | grep -qi explode && npm run typecheck</automated>
  </verify>
  <done>EXPLODE_FIXED_UNIT is 500; the Explode control is a single toggle button (no range input for explode); on→explode 1, off→explode 0; explodeNonce still bumps on toggle; cogVisible gate unchanged; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Layers build-up-only with +/- steppers; remove Isolate from Boxes + LayerControls</name>
  <files>src/components/viewer/Boxes.tsx, src/components/viewer/LayerControls.tsx, src/routes/ResultPage.tsx</files>
  <action>
In src/components/viewer/Boxes.tsx: remove Isolate ENTIRELY.
  - Delete the `GHOST_OPACITY` constant.
  - Remove the `focusMode` prop from `BoxesProps` and from the component signature/destructure.
  - Rewrite `layerAppearance(layerIndex, focusIndex)` to build-up only: `focusIndex == null` → `{ visible: true, opacity: 1 }`; otherwise `{ visible: layerIndex <= focusIndex - 1, opacity: 1 }`. Opacity is ALWAYS 1 and `transparent` is ALWAYS false (delete the `const transparent = opacity < 1` derivation and pass `transparent={false}`, `opacity={1}` — or drop them to defaults). Update the function's doc comment to describe build-up only (drop the isolate/ghost prose). Keep the export.
  - In the JSX, replace the `layerAppearance(layerIndex, focusMode, focusIndex)` call with `layerAppearance(layerIndex, focusIndex)` and drop the opacity/transparent plumbing accordingly.

In src/components/viewer/LayerControls.tsx: build-up is the only mode.
  - Remove the `focusMode` and `onFocusMode` props and the Build-up/Isolate two-pill mode toggle block entirely.
  - Replace the Layers `<input type="range">` + its readout with a compact stepper cluster: a `−` button, the readout, and a `+` button. The `+` button increases revealed layers, `−` decreases. Keep the "Layers" label and the divider.
  - State model (1-based focusIndex, null = All): the control receives `focusIndex: number | null` and `layerCount: number`, and calls `onFocusIndex(next | null)`. Define a current numeric level `k = focusIndex ?? layerCount` (All shows as N). `−` sets level `k-1` clamped to `[0..layerCount]`; `+` sets level `k+1`. When the resulting level is `>= layerCount` (or `layerCount` itself), pass `null` (collapse to All) — top of range is All. When level is `0`, pass `0` (no boxes revealed; ResultPage maps 0 to focusIndex 0 → build-up hides everything since `layerIndex <= -1` is never true). Disable `+` at All and `−` at 0; disable the whole cluster when `layerCount <= 1` (single/empty) as today.
  - Readout (compact, tabular-nums, same token classes): `No boxes` when empty; `Single layer` when `layerCount === 1`; `All` when `focusIndex == null` (or `>= layerCount`); else `Layers 1–${k} / ${layerCount}`. Buttons get accessible labels: `aria-label="Reveal one more layer"` (+) and `aria-label="Reveal one fewer layer"` (−) so e2e can target them by role/name.

In src/routes/ResultPage.tsx:
  - Remove the `focusMode` state (`const [focusMode, setFocusMode] = useState(...)`) and `setFocusMode` usage.
  - Keep `focusIndex` state. Remove the `selectedId` state here only if Task 3 hasn't already (Task 3 owns it; if running sequentially, leave the row-click rewrite to Task 3 — but DO drop `focusMode` from the `selectPallet` reset and from the `<LayerControls>` / `<Boxes>` prop lists in THIS task).
  - In `selectPallet`, drop `setFocusMode('buildup')` (mode no longer exists); keep `setExplode(0)` and `setFocusIndex(null)`.
  - In the `<LayerControls .../>` JSX remove `focusMode`/`onFocusMode` props; keep `focusIndex` and `onFocusIndex={setFocusIndex}`.
  - In the `<Boxes .../>` JSX remove the `focusMode` prop; keep `focusIndex`.
  </action>
  <verify>
    <automated>! grep -rn "GHOST_OPACITY\|focusMode\|isolate\|Isolate" src/components/viewer/Boxes.tsx src/components/viewer/LayerControls.tsx src/routes/ResultPage.tsx && npm run typecheck</automated>
  </verify>
  <done>Boxes has no GHOST_OPACITY, no focusMode, no isolate branch; layerAppearance is build-up only (opacity always 1, transparent false). LayerControls has no mode toggle and no range inputs — only +/− steppers with an `All`/`Layers 1–k / N` readout. ResultPage no longer references focusMode. typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Row-click → build-up + remove selected cue + update all tests</name>
  <files>src/routes/ResultPage.tsx, src/components/result/PlacementList.tsx, src/components/result/PlacementList.test.tsx, e2e/assembly-insight.spec.ts</files>
  <action>
In src/routes/ResultPage.tsx:
  - Remove the `selectedId` state (`const [selectedId, setSelectedId] = useState(...)`) and all `setSelectedId` calls.
  - Rewrite the placement-row seam: it currently calls `onIsolate` to set Isolate focus. Change it to build UP to the clicked box's layer: map `id → layerModel.itemToLayer.get(id)`, and if defined set `setFocusIndex(li + 1)` so the build-up reveals floor-up through that box's layer (the box becomes visible). Keep the `li != null` no-op guard (T-08-DOS). Do NOT touch hover state (the Phase-6 hover↔mesh highlight stays).
  - Rename the prop passed to PlacementList from `onIsolate` to a clearer name `onRevealToLayer` (or keep `onIsolate` name if you prefer minimal churn — but the BEHAVIOR must be build-up, not isolate). Whichever name, update PlacementList's prop in lockstep. Remove the `selectedId={selectedId}` prop from `<PlacementList>`.

In src/components/result/PlacementList.tsx:
  - Remove the persistent selected cue: delete the `selectedId` prop, the `isSelected` derivation, the `data-selected` attribute, and the `ring-accent`/`border-accent ring-1` selected branch. The card className collapses to: hovered → `border-accent bg-accent-weak`, else → `border-border bg-surface`.
  - Keep the one-way hover (onMouseEnter/Leave → onHover) UNCHANGED.
  - Keep the row `onClick` calling the (renamed) callback with `item.item_id`. Update the prop name + its doc comment to say "row-click → build up to this box's layer" instead of "isolate". Keep `role="button"`, `tabIndex={0}`, `cursor-pointer`.

In src/components/result/PlacementList.test.tsx:
  - Drop the `marks the selectedId row with the persistent selected cue (data-selected)...` test entirely (the cue is gone).
  - Update the `fires onIsolate(item_id) on row click` test: rename to reflect build-up, assert the (renamed) callback fires with `'T000'` on click. Keep the human-label/size/weight/support test and the hover test as-is (those behaviors are unchanged).

In e2e/assembly-insight.spec.ts:
  - Remove the isolate scenarios: delete `isolate-dims the non-focused layers (D-09)` and the isolate half of `build-up-hides upper layers (D-08)` (drop the `getByRole('switch', { name: 'Isolate' })` clicks and the isolate-vs-buildup comparison). Rework `build-up-hides upper layers` to: default reads `All`; click `+`/`−` steppers (target by the new aria-labels) to reveal layer 1 of 2 (readout `Layers 1–1 / 2`); assert the canvas differs from All.
  - Update the layers helper: replace `layersSlider`/`setLayer(value)` (which used `.fill`) with helpers that click the `+`/`−` stepper buttons to reach a target revealed-layer count, then wait ~250ms.
  - Rework `row-click → isolate with persistent selected cue` into `row-click → build up to that box's layer`: click the first placement card, assert the canvas differs from assembled (the build-up changed the scene), and assert hover still works (hover the card, no error). Drop the `data-selected` assertion (cue removed).
  - Update `reset-on-switch...`: drive explode (toggle) + reveal a layer via steppers on P001, switch to P002, assert readouts return to `Assembled` + `All`, camera preserved (existing epsilon check).
  - KEEP: `camera-unchanged-on-switch`, `assembled-default` (now toggle-based), `compose-with-preset+heatmap`, and `full compose: build-up + explode + preset + CoG + heatmap` — but update them to use the toggle + steppers instead of the removed sliders, and drop any Isolate references.
  </action>
  <verify>
    <automated>! grep -rn "selectedId\|data-selected\|Isolate\|onIsolate" src/routes/ResultPage.tsx src/components/result/PlacementList.tsx src/components/result/PlacementList.test.tsx e2e/assembly-insight.spec.ts && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>Row click builds the stack up to that box's layer (setFocusIndex(layer+1)), not isolate. The persistent selected cue (selectedId/data-selected/ring) is gone from PlacementList and ResultPage. PlacementList.test.tsx drops the selected-cue test and asserts build-up-on-row-click. assembly-insight.spec.ts has no isolate scenarios and covers explode toggle + +/- steppers + row-click→build-up. typecheck, lint, and unit tests pass.</done>
</task>

</tasks>

<verification>
GATE — all must stay green (run from repo root):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npx playwright test e2e/assembly-insight.spec.ts`
- `npm run build && node scripts/check-code-split.mjs` (three stays in the lazy /result chunk; computeLayers + LayerControls + PlacementList stay three-free)

Manual spot-check (optional, not part of the gate): the overlay bar now shows an Explode toggle button and Layers − / readout / + steppers, no sliders, no Build-up/Isolate mode toggle.
</verification>

<success_criteria>
- EXPLODE_FIXED_UNIT = 500 in src/lib/computeLayers.ts.
- Explode is a single toggle button: on → explode 1 (full separation), off → explode 0 (byte-identical assembled). explodeNonce bumps on toggle (camera re-frames); CoG hides while exploded, returns when assembled.
- Layers is build-up only with + / − steppers; no Isolate mode in Boxes or LayerControls; opacity always 1 / transparent false. Readout reads `All` at full and `Layers 1–k / N` below, clamped [0..N] with top = All.
- Clicking a placement row builds up to that box's layer (setFocusIndex(layer+1)); the box becomes visible. Hover↔mesh highlight (Phase 6) unchanged. No persistent selected cue.
- Pallet switch resets explode to assembled + layers to All; camera preserved (no snap). focusMode/isolate/selectedId state removed.
- All five gate commands green.
</success_criteria>

<output>
Create `.planning/quick/260619-r3x-simplify-phase-8-assembly-insight-viewer/260619-r3x-SUMMARY.md` when done.
</output>
