---
phase: quick-260617-v8x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/config/ConfigForm.tsx
  - src/routes/LoadingPage.tsx
  - src/routes/ResultPage.tsx
  - index.html
  - src/components/Hello.tsx
  - src/components/viewer/ViewerOverlay.tsx
  - src/components/result/PalletSwitcher.tsx
  - src/components/viewer/CogMarker.tsx
  - src/components/viewer/CameraPresets.tsx
  - src/styles.css
autonomous: true
requirements: [QUICK-260617-v8x]

must_haves:
  truths:
    # Task A — cross-page chrome
    - "#2 The brand wordmark reads 'Pallet Packer' (no 'Palletize', no 'pack studio' sub-label) in all three topbars (Configure, Loading, Result) and the document <title>."
    - "#1 On the Configure page the last box card has clear breathing room above the sticky full-width footer — the footer's top border no longer overlaps the last card's border."
    - "#3 On the Loading page the Cancel button turns red (danger token) on hover instead of neutral text colour."
    # Task B — result rail / overlay
    - "#5 The 3D viewer's top-right by-type legend shows box TYPE NAMES (labels), not raw type codes/ids; no raw type code remains anywhere in the viewer overlay chrome."
    - "#7 The 3D viewer column is ~15% narrower and the right info rail is correspondingly wider (rail fixed width grown from 440px to ~600px); the <900px single-column stack behaviour is preserved."
    - "#8 The selected pallet card in the switcher is visibly distinct — its fill no longer reads as the same grey as the unfilled progress track (#edeef1) or the page background; the non-colour-only cue (accent border + ring + filled index chip) is preserved."
    # Task C — 3D viewer bugs
    - "#4 BUG: the Centre-of-gravity marker (sphere + drop-line) is VISIBLE over/through the opaque box stack — it is no longer occluded inside the tower."
    - "#6 BUG: TOP (and every preset) reaches its final orientation WITHOUT an end-of-transition snap — the slerp endpoint equals OrbitControls' settled orientation (both derived from three's actual look-at with camera.up)."
  artifacts:
    - path: "src/features/config/ConfigForm.tsx"
      provides: "Rebranded Configure topbar + bottom padding on <main> to clear the sticky footer"
      contains: "Pallet Packer"
    - path: "src/routes/LoadingPage.tsx"
      provides: "Rebranded Loading topbar + Cancel hover:text-danger"
      contains: "hover:text-danger"
    - path: "src/routes/ResultPage.tsx"
      provides: "Rebranded Result topbar + widened rail grid + label-resolved legend prop"
      contains: "Pallet Packer"
    - path: "index.html"
      provides: "Document title 'Pallet Packer'"
      contains: "<title>Pallet Packer</title>"
    - path: "src/components/viewer/ViewerOverlay.tsx"
      provides: "By-type legend rendering labels (no raw type code in chrome)"
    - path: "src/components/result/PalletSwitcher.tsx"
      provides: "Selected-card fill bumped to a distinct accent tint"
    - path: "src/components/viewer/CogMarker.tsx"
      provides: "Always-visible CoG overlay (depthTest=false + high renderOrder on sphere and drop-line)"
      contains: "renderOrder"
    - path: "src/components/viewer/CameraPresets.tsx"
      provides: "Orientation endpoints from three's Matrix4.lookAt(camera.up) so slerp endpoint == OrbitControls settled orientation"
      contains: "Matrix4"
    - path: "src/styles.css"
      provides: "Accent-weak (or a stronger selected-card token) tuned so the selected pallet card pops"
  key_links:
    - from: "src/routes/ResultPage.tsx"
      to: "src/components/viewer/ViewerOverlay.tsx"
      via: "legend prop built as [typeToLabel.get(typeId) ?? typeId, hex]"
      pattern: "typeToLabel"
    - from: "src/components/viewer/CameraPresets.tsx"
      to: "three Matrix4/Quaternion"
      via: "toQuat = setFromRotationMatrix(lookAt(toPos, toTarget, camera.up)); fromQuat = camera.quaternion.clone()"
      pattern: "setFromRotationMatrix"
    - from: "src/components/viewer/CogMarker.tsx"
      to: "three material/Line render state"
      via: "depthTest={false} + renderOrder on sphere material and drop-line"
      pattern: "depthTest"
---

<objective>
Land 8 verified Configure / Loading / Result / 3D-viewer fixes from quick task 260617-v8x — including two real bugs (the invisible Centre-of-gravity marker, #4; and the TOP-preset end-of-transition snap, #6) — in a single 3-task plan.

Purpose: cross-page rebrand to "Pallet Packer", de-clutter and rebalance the Result page, fix the two visible 3D bugs. All root causes are pre-identified; this plan implements the specific fixes, not exploration.
Output: edits across config/loading/result chrome, the viewer overlay + 3D components, the pallet switcher, styles, and index.html — with the full gate kept green and changed-behaviour tests updated.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Cross-page chrome (Task A)
@src/features/config/ConfigForm.tsx
@src/routes/LoadingPage.tsx
@src/routes/ResultPage.tsx
@index.html
@src/components/Hello.tsx

# Result rail / overlay (Task B)
@src/components/viewer/ViewerOverlay.tsx
@src/components/result/PalletSwitcher.tsx
@src/styles.css

# 3D viewer bugs (Task C)
@src/components/viewer/CogMarker.tsx
@src/components/viewer/CameraPresets.tsx
@src/lib/camera-presets.ts

# Tests that gate the changed behaviour
@e2e/result-viewer.spec.ts
@src/lib/camera-presets.test.ts
@src/components/result/PalletSwitcher.test.tsx
@src/routes/LoadingPage.test.tsx
</context>

<constraints>
- Executor runs on the MAIN working tree (NOT a worktree); node_modules present; a dev server is running on :5173 — do NOT kill it; use it to eyeball the CoG and camera fixes.
- Code-split gate (`node scripts/check-code-split.mjs`) MUST stay green: config files (ConfigForm, defaults, etc.) stay three-free; the viewer files (CogMarker, CameraPresets, ViewerOverlay, ResultPage) stay in the lazy /result chunk. Do not introduce a three/r3f/drei import into any eager-chunk file.
- Do NOT change the localStorage STORAGE_KEY `'palletize:config:v1'` in src/lib/config-persist.ts — renaming it wipes users' saved drafts. It is an internal key, not user-facing brand text.
- Do NOT regress prior fixes: StrictMode loading fix, #5/#8 tally fix, code-split, camera-preservation-on-switch (D-02 / measureNonce), opaque preset/toggle buttons, the realistic two-direction pallet model + deck-top invariant.
- Full gate must pass at the end: `npm test`, `npm run typecheck`, `npm run lint`, `npm run build`, `node scripts/check-code-split.mjs`, `npm run test:e2e`.
- Units stay mm (integers) / kg. No new dependencies.
</constraints>

<tasks>

<task type="auto">
  <name>Task A: Cross-page chrome — rebrand (#2), Configure footer spacing (#1), Loading Cancel hover red (#3)</name>
  <files>src/features/config/ConfigForm.tsx, src/routes/LoadingPage.tsx, src/routes/ResultPage.tsx, index.html, src/components/Hello.tsx</files>
  <action>
    #2 Rebrand to "Pallet Packer" (title case) and DROP the "pack studio" sub-label in all three topbars:
    - ConfigForm.tsx: replace the `Palletize` brand text (line ~96) with `Pallet Packer` and DELETE the `<small>...pack&nbsp;studio</small>` sub-label element (lines ~97-99). Keep the brand glyph `<span aria-hidden>`.
    - LoadingPage.tsx: same change in the shared `Topbar()` (line ~320 `Palletize` -> `Pallet Packer`; delete the `pack&nbsp;studio` `<small>` lines ~321-323).
    - ResultPage.tsx: same change in the Result header (line ~179 `Palletize` -> `Pallet Packer`; delete the `pack&nbsp;studio` `<small>` lines ~180-182).
    - index.html: change `<title>Palletize</title>` (line ~6) to `<title>Pallet Packer</title>`.
    - src/components/Hello.tsx: unused demo component, not user-facing. If it renders the string "Palletize", update it to "Pallet Packer" so the brand-text grep is clean; otherwise leave it. Keep it cheap — do not restructure it.
    - DO NOT touch src/lib/config-persist.ts STORAGE_KEY `'palletize:config:v1'`.

    #1 Configure footer spacing: ConfigForm.tsx `<main>` (line ~121) is `mx-auto w-full max-w-[960px] px-6 pt-12 font-sans` with NO bottom padding, so the last box card sits flush under the sticky full-width FooterBar and the footer's top border overlaps the card border. Add generous bottom padding to clear the sticky footer (e.g. `pb-28` or `pb-32` — pick the value that visibly separates the last card from the footer; the FooterBar height is the sizing reference). Pure Tailwind class change on the `<main>`.

    #3 Loading Cancel hover red: LoadingPage.tsx Cancel button (line ~301) ends its className with `hover:text-text`. Change ONLY that hover colour token to the danger/red token `hover:text-danger`. Keep every other class (layout, base `text-text-3`, transition) intact.

    Keep all three route/component files three-free (no viewer imports) — they are eager-chunk / loading-chunk files; this task adds no imports.
  </action>
  <verify>
    <automated>cd /Users/anzozulia/Desktop/pallet_stacker_front && ! grep -rqn "Palletize" src/features/config/ConfigForm.tsx src/routes/LoadingPage.tsx src/routes/ResultPage.tsx src/components/Hello.tsx index.html && grep -q "<title>Pallet Packer</title>" index.html && grep -q "hover:text-danger" src/routes/LoadingPage.tsx && grep -Eq "pb-(2[0-9]|3[0-9])" src/features/config/ConfigForm.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    No file under src/ or index.html contains the string "Palletize" (STORAGE_KEY in config-persist.ts is unchanged and is the only `palletize:` occurrence allowed). All three topbars and the document title read "Pallet Packer" with no "pack studio" sub-label. The Configure `<main>` has a large bottom padding clearing the sticky footer. The Loading Cancel button uses `hover:text-danger`. typecheck + lint pass.
  </done>
</task>

<task type="auto">
  <name>Task B: Result rail/overlay — legend names (#5), viewer/rail resize (#7), selected-card contrast (#8)</name>
  <files>src/components/viewer/ViewerOverlay.tsx, src/routes/ResultPage.tsx, src/components/result/PalletSwitcher.tsx, src/styles.css, e2e/result-viewer.spec.ts, src/components/result/PalletSwitcher.test.tsx</files>
  <action>
    #5 Legend shows TYPE NAMES, not codes: in ResultPage.tsx the `legend` is built (line ~146) as `[...palette.entries()]` — each entry is `[typeId, hexColor]` where `typeId` is the raw recovered code (e.g. "D"/"F"/"T"). The `typeToLabel` Map (typeId -> human label) is already in scope (read from nav state, line ~79). Resolve labels at the legend build: change the legend memo to map each `[typeId, hex]` to `[typeToLabel?.get(typeId) ?? typeId, hex]`. KEEP the `?? typeId` fallback — it is load-bearing: in the e2e flow the fixture's recovered typeIds (D/F/T) have NO matching entry in the DEFAULT_CONFIG-derived `typeToLabel` (its keys are the request's `b{nanoid}` ids), so the fallback preserves the existing e2e legend assertions (`D`/`F`/`T` still render). Add `typeToLabel` to the memo deps. Do NOT change ViewerOverlay's `legend: [string, string][]` prop shape — the first tuple element is now a label string instead of a code. Also scan ViewerOverlay for any OTHER raw type-code rendering (title/subline are pallet-level, not type codes; the support-heatmap SUPPORT_KEY is a fixed labelled scale — LEAVE it). The by-type legend (`data-viewer-legend`) is the known spot.

    #7 Viewer ~15% narrower + rail wider: ResultPage.tsx outer grid (line ~169) is `grid-cols-[1fr_440px]` (viewer=1fr, rail=440px). Grow the rail fixed width to ~600px (clean value in the 560-600px range) so the 1fr viewer column shrinks ~15% on common 1400-1600px screens. KEEP `max-[900px]:grid-cols-1` and the `max-[900px]:grid-rows-...` stack behaviour unchanged.

    #8 Selected pallet-card contrast: PalletSwitcher.tsx selected row (line ~60) uses `bg-accent-weak` + `border-accent` + `ring-1 ring-inset ring-accent`. The token `--color-accent-weak` is `#ecebfd` (styles.css line ~45) — a very pale near-grey that reads like the `#edeef1` progress track and the page bg. Make the selected fill clearly pop while KEEPING the non-colour-only cues (accent border + ring + filled index chip) intact. Choose ONE: (a) swap the selected card's `bg-accent-weak` for a stronger accent tint utility (e.g. `bg-accent/10` or `bg-accent/15` — a higher-opacity accent that is unmistakably purple-tinted, not grey); OR (b) add a dedicated stronger token in styles.css (e.g. `--color-accent-weak-2`) and use it ONLY on this card. Prefer (a) for minimal blast radius unless the Tailwind v4 `@theme` setup makes the bare `bg-accent/NN` opacity utility unavailable — in that case use (b). Verify the selected card no longer matches `#edeef1`/`#eceef1`/page bg.

    Test updates (don't leave stale tests):
    - e2e/result-viewer.spec.ts: confirm the legend assertions (`D`/`F`/`T`, lines ~98-100) still pass with the `?? typeId` fallback (they should — the stubbed flow uses DEFAULT_CONFIG whose label keys never match D/F/T). If the rendered legend now diverges, update the assertions to the labels actually rendered; otherwise leave them.
    - PalletSwitcher.test.tsx: if any test asserts the selected style by class (`bg-accent-weak`), update it to the new selected-fill class. The existing tests assert aria-pressed + onSelect + no-warn (lines ~34-54) — those are unaffected; only touch class-based selected-style assertions if present.

    Keep ViewerOverlay, ResultPage, PalletSwitcher within their existing code-split tiers (ViewerOverlay/ResultPage lazy /result; PalletSwitcher three-free rail) — this task adds no three imports.
  </action>
  <verify>
    <automated>cd /Users/anzozulia/Desktop/pallet_stacker_front && grep -q "typeToLabel" src/routes/ResultPage.tsx && grep -Eq "grid-cols-\[1fr_(5[6-9][0-9]|600)px\]" src/routes/ResultPage.tsx && ! grep -q "bg-accent-weak ring-1" src/components/result/PalletSwitcher.tsx && npm run typecheck && npm run lint && npx vitest run src/components/result/PalletSwitcher.test.tsx</automated>
  </verify>
  <done>
    The viewer legend resolves type labels via `typeToLabel?.get(typeId) ?? typeId` (deps include typeToLabel). The Result grid rail is ~560-600px (viewer ~15% narrower) with the <900px stack preserved. The selected pallet card uses a distinctly accent-tinted fill (not `#ecebfd`-grey) while keeping border+ring+filled-chip cues. PalletSwitcher unit tests and typecheck/lint pass; e2e legend assertions still hold (or are updated to the rendered labels).
  </done>
</task>

<task type="auto">
  <name>Task C: 3D viewer bugs — CoG occlusion fix (#4), TOP transition end-snap fix (#6)</name>
  <files>src/components/viewer/CogMarker.tsx, src/components/viewer/CameraPresets.tsx, src/lib/camera-presets.test.ts</files>
  <action>
    #4 BUG (CoG marker invisible — occlusion): CogMarker.tsx renders the marker sphere + drop-line at the pallet's centre-of-gravity, which sits INSIDE the opaque box stack (dead-centre of the tower), so the box meshes depth-occlude it and the user sees nothing. `mapCog` and the props are correct (golden-tested) — the fix is render state, NOT math. Make the marker an ALWAYS-VISIBLE overlay drawn over the boxes:
    - On the sphere `<mesh>`: set a high `renderOrder` (e.g. `renderOrder={999}`). On its material set `depthTest={false}` and `depthWrite={false}`. Either keep `meshStandardMaterial` with those flags, or switch to `meshBasicMaterial` (lit-independent, reads consistently as an overlay) — basic is simplest for an always-on marker. Optionally bump the sphere radius modestly (e.g. 14 -> ~18) for legibility.
    - On the drei `<Line>` drop-line: the drei Line accepts `renderOrder` (set ~999) and exposes material props — set `depthTest={false}` so the line draws over the boxes too. Keep it dashed/transparent.
    - Keep CogMarker entirely three/drei inside the lazy /result chunk (no new eager imports). Use the running :5173 dev server to confirm the white sphere + drop-line are now visible over the box tower.

    #6 BUG (TOP end-snap): CameraPresets.tsx slerps the camera orientation during the transition (lines ~135-163) using endpoints from the PURE `lookQuaternion` (camera-presets.ts). For straight-down TOP, `lookQuaternion`'s degenerate-up handling (`altUp=[1,0,0]`) produces a DIFFERENT roll than three's own `Matrix4.lookAt` (which OrbitControls.update() reasserts on the settled frame), so on the final frame the camera snaps ~90deg. FIX: compute the orientation endpoints with three's ACTUAL look-at so the slerp endpoint equals OrbitControls' settled orientation:
    - Import `Matrix4` from `three` (alongside the existing `Quaternion`, `Vector3`, `Box3`).
    - In the animation-setup effect (lines ~113-142): set `fromQuat` from the camera's ACTUAL current orientation — `camera.quaternion.clone()` (read as `[x,y,z,w]`) — NOT a recomputed `lookQuaternion(fromPos, fromTarget)`.
    - Set `toQuat` from three's look-at at the target pose: build `new Matrix4().lookAt(toPosVec, toTargetVec, camera.up)` then `new Quaternion().setFromRotationMatrix(m)` and read its `[x,y,z,w]`. (Equivalently: clone the camera, set its position to `toPos`, call `.lookAt(toTarget)`, read `.quaternion`.) `camera.up` MUST be the up used, matching what OrbitControls.update() uses, so the slerp endpoint == the settled orientation -> continuous handoff, no snap, for TOP and every preset.
    - `slerpQuat` is still used for the per-frame interpolation (keep it). If the pure `lookQuaternion`/`presetQuaternion` become unused by the COMPONENT, KEEP them in camera-presets.ts because camera-presets.test.ts tests them (lines ~66-107) — do not delete the exports or break that gate.
    - PRESERVE: distinct ISO/TOP/FRONT framings, the `measureNonce` camera-preservation-on-switch (D-02 — `bbox` stays out of the animation-effect deps), the `__cameraState` test hook, and the e2e in e2e/result-viewer.spec.ts (it asserts distinct positions + active-button class + TOP-looks-straight-down via positions, NOT the quaternion).
    - The `Quaternion`/`Vector3`/`Matrix4`/`Box3` imports stay inside CameraPresets (lazy /result chunk) — code-split gate unaffected.

    Test: camera-presets.test.ts asserts on the PURE `lookQuaternion`/`slerpQuat`/`presetQuaternion`/`quatAngle` (still exported, unchanged behaviour) — those should still pass. If you trim or change any exported pure function's signature, update the corresponding tests accordingly; otherwise leave the test file untouched.
  </action>
  <verify>
    <automated>cd /Users/anzozulia/Desktop/pallet_stacker_front && grep -q "depthTest" src/components/viewer/CogMarker.tsx && grep -q "renderOrder" src/components/viewer/CogMarker.tsx && grep -q "Matrix4" src/components/viewer/CameraPresets.tsx && grep -q "camera.quaternion.clone" src/components/viewer/CameraPresets.tsx && npm run typecheck && npm run lint && npx vitest run src/lib/camera-presets.test.ts && node scripts/check-code-split.mjs</automated>
  </verify>
  <done>
    The CoG sphere + drop-line render with `depthTest={false}` and high `renderOrder`, visible over the box stack (confirmed on :5173). CameraPresets derives `toQuat` via three's `Matrix4().lookAt(...camera.up)` -> `Quaternion.setFromRotationMatrix` and `fromQuat` via `camera.quaternion.clone()`, so the slerp endpoint matches OrbitControls' settled orientation and TOP no longer end-snaps. D-02 preservation, the `__cameraState` hook, the pure camera-presets exports, and the code-split gate are all intact. camera-presets unit tests + typecheck + lint + code-split pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API done-response -> viewer render | Untrusted API strings (pallet_id, item_id-derived type codes, labels) cross into DOM/legend text. |
| nav-state -> ResultPage | `typeToLabel` Map carried via react-router state, already `instanceof Map`-validated upstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v8x-01 | Tampering/Info-disclosure | Legend label rendering (#5) | mitigate | `typeToLabel.get(...) ?? typeId` resolves to React text children only — never a raw-HTML sink; preserves the Phase-6 escaped-text rule (T-06-02). No new sink introduced. |
| T-v8x-02 | Denial of service | typeToLabel lookup | accept | `typeToLabel` is optional + already Map-validated (isResultNavState); `?? typeId` fallback means a missing/empty map degrades gracefully, never crashes the render. |
| T-v8x-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added by this plan; no install tasks. |
</threat_model>

<verification>
After all three tasks, run the FULL gate from the repo root and confirm green:
- `npm test`
- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `node scripts/check-code-split.mjs` (config files three-free; viewer files in lazy /result chunk)
- `npm run test:e2e`

Manual eyeball on :5173 (dev server already running — do NOT restart it):
- #4: open /result, confirm the white CoG sphere + dashed drop-line are visible over the box tower (toggle "Centre of gravity" on/off).
- #6: click TOP (and ISO/FRONT) — the transition sweeps smoothly to the final orientation with NO end snap.
- #1/#2/#3/#5/#7/#8: visually confirm footer spacing, "Pallet Packer" wordmark, red Cancel hover, label legend, narrower viewer / wider rail, and a clearly-distinct selected pallet card.
</verification>

<success_criteria>
- All 8 issues fixed per the truths in `must_haves`.
- Both bugs (#4 CoG occlusion, #6 TOP end-snap) resolved with the root-caused fixes (render-state overlay; three-lookAt orientation endpoints).
- No string "Palletize" remains anywhere except the unchanged localStorage STORAGE_KEY.
- Full gate green: test, typecheck, lint, build, check-code-split, test:e2e.
- No prior-fix regressions (StrictMode loading, tally, code-split, camera-preservation-on-switch, opaque buttons, realistic pallet model).
</success_criteria>

<output>
Create `.planning/quick/260617-v8x-config-spacing-rebrand-to-pallet-packer-/260617-v8x-SUMMARY.md` when done.
</output>
