---
phase: quick-260617-w8a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/config/demo-presets.ts
  - src/features/config/demo-presets.test.ts
  - src/features/config/DemoPresets.tsx
  - src/features/config/ConfigForm.tsx
  - src/features/config/ConfigForm.test.tsx
autonomous: true
requirements: [QUICK-260617-w8a]
---

<objective>
Add 3-4 vetted demo box-catalog presets to the Configure ("Packing task") page, pickable at the TOP of `<main>` (above the pallet config). Clicking a preset pre-fills the entire config form via `form.reset(cfg)`. The pallet envelope is FIXED for every preset — `{ length:1200, width:800, height:1800, maxWeight:1000, maxOverhang:0, allowOverhang:false }` — only the boxTypes vary.

Purpose: give first-time users a one-click path to a correct, explorable plan without hand-typing a catalog — the four presets are already validated against the live API (clean multi-pallet layouts, 0 unpacked, all rotation `uprightOnly`).

Output: a three-free preset data module + builder (`demo-presets.ts`), a presentational picker (`DemoPresets.tsx`), and the wiring in `ConfigForm.tsx`, plus co-located tests.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/features/config/ConfigForm.tsx
@src/features/config/defaults.ts
@src/types/config.ts
@src/features/config/schema.ts
@src/features/config/BoxCatalogCard.tsx
@src/features/config/PalletCard.tsx
@src/features/config/ConfigForm.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preset data + buildPresetConfig builder (three-free, tested)</name>
  <files>src/features/config/demo-presets.ts, src/features/config/demo-presets.test.ts</files>
  <behavior>
    - Every preset, when passed through `buildPresetConfig`, parses cleanly against `packConfigSubmitSchema` (the strict submit schema).
    - Every built config has the FIXED pallet `{ length:1200, width:800, height:1800, maxWeight:1000, maxOverhang:0, allowOverhang:false }`.
    - Every built config has ≥1 boxType; every boxType id is non-empty, letter-prefixed (matches `/^[^\d]/`, the `boxTypeSubmit` id regex), and unique within the config.
    - Every boxType has rotation `'uprightOnly'`, `fragile:false`, and `maxLoad` 90 (the makeDefaultBoxType default).
    - Two successive `buildPresetConfig(preset)` calls for the same preset produce DIFFERENT ids (fresh nanoid per call) — no shared/frozen id arrays.
    - `DEMO_PRESETS` has exactly 4 entries with the names/descriptions/box rows specified in the action.
  </behavior>
  <action>
Create `src/features/config/demo-presets.ts`. Mirror the file-header/code-split discipline of `defaults.ts`: import ONLY `nanoid` (from 'nanoid') and the `BoxType` / `PackConfig` types from `@/types/config` — NEVER three/r3f/drei or any viewer module (this file lives in the eager `/` chunk, C-05). Units are mm integers for dimensions/quantity, kg for weight, matching the model.

Define a `DemoPreset` type: `{ name: string; description: string; boxTypes: Array<Omit<BoxType, 'id'>> }`. The box-type templates carry every BoxType field EXCEPT `id`: label, length, width, height, weight, quantity, plus `maxLoad: 90`, `fragile: false`, `rotation: 'uprightOnly'`.

Export `const DEMO_PRESETS: DemoPreset[]` with EXACTLY these 4 entries (in this order):

1. name `'Office supply cartons'`, description `'Two uniform square cartons that fill two pallets perfectly.'`
   - label `'Paper-ream carton'`  — 400×400×300, weight 10, quantity 36
   - label `'File-storage box'`    — 400×400×450, weight 14, quantity 24
2. name `'Distribution-centre mix'`, description `'Three DC cartons, each cleanly fills its own pallet.'`
   - label `'Master carton (tall)'` — 600×400×450, weight 20, quantity 16
   - label `'Case box'`             — 600×400×300, weight 15, quantity 24
   - label `'Square tote'`          — 400×400×300, weight 9,  quantity 36
3. name `'Stationery & archive boxes'`, description `'Two square cartons at volume → three tidy pallets.'`
   - label `'Archive box'`    — 400×400×300, weight 10, quantity 48
   - label `'Catalogue box'`  — 400×400×450, weight 14, quantity 36
4. name `'Wholesale grocery cases'`, description `'Heavy cases + totes at volume → four full pallets.'`
   - label `'Grocery case (heavy)'` — 600×400×300, weight 16, quantity 48
   - label `'Produce tote'`          — 400×400×300, weight 10, quantity 72

(length×width×height = the three dimension fields; all box rows are `maxLoad:90, fragile:false, rotation:'uprightOnly'`.)

Export `function buildPresetConfig(preset: DemoPreset): PackConfig`. It returns a fresh full `PackConfig` on every call:
- `pallet`: a NEW object literal with the FIXED values `{ length:1200, width:800, height:1800, maxWeight:1000, maxOverhang:0, allowOverhang:false }`. There is NO `maxPallets` field anywhere (it was removed from the model — do not add it).
- `boxTypes`: `preset.boxTypes.map((t) => ({ id: \`b${nanoid(8)}\`, ...t }))` — each box gets a FRESHLY MINTED, letter-prefixed unique id per call (same scheme as `makeDefaultBoxType`, so the `b`-prefix keeps palette/typeKey/request ids valid). Spread the template AFTER setting the id so template fields are preserved.

Do NOT freeze or share the returned arrays/objects — RHF will own and mutate them after reset.

Create `src/features/config/demo-presets.test.ts` (vitest, no React/jsdom needed — pure-logic test). Import `DEMO_PRESETS`, `buildPresetConfig`, and `packConfigSubmitSchema` from `@/features/config/schema`. Cover the behaviors above: (a) `DEMO_PRESETS.length === 4`; (b) `it.each(DEMO_PRESETS)` — `packConfigSubmitSchema.safeParse(buildPresetConfig(preset)).success === true`; (c) for each built config assert the fixed pallet equals the expected object, every boxType id matches `/^[^\d]/` and is non-empty, ids are unique (`new Set(ids).size === ids.length`), and every rotation is `'uprightOnly'`; (d) `buildPresetConfig(p)` called twice yields disjoint id sets.
  </action>
  <verify>
    <automated>npm test -- src/features/config/demo-presets.test.ts</automated>
  </verify>
  <done>demo-presets.test.ts passes: all 4 presets build configs that parse against packConfigSubmitSchema with the fixed pallet, unique letter-prefixed ids, uprightOnly rotation, and fresh ids across calls. demo-presets.ts imports only nanoid + types (no three/r3f/drei).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DemoPresets picker UI + wire into ConfigForm via form.reset</name>
  <files>src/features/config/DemoPresets.tsx, src/features/config/ConfigForm.tsx, src/features/config/ConfigForm.test.tsx</files>
  <behavior>
    - Rendering ConfigForm shows a labelled "demos" section above the page H1 ("Packing task") with all 4 preset names visible and clickable.
    - Clicking a preset (e.g. "Distribution-centre mix") re-seeds the form: the box catalog now shows that preset's box-type labels (e.g. the name inputs hold "Master carton (tall)", "Case box", "Square tote") and the pallet fields hold 1200 / 800 / 1800.
    - Picking a preset replaces the prior catalog wholesale (a pick after the default single box leaves only the preset's boxTypes — count matches the preset).
    - All EXISTING ConfigForm tests stay green (Run gate, disabled-while-invalid, shell heading + both cards + tally).
  </behavior>
  <action>
Create `src/features/config/DemoPresets.tsx` — a presentational, three-free component. Mirror the code-split header note used in PalletCard/BoxCatalogCard: imports ONLY React, `@/features/config/demo-presets`, `@/types/config` (and optionally `clsx` for class toggling) — NEVER three/r3f/drei. Props: `{ onPick: (config: PackConfig) => void }` (import `PackConfig` from `@/types/config`, `DEMO_PRESETS` + `buildPresetConfig` from `@/features/config/demo-presets`).

Render a labelled section (e.g. a `SectionLabel`-style eyebrow "Try a demo" + a short sub-line like "Prefill a vetted catalog — the pallet stays 1200×800×1800.") followed by a flex row of clickable preset cards/buttons that WRAP on narrow screens (`flex flex-wrap gap-3` or a responsive grid that collapses to one column under the existing `max-[720px]` breakpoint). Each card is a `<button type="button">` (so it never submits the form) with `cursor-pointer`, styled with the existing token system to match the cards (e.g. `border border-border bg-surface rounded-[var(--radius-sm)] ...` with an accent hover: `hover:border-accent hover:bg-accent-weak`). Each card shows the preset NAME (semibold) + its one-line `description` (text-text-3, smaller); optionally append a small hint like `{preset.boxTypes.length} box types`. On click → `onPick(buildPresetConfig(preset))`. Key the list on `preset.name`. Give the section an accessible label (e.g. wrap in a `<section aria-label="Demo presets">`).

Edit `src/features/config/ConfigForm.tsx`:
- Import `DemoPresets` from `@/features/config/DemoPresets`. Keep the code-split note in the header accurate (DemoPresets is a `@/features/config/*` sibling — still three-free, no change to the allowed-imports list needed).
- Render `<DemoPresets onPick={(cfg) => form.reset(cfg)} />` at the TOP of the `<main>` content — ABOVE the `<div className="mb-12">` page-head block (this is "the top of the Packing task page, before pallet config"). Add bottom spacing so it sits cleanly above the heading (e.g. wrap so it reads as the first child of `<main>`).
- `form.reset(cfg)` is the correct re-seed: it updates ALL registered fields including the `useFieldArray` catalog in BoxCatalogCard (RHF reset replaces field-array contents) and the PalletCard inputs, and the existing `useLocalStorageAutosave(form)` subscription captures the change (debounced) — no extra persistence wiring needed. Do NOT manually setValue field-by-field; one `reset(cfg)` does it. Do not change the resolver/mode/persistence wiring.

Extend `src/features/config/ConfigForm.test.tsx` (keep every existing test untouched and green). Add a new describe block, e.g. "ConfigForm — demo presets". Reuse the existing `renderForm()` / `inputByName()` helpers. Test: render the form, `await user.click(screen.getByRole('button', { name: /Distribution-centre mix/ }))`, then assert (a) the pallet fields hold the fixed envelope — `inputByName('pallet.length').value === '1200'`, width `'800'`, height `'1800'`; and (b) the catalog now shows the preset's labels — query the box-name inputs (each BoxRow name input has `aria-label="Box type name"`; use `screen.getAllByLabelText('Box type name')` and assert their values include `'Master carton (tall)'`, `'Case box'`, `'Square tote'`, and that there are exactly 3). RHF reset is async to the inputs — wrap assertions in `await waitFor(...)` or use `findBy*`/`findAllBy*` so the re-render settles.
  </action>
  <verify>
    <automated>npm test -- src/features/config/ConfigForm.test.tsx</automated>
  </verify>
  <done>ConfigForm renders the 4-preset picker above the H1; clicking a preset re-seeds the form (pallet 1200/800/1800 + that preset's box labels, replacing the prior catalog). New test passes and all pre-existing ConfigForm tests stay green. DemoPresets.tsx imports no three/r3f/drei.</done>
</task>

</tasks>

<verification>
Run the full gate from the repo root (main working tree — node_modules present; dev server on :5173 must stay up, do NOT kill it):

1. `npm test` — full vitest run green (new demo-presets + ConfigForm tests + all prior tests).
2. `npm run typecheck` — `tsc -b --noEmit` clean.
3. `npm run lint` — eslint clean.
4. `npm run build` — `tsc -b && vite build` succeeds.
5. `node scripts/check-code-split.mjs` — PASSES (three stays out of the entry/`index-*` chunk; demo-presets.ts + DemoPresets.tsx are three-free and ride the eager `/` chunk).
6. `npm run test:e2e` — Playwright Configure→Run→Result flow stays green.

Do not regress prior fixes. Do not touch pre-existing untracked artifacts (`.planning/phases/**/*-PATTERNS.md`, `*-RESEARCH.md`, `design/`) or ROADMAP.md.
</verification>

<success_criteria>
- 4 demo presets pickable at the TOP of the Configure page (above pallet config), each showing name + one-line description.
- Clicking a preset pre-fills the WHOLE form via `form.reset(buildPresetConfig(preset))`: pallet always `1200×800×1800 / 1000 kg / overhang 0 (disabled)`, boxTypes = the preset's rows with fresh unique letter-prefixed ids, rotation `uprightOnly`.
- Every preset builds a PackConfig that parses against `packConfigSubmitSchema`.
- demo-presets.ts and DemoPresets.tsx import only nanoid/types/React + config siblings — no three/r3f/drei; code-split gate stays green.
- Full gate green: test, typecheck, lint, build, code-split, test:e2e.
</success_criteria>

<output>
Create `.planning/quick/260617-w8a-add-demo-box-catalog-presets-pickable-at/260617-w8a-SUMMARY.md` when done.
</output>
