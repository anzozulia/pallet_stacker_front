---
phase: quick-260607-1fa
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [QUICK-260607-1fa]
files_modified:
  - src/features/config/FooterBar.tsx
  - src/features/config/FooterBar.test.tsx
  - src/features/config/ConfigForm.tsx
  - src/features/config/PalletCard.tsx
  - src/features/config/PalletCard.test.tsx
  - src/features/config/schema.ts
  - src/features/config/schema.test.ts
  - src/features/config/defaults.ts
  - src/types/config.ts
  - src/lib/request-builder.ts
  - src/lib/request-builder.test.ts
  - src/components/result/SummaryBlock.tsx
  - src/components/result/SummaryBlock.test.tsx
  - src/components/result/PlacementList.tsx
  - src/components/result/PlacementList.test.tsx
  - src/routes/LoadingPage.tsx
  - src/routes/LoadingPage.test.tsx
  - src/routes/LoadingPage.strictmode.test.tsx
  - src/routes/ResultPage.tsx
  - src/components/viewer/ViewerOverlay.tsx
  - src/components/viewer/Pallet.tsx
  - e2e/config-persist.spec.ts
  - e2e/api-poll.spec.ts

must_haves:
  truths:
    - "The Configure footer is a full-width sticky bottom bar that visually mirrors the full-width header (border, bg, backdrop, px-6)."
    - "There is no Save draft button anywhere on the Configure page; debounced autosave still persists the draft."
    - "Max overhang defaults to 0 and is gated behind an Allow overhang switch (default OFF); OFF zeroes + disables the overhang field, ON enables it."
    - "There is no Max pallets field on the Configure page; the solver is never artificially capped on pallet count."
    - "The Summary block no longer shows the Unpacked stat, but the Unpacked panel listing un-packable items + reasons still renders."
    - "Placement cards show the human box-TYPE LABEL (e.g. Box type 1), not the raw item_id or raw typeId slug."
    - "Placement cards no longer show the orientation/permutation badge."
    - "Placement cards no longer show the Position x,y,z field; the field grid is Size and Support."
    - "The positions-are-box-min-corner caption is gone."
    - "The ISO/TOP/FRONT camera preset buttons are non-transparent (solid surface inactive, solid accent active)."
    - "The pallet 3D model has wood structure in BOTH the length and width directions and a centered 3x3 support-block grid aligned to the deck footprint."
    - "The pallet deck top face stays at y=DECK_TOP_Y (100) so box placement (mapping.ts) is unchanged."
  artifacts:
    - path: "src/features/config/FooterBar.tsx"
      provides: "Full-width footer; no Save draft button/state/prop"
    - path: "src/features/config/PalletCard.tsx"
      provides: "Allow-overhang switch; no Max pallets field"
    - path: "src/types/config.ts"
      provides: "PalletConfig.allowOverhang added; PackConfig.maxPallets removed"
    - path: "src/lib/request-builder.ts"
      provides: "max_pallets = boxes.length (uncapped); typeToLabel map in BuildResult"
    - path: "src/components/result/SummaryBlock.tsx"
      provides: "3-stat grid (no Unpacked stat)"
    - path: "src/components/result/PlacementList.tsx"
      provides: "type-label identifier; no orientation badge, no position field, no caption"
    - path: "src/components/viewer/ViewerOverlay.tsx"
      provides: "opaque ISO/TOP/FRONT preset buttons"
    - path: "src/components/viewer/Pallet.tsx"
      provides: "two-direction deck structure + centered 3x3 support grid; deck top at y=100"
  key_links:
    - from: "src/lib/request-builder.ts"
      to: "src/routes/ResultPage.tsx (via ConfigForm -> LoadingPage nav state)"
      via: "typeToLabel Map threaded through nav state"
      pattern: "typeToLabel"
    - from: "src/components/result/PlacementList.tsx"
      to: "typeToLabel"
      via: "typeToLabel.get(item.typeId) fallback item.typeId"
      pattern: "typeToLabel"
    - from: "src/components/viewer/Pallet.tsx"
      to: "src/lib/mapping.ts DECK_TOP_Y"
      via: "top deck top face computed to land at y=100 (BLOCK_H + DECK_H)"
      pattern: "BLOCK_H + DECK_H"
---

<objective>
A batch of 11 UI/model fixes across the Configure page, the Result page, and the 3D viewer,
grouped into 3 atomic tasks. Honors one locked USER DECISION on the unpacked item: remove
the Unpacked STAT from the Summary only, KEEP the Unpacked panel.

Purpose: Declutter and correct the UI/model after Phase 6, per the user's verbatim issue list.
Output: Updated config form (footer, overhang switch, removed save-draft + max-pallets),
decluttered result rail (type labels, fewer fields), and a realistic, correctly-centered
3D pallet with opaque camera buttons. All gates green.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Pattern anchors the executor must mirror:
@src/features/config/BoxRow.tsx        # Fragile/maxLoad Switch pattern to mirror for Allow-overhang
@src/components/Switch.tsx             # the Switch primitive (size="sm")
@src/lib/mapping.ts                    # DECK_TOP_Y = 100 invariant for the pallet model
</context>

<constraints>
- Executor runs on the MAIN working tree (NOT a worktree); node_modules present. A dev server
  is running on :5173 — do NOT kill it.
- Full gate is the final acceptance bar and must stay green:
  npm test, npm run typecheck, npm run lint, npm run build,
  node scripts/check-code-split.mjs, npm run test:e2e.
- CODE-SPLIT GATE (CLAUDE.md C-05): the eager / entry chunk MUST stay three-free. Config/loading
  files (FooterBar, ConfigForm, PalletCard, schema, defaults, types/config, request-builder,
  LoadingPage) MUST NOT import three/r3f/drei. Only Pallet.tsx and the viewer subtree may use
  three/drei JSX (lazy /result chunk). Do NOT introduce a three import into any config/loading file.
- Do NOT regress prior fixes: StrictMode loading-post re-fire, the #5/#8 tally fix, camera
  preservation on pallet switch, the code-split discipline.
- Every behavior change MUST update its co-located test(s) in the same task.
</constraints>

<tasks>

<task type="auto">
  <name>Task A: Configure page — full-width footer, remove Save draft, Allow-overhang switch (default 0), remove Max pallets (#1-#4)</name>
  <files>
    src/features/config/FooterBar.tsx, src/features/config/FooterBar.test.tsx,
    src/features/config/ConfigForm.tsx,
    src/features/config/PalletCard.tsx, src/features/config/PalletCard.test.tsx,
    src/features/config/schema.ts, src/features/config/schema.test.ts,
    src/features/config/defaults.ts, src/types/config.ts,
    src/lib/request-builder.ts, src/lib/request-builder.test.ts,
    e2e/config-persist.spec.ts
  </files>
  <action>
    Issue #1 FULL-WIDTH FOOTER. In ConfigForm.tsx FooterBar currently renders INSIDE the
    main element (mx-auto w-full max-w-[960px]), constraining it to the 960px column. Move
    FooterBar OUT of main so it is a sibling of header and main at the FormProvider page-shell
    level. In FooterBar.tsx make the outer bar a full-width sticky bottom bar that mirrors the
    header (the ConfigForm header is sticky top-0 z-20 ... border-b border-border
    bg-[rgba(255,255,255,0.82)] px-6 backdrop-blur): change FooterBar root from the current
    sticky bottom-0 mt-8 mb-2 ... rounded-[var(--radius)] border-t border-border bg-surface px-6
    py-4 pb-5 to a full-bleed sticky bottom-0 z-20 flex items-center gap-4 border-t border-border
    bg-[rgba(255,255,255,0.82)] px-6 py-4 backdrop-blur (drop the rounded card chrome + horizontal
    margins so it spans the viewport like the header). Keep the tally on the left and the Run CTA
    on the right. Optionally wrap the inner content in a mx-auto w-full max-w-[960px] div so its
    content aligns with the form column — but the BAR itself (border-t/bg/backdrop) must be full-width.

    Issue #2 REMOVE SAVE DRAFT. In FooterBar.tsx delete the Save draft button, the saved /
    savedTimer state, the handleSaveDraft fn, the SAVED_CONFIRM_MS const, the useRef / useState
    imports if now unused, and the onSaveDraft prop from FooterBarProps + the component signature.
    In ConfigForm.tsx remove the onSaveDraft prop wiring on FooterBar and the
    const onSaveDraft = () => flushSave(getValues()) line; if flushSave / getValues are then unused,
    drop them from the useLocalStorageAutosave(form) / form destructures. KEEP useLocalStorageAutosave
    (the debounced autosave stays — only the manual button goes).

    Issue #3 ALLOW-OVERHANG SWITCH (default 0, default OFF). Mirror the Fragile/maxLoad pattern in
    BoxRow.tsx (a Switch size="sm" next to the field; OFF zeroes + disables the field, ON
    restores/enables it). Changes:
      - src/types/config.ts: add allowOverhang: boolean to PalletConfig.
      - src/features/config/schema.ts: add allowOverhang: z.boolean() to the pallet object in
        packConfigSubmitSchema AND to the pallet object in packConfigShapeSchema (restore guard).
        VERIFY maxOverhang allows 0: mmInt is currently .pipe(z.number().int().positive()) which
        REJECTS 0. The overhang field must accept 0, so DO NOT validate maxOverhang with mmInt.
        Define a mmIntNonNeg (same as mmInt but .min(0) instead of .positive()) and use it for
        pallet.maxOverhang ONLY (keep mmInt for length/width/height/quantity). The restore guard
        uses looseNumber (already allows 0) — fine.
      - src/features/config/defaults.ts: in DEFAULT_CONFIG.pallet set maxOverhang: 0 and add
        allowOverhang: false.
      - src/features/config/PalletCard.tsx: convert the Max overhang NumberField to the Switch-gated
        pattern. Use useWatch for pallet.allowOverhang, getValues / setValue from useFormContext, and
        a per-session useRef stashing the prior overhang value (default fallback 40, mirroring
        BoxRow prevMaxLoad). Add an Allow overhang Switch size="sm" (label Allow overhang) in the
        field header. OFF -> setValue(pallet.maxOverhang, 0, {shouldValidate, shouldDirty}) and
        disable the input; ON -> restore the stashed value and enable. Bind via
        register(pallet.maxOverhang) with disabled={!allowOverhang}. Default OFF -> field shows 0,
        disabled.

    Issue #4 REMOVE MAX PALLETS.
      - src/types/config.ts: remove maxPallets: number from PackConfig (update the doc comment).
      - src/features/config/schema.ts: remove maxPallets: mmInt (submit) and maxPallets: looseNumber
        (shape).
      - src/features/config/defaults.ts: remove maxPallets: 2.
      - src/features/config/PalletCard.tsx: remove the Max pallets NumberField; change the Limits row
        grid-cols-3 to grid-cols-2 so the two remaining fields (Max weight, Max overhang) are not
        orphaned.
      - src/lib/request-builder.ts: line ~77 max_pallets: config.maxPallets ->
        max_pallets: boxes.length (you never need more pallets than boxes; this guarantees the solver
        is never artificially capped). Add a one-line comment explaining the rationale. boxes is
        already built above the options object, so it is in scope.

    TESTS for Task A:
      - FooterBar.test.tsx: remove the Save draft confirmation describe block and the onSaveDraft
        prop from the test Harness. Keep the tally + threshold + Run tests.
      - PalletCard.test.tsx: remove any Max pallets field assertion; add an Allow overhang switch
        test (default OFF disables the overhang input / shows 0; toggling ON enables it).
      - schema.test.ts: update any maxPallets cases; add allowOverhang to valid fixtures; add a case
        proving pallet.maxOverhang: 0 now PASSES the submit schema (previously failed under positive()).
      - request-builder.test.ts: remove/replace any maxPallets input; assert options.max_pallets
        equals request.boxes.length; add allowOverhang to input configs that spread a pallet.
      - e2e/config-persist.spec.ts: this spec FORCES an immediate save via the now-removed Save draft
        button (lines ~31-33) then asserts the Saved confirmation. REPLACE that flush trigger: drop
        the button click + the Saved assertion, and instead let the ~400ms debounce settle before
        reading localStorage — e.g. blur the input and waitForFunction polling
        localStorage.getItem(palletize:config:v1) until it includes 1234 and Reload Survivor
        (timeout 2000) before page.reload(). Keep the post-reload value assertions unchanged.

    Search the whole repo for any remaining maxPallets / Max pallets / Save draft references and
    update any other tests (useSubmitJob, LoadingPage, palette-integration, result-summary fixtures
    may seed a PackConfig with maxPallets) so typecheck + tests pass. NOTE: SummaryBlock keeps its
    optional maxPallets? PROP (an unrelated display denominator) — do NOT remove that prop; it is
    unaffected by removing PackConfig.maxPallets.
  </action>
  <verify>
    <automated>cd /Users/anzozulia/Desktop/pallet_stacker_front; npm test -- --run src/features/config/FooterBar.test.tsx src/features/config/PalletCard.test.tsx src/features/config/schema.test.ts src/lib/request-builder.test.ts; npm run typecheck</automated>
    <human-check>grep -rn "Save draft" src e2e returns nothing; the Configure footer spans the full viewport width like the header; the overhang field is disabled at 0 until the Allow overhang switch is ON.</human-check>
  </verify>
  <done>
    Footer is a full-width sticky bottom bar mirroring the header; no Save draft button/state/prop
    remains; debounced autosave still works (config-persist e2e green). maxOverhang defaults to 0
    behind an Allow overhang switch (default OFF, field disabled at 0; ON enables it) and the submit
    schema accepts overhang 0. No Max pallets field exists; options.max_pallets equals boxes.length.
    Listed unit tests + typecheck pass; no Save draft strings remain.
  </done>
</task>

<task type="auto">
  <name>Task B: Result-page declutter — remove Unpacked stat (keep panel), type labels in placement, drop orientation/position/caption (#5,#6,#7,#8,#11)</name>
  <files>
    src/components/result/SummaryBlock.tsx, src/components/result/SummaryBlock.test.tsx,
    src/components/result/PlacementList.tsx, src/components/result/PlacementList.test.tsx,
    src/lib/request-builder.ts, src/lib/request-builder.test.ts,
    src/features/config/ConfigForm.tsx,
    src/routes/LoadingPage.tsx, src/routes/LoadingPage.test.tsx, src/routes/LoadingPage.strictmode.test.tsx,
    src/routes/ResultPage.tsx,
    e2e/api-poll.spec.ts
  </files>
  <action>
    Issue #5 REMOVE SUMMARY UNPACKED STAT ONLY (locked USER DECISION). In SummaryBlock.tsx remove
    the Stat label="Unpacked" (line ~77). The grid drops from 4 to 3 stats (Pallets used,
    Utilisation, Total weight). Make 3 stats look intentional: change the wrapper from grid-cols-2 to
    grid-cols-3 gap-x-4 (a clean 3-up row). The summarise() lib still computes unpacked/totalItems —
    leave the lib untouched (only the rendered stat goes). DO NOT touch UnpackedPanel.tsx or its
    UnpackedPanel wiring in ResultPage — the panel STAYS (user decision).

    Issue #6 BOX TYPE LABELS IN PLACEMENT (thread typeToLabel end-to-end). The human label lives in
    config (boxTypes[].label), NOT in the API response, so it must be threaded:
      - src/lib/request-builder.ts: build typeToLabel = new Map<string,string>() and
        typeToLabel.set(boxType.id, boxType.label) once per boxType (alongside the existing per-item
        idToType.set). Extend BuildResult with typeToLabel: Map<string,string> and return it.
      - src/features/config/ConfigForm.tsx: destructure typeToLabel from buildPackRequest(config) and
        include it in navigate(/loading, { state: { request, idToType, typeToLabel } }).
      - src/routes/LoadingPage.tsx: extend LoadingNavState with typeToLabel: Map<string,string>;
        extend isLoadingNavState to also require typeToLabel instanceof Map (mirror the existing
        idToType instanceof Map check); read const typeToLabel = valid ? navState.typeToLabel :
        undefined and forward it in the done navigation: navigate(/result, { replace: true,
        state: { jobId, idToType, typeToLabel } }) (add typeToLabel to that effect dep array).
      - src/routes/ResultPage.tsx: extend ResultNavState with typeToLabel?: Map<string,string>;
        extend isResultNavState to validate typeToLabel is a Map when present (mirror idToType); read
        const typeToLabel = valid ? navState.typeToLabel : undefined and pass typeToLabel to
        PlacementList.
      - src/components/result/PlacementList.tsx: add typeToLabel?: Map<string,string> to
        PlacementListProps. Replace the head item_id chip AND the typeId sub-line with a SINGLE
        identifier showing the human label: const typeLabel = typeToLabel?.get(item.typeId) ??
        item.typeId rendered as the card identifier chip (keep the swatch). One label per card is
        fine (repeated boxes of a type repeat the name). Remove the now-unused typeId sub-line div.

    Issue #7 REMOVE ORIENTATION BADGE. In PlacementList.tsx remove the span with item.orientation.name
    (lines ~108-110).

    Issue #8 REMOVE POSITION FIELD. In PlacementList.tsx remove the Field label="Position x,y,z"
    (line ~125) and the now-unused const { x, y, z } = item.position destructure (line ~79). The
    field grid goes from grid-cols-3 to grid-cols-2 (Size, Support).

    Issue #11 REMOVE CAPTION. In PlacementList.tsx remove the p element with the positions-are-box-
    min-corner caption (lines ~69-72). Update the file top doc-comment that mentions the min-corner
    note + item_id/orientation/typeId/position so it matches the new card (now: swatch, type label,
    weight; field grid Size, Support).

    TESTS for Task B:
      - SummaryBlock.test.tsx: remove the Unpacked stat assertion; assert the three stats (Pallets
        used, Utilisation, Total weight) render and the Unpacked label is ABSENT.
      - PlacementList.test.tsx: update props to pass a typeToLabel Map; assert the card shows the
        human label (e.g. Box type 1); assert the raw item_id chip / typeId sub-line / orientation
        badge / Position field / min-corner caption are all ABSENT; keep Size + Support assertions;
        keep the data-placement-card + hover-accent (border-accent) expectations (the api-poll e2e
        depends on both).
      - request-builder.test.ts: assert typeToLabel.get(boxType.id) equals boxType.label for each type.
      - LoadingPage.test.tsx + LoadingPage.strictmode.test.tsx: any nav-state fixture/mock that builds
        a LoadingNavState must now include typeToLabel: new Map(...) so isLoadingNavState passes;
        assert (where the test inspects the done navigation) that typeToLabel is forwarded.
      - e2e/api-poll.spec.ts: the legend assertions (D/F/T) are scoped to [data-viewer-legend] and are
        unchanged; the placement-hover spec uses [data-placement-card] + border-accent (both
        preserved) — no change needed UNLESS a spec asserts a raw item_id / orientation / position
        string in the rail; grep and update any such assertion to the new type-label text.
  </action>
  <verify>
    <automated>cd /Users/anzozulia/Desktop/pallet_stacker_front; npm test -- --run src/components/result/SummaryBlock.test.tsx src/components/result/PlacementList.test.tsx src/lib/request-builder.test.ts src/routes/LoadingPage.test.tsx src/routes/LoadingPage.strictmode.test.tsx src/routes/ResultPage.test.tsx; npm run typecheck; grep -q typeToLabel src/lib/request-builder.ts src/components/result/PlacementList.tsx</automated>
    <human-check>On /result a placement card shows a box-type name (e.g. Box type 1), no raw id, no orientation chip, no Position field, no min-corner caption; the Summary shows 3 stats with no Unpacked stat, while the Unpacked panel below still lists un-packable items.</human-check>
  </verify>
  <done>
    Summary shows 3 stats (Pallets used, Utilisation, Total weight) and NO Unpacked stat; the
    UnpackedPanel still renders. Placement cards show the human type LABEL (typeToLabel.get(typeId)
    fallback typeId), with no item_id chip, no typeId sub-line, no orientation badge, no Position
    field, and no min-corner caption; the field grid is Size, Support. typeToLabel is threaded
    request-builder -> ConfigForm -> LoadingPage -> ResultPage -> PlacementList. Listed unit tests +
    typecheck pass.
  </done>
</task>

<task type="auto">
  <name>Task C: 3D viewer — opaque ISO/TOP/FRONT buttons, realistic two-direction pallet with centered 3x3 support grid (deck top stays at y=100) (#9,#10)</name>
  <files>
    src/components/viewer/ViewerOverlay.tsx,
    src/components/viewer/Pallet.tsx,
    e2e/api-poll.spec.ts
  </files>
  <action>
    Issue #9 OPAQUE PRESET BUTTONS. In ViewerOverlay.tsx the ISO/TOP/FRONT preset buttons (rendered
    ~lines 140-157) use translucent backgrounds — inactive bg-white/5 (hover bg-white/10), active
    bg-[rgba(99,90,245,0.32)]. Make them NON-transparent: inactive -> a SOLID dark surface (use a
    solid token like bg-[var(--color-d-bg-2)] or bg-[var(--color-d-surface)] if defined, else a
    solid hex matching the overlay panel bg such as bg-[#1a2030]), with a slightly lighter solid
    hover (e.g. hover:bg-[#222a3d]); active -> SOLID accent bg-accent (or solid bg-[#4f46e5]) with
    white text. Keep them legible on the dark viewer and keep the existing border / active-border
    treatment. The diagnostics TOGGLE buttons (~lines 119-135) share this exact style — apply the
    SAME opaque treatment to them for consistency (the explicit ask is the presets, but matching the
    toggles avoids a visual mismatch). Verify the chosen --color-d-* token exists in the @theme block
    (grep --color-d- in the CSS theme); if a chosen token is absent, fall back to a solid hex.

    Issue #10 REALISTIC TWO-DIRECTION PALLET + CENTERED 3x3 SUPPORT GRID. Rework Pallet.tsx
    (function Pallet({ length: L, width: W })). CURRENT problem: all 6 deck slats run along x (full
    length L, spaced across W) AND the 3 support blocks are actually full-length beams along x
    (boxGeometry args=[L, BLOCK_H, 90] at z = [-W/2+90, 0, W/2-90]) — so ALL structure runs
    lengthwise (no width-direction members) and the 3 beams look miscentered vs the deck (the
    screenshot). REWORK to a block-pallet structure with wood in BOTH directions:
      * TOP DECK BOARDS: boards running along ONE axis (e.g. along the width/z: boxGeometry
        args=[boardW, DECK_H, W*0.92] — thin in x, long in z), spaced along the length/x. This is the
        visible top surface. Keep the existing per-face material order so the TOP face (material-2)
        uses WOOD_TOP and the sides use WOOD_SIDE.
      * PERPENDICULAR LAYER: a layer of boards running along the OTHER axis (along the length/x: long
        in x, thin in z), positioned BELOW the top deck and spaced across the width/z — so there is
        visible wood structure in both directions (top deck boards + bottom/stringer boards crossing
        them). This perpendicular layer sits between the support blocks and the top deck.
      * SUPPORT BLOCKS: replace the 3 full-length beams with NINE short blocks in a centered 3x3 grid
        (block-pallet style). Place them at x in {-L/2 + inset, 0, L/2 - inset} and z in
        {-W/2 + inset, 0, W/2 - inset} (a small inset, e.g. ~90-100mm, so corner blocks sit at the
        deck corners not past the edge). Each block boxGeometry args=[blockL, BLOCK_H, blockW] (small
        footprint, e.g. ~120x120mm), centered at y = BLOCK_H/2. This centers the structure in the
        L x W footprint and aligns blocks under the deck-board intersections (fixes the miscentering).
      * CRITICAL INVARIANT (DO NOT BREAK): the TOP DECK top face MUST remain at y = DECK_TOP_Y = 100.
        Currently BLOCK_H (78) + DECK_H (22) = 100. Keep that arithmetic: the top deck boards centre y
        must be BLOCK_H + DECK_H/2 so their top face lands at exactly BLOCK_H + DECK_H = 100. The
        perpendicular layer sits BELOW that (e.g. its top at BLOCK_H, so its centre is below the top
        deck). Boxes are placed by mapping.ts assuming deck top = 100, so do NOT change
        BLOCK_H + DECK_H = 100. Add an inline comment restating this invariant.
      * KEEP the ground plane + Grid exactly as-is. Materials stay module constants (WOOD_TOP /
        WOOD_SIDE / GROUND_COLOR) — NOT @theme tokens. This file lives in the lazy /result subtree so
        three/drei JSX is allowed here (do NOT import three into any eager-chunk file).

    No new unit test (the WebGL scene is not unit-testable in jsdom per CLAUDE.md — jsdom has no
    WebGL). The e2e (api-poll result-viewer / ISO-TOP-FRONT reframe specs) already render and diff the
    canvas; ensure those still pass. The specs select preset buttons by role+name
    (getByRole(button, { name: ISO|TOP|FRONT })) which is class-agnostic — no change expected.
  </action>
  <verify>
    <automated>cd /Users/anzozulia/Desktop/pallet_stacker_front; npm run typecheck; npm run lint; grep -q "BLOCK_H + DECK_H" src/components/viewer/Pallet.tsx; node scripts/check-code-split.mjs</automated>
    <human-check>The ISO/TOP/FRONT buttons are fully opaque (solid dark inactive, solid accent active). The pallet shows wood boards in both directions with a centered 3x3 block grid, the structure is centered under the deck, and boxes still rest exactly on the deck surface (no float/sink).</human-check>
  </verify>
  <done>
    ISO/TOP/FRONT (and the diagnostic toggle) buttons are non-transparent — solid dark surface
    inactive, solid accent active. The pallet has top deck boards in one direction, a perpendicular
    board layer in the other, and a centered 3x3 grid of support blocks aligned to the L x W
    footprint. The top deck top face is at y = BLOCK_H + DECK_H = DECK_TOP_Y = 100 (box placement
    unchanged). typecheck + lint pass; code-split gate stays green (no three import leaked into the
    eager chunk).
  </done>
</task>

</tasks>

<verification>
After all three tasks, run the FULL gate (final acceptance bar) from the repo root and confirm each
is green:

  npm test
  npm run typecheck
  npm run lint
  npm run build
  node scripts/check-code-split.mjs
  npm run test:e2e

Regression watch (must NOT break): the StrictMode loading-post re-fire, the #5/#8 tally fix, camera
preservation on pallet switch, and the code-split discipline (entry chunk three-free).
</verification>

<success_criteria>
All 11 user issues resolved and the full gate green:
- #1 Footer full-width, header-like.  - #2 No Save draft (autosave kept).
- #3 Overhang defaults 0 behind an Allow-overhang switch (schema accepts 0).
- #4 No Max pallets; max_pallets = boxes.length.  - #5 No Summary Unpacked stat; Unpacked panel kept.
- #6 Placement shows box-type labels.  - #7 No orientation badge.  - #8 No position field.
- #9 Opaque ISO/TOP/FRONT buttons.  - #10 Two-direction pallet + centered 3x3 supports, deck top y=100.
- #11 No min-corner caption.
</success_criteria>

<output>
Create .planning/quick/260607-1fa-configure-footer-overhang-max-pallets-re/260607-1fa-SUMMARY.md when done.
</output>
