---
phase: quick-260618-eg4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .env.example
  - CONTRIBUTING.md
  - docker-compose.yml
  - public/favicon.svg
  - index.html
  - LICENSE
  - package.json
  - .dockerignore
  - .github/workflows/ci.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - "A new visitor can read README.md and self-host the app via Docker with the exact build-arg/run commands provided"
    - "A contributor can read CONTRIBUTING.md and run the full local gate before opening a PR"
    - "docker compose up --build produces the same nginx-unprivileged:8080 static image as the documented docker build, with VITE_API_URL baked at build time"
    - "The browser tab shows the Pallet Packer indigo brand favicon (not the default Vite icon)"
    - "No brand-facing file still says 'Palletize' — only the localStorage STORAGE_KEY (and its test) and the frozen design/*.html mockups retain the legacy string"
    - "CI runs the code-split gate after build so a three-in-entry-chunk regression fails the pipeline"
  artifacts:
    - path: "README.md"
      provides: "Open-source project front door: what-it-is, demo, Docker + compose quick start, configuration, local dev, scripts, testing, tech stack, structure, deployment, contributing, license"
      min_lines: 80
    - path: ".env.example"
      provides: "Documented VITE_API_URL build-time env var with example value"
      contains: "VITE_API_URL"
    - path: "CONTRIBUTING.md"
      provides: "Prereqs, dev loop, full pre-PR gate command, code-split rule, commit/husky conventions"
      contains: "check-code-split"
    - path: "docker-compose.yml"
      provides: "Single build-from-source service mapping 8080:8080 with VITE_API_URL build-arg"
      contains: "VITE_API_URL"
    - path: "public/favicon.svg"
      provides: "Brand-glyph SVG favicon (indigo #6d63f5→#4f46e5 rounded square + white inset)"
      contains: "svg"
    - path: "index.html"
      provides: "Head with favicon link, description meta, theme-color meta"
      contains: "favicon.svg"
    - path: "LICENSE"
      provides: "MIT license attributed to Pallet Packer contributors"
      contains: "Pallet Packer contributors"
    - path: "package.json"
      provides: "Package renamed to pallet-packer"
      contains: "pallet-packer"
    - path: ".dockerignore"
      provides: "Build-context exclusions for planning/tooling/design dirs"
      contains: ".planning"
    - path: ".github/workflows/ci.yml"
      provides: "CI build-and-test job with code-split gate step after build"
      contains: "check-code-split"
  key_links:
    - from: "README.md"
      to: "docker-compose.yml"
      via: "Quick start (docker compose) section references the compose file"
      pattern: "docker compose up --build"
    - from: "README.md"
      to: ".env.example"
      via: "Configuration section points to .env.example"
      pattern: "\\.env\\.example"
    - from: "index.html"
      to: "public/favicon.svg"
      via: "link rel=icon href=/favicon.svg"
      pattern: "favicon\\.svg"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-code-split.mjs"
      via: "CI step runs the gate after build"
      pattern: "check-code-split\\.mjs"
---

<objective>
Make Pallet Packer demo-deploy ready as an open-source, self-hostable project by adding the missing front-door documentation and fixing branding/build-context/CI consistency gaps. The app code, Dockerfile, nginx.conf, and dependency versions are already verified working and MUST NOT change — this plan is DOCS + CONFIG CONSISTENCY only.

Purpose: A first-time visitor can understand, demo, and self-host the tool from the README alone; a contributor can run the full gate before a PR; the brand is consistent everywhere except the load-bearing localStorage key; the build context and CI mirror the local gate.

Output: README.md, .env.example, CONTRIBUTING.md, docker-compose.yml, public/favicon.svg (all new); edits to index.html, LICENSE, package.json, .dockerignore, .github/workflows/ci.yml.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Existing files to reference / edit (read before editing):
@index.html
@LICENSE
@package.json
@.dockerignore
@.github/workflows/ci.yml
@scripts/check-code-split.mjs

# Brand glyph source (DO NOT edit — read only, to mirror in favicon.svg):
# src/features/config/ConfigForm.tsx line ~95:
#   rounded-[6px] bg-[linear-gradient(150deg,#6d63f5,#4f46e5)]
#   after:inset-[5px] after:rounded-[2px] after:border-[1.5px] after:border-white/90
</context>

<constraints>
- DO NOT change src/lib/config-persist.ts STORAGE_KEY ('palletize:config:v1') or its test, and DO NOT touch any e2e/*.spec.ts localStorage usage — renaming wipes user drafts. "palletize" stays ONLY in those + design/*.html.
- DO NOT modify design/*.html (frozen mockups) or any src/ runtime code.
- DO NOT change dependency versions, the Dockerfile, or nginx.conf.
- DO NOT run `npm install`/`npm ci` to rewrite package-lock.json. Edit package.json's top-level "name" only; leave package-lock.json as-is (its "name" fields are non-brand-facing build metadata and out of scope — the verify grep accounts for this).
- NO badges in README pointing to nonexistent services. A CI badge is permitted ONLY if the GitHub repo slug is actually known; otherwise omit badges entirely. (No git remote is configured per STATE.md Deferred Items — so omit badges.)
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Author the open-source docs (README, .env.example, CONTRIBUTING, docker-compose)</name>
  <files>README.md, .env.example, CONTRIBUTING.md, docker-compose.yml</files>
  <action>
Create README.md (root) as the centerpiece. Use accurate, skimmable GitHub-Flavored Markdown. Sections IN THIS ORDER:
1. Title `# Pallet Packer` + one-line tagline: a free, open-source, self-hostable web tool for pallet packing.
2. "What it is" — a user describes a pallet and a box catalog; the app submits the job to an EXISTING external packing API and returns an explorable 3D plan with fill rate, weight, centre-of-gravity, and stacking stability. No login, no accounts, no stored history. State clearly it is a FRONTEND that requires an external packing API to function.
3. "Try it / Demo" — the built-in one-click "Try a demo" presets on the Configure page give instant exploration.
4. "Features" — bullet list (dynamic box catalog, mm/kg units, async submit-then-poll, explorable 3D viewer with ISO/TOP/FRONT presets, CoG marker, support heatmap, localStorage draft persistence, single static Docker image).
5. "Quick start (Docker)" — EXACT fenced commands: `docker build --build-arg VITE_API_URL=https://packerapi.anzozulia.xyz -t pallet-packer .` then `docker run --rm -p 8080:8080 pallet-packer` then open http://localhost:8080. State VITE_API_URL is REQUIRED and baked at build time; the image is a static build served by nginx-unprivileged (non-root, port 8080).
6. "Quick start (docker compose)" — `docker compose up --build`; explain VITE_API_URL is set via shell env or the compose file's build args; reference the new docker-compose.yml.
7. "Configuration" — VITE_API_URL is build-time ONLY (rebuild to change the backend); a production build with NO VITE_API_URL throws at module load (fail-loud guard); the API must allow CORS for the serving origin; brief submit-then-poll contract (POST /api/v1/pack → 202 {job_id} → poll GET /api/v1/jobs/{job_id} until status done/failed/timeout; the client owns the /api/v1 prefix, so VITE_API_URL is the ORIGIN only); point to .env.example. Reference/public API: https://packerapi.anzozulia.xyz.
8. "Local development" — Node 22, `npm ci`, `npm run dev`, http://localhost:5173; the Vite dev proxy forwards /api to the public API so NO CORS and NO VITE_API_URL are needed locally.
9. "Scripts" — a table of npm scripts: dev, build (tsc -b && vite build), preview (port 4173), typecheck, lint, format, test (vitest run), test:e2e (playwright).
10. "Testing" — vitest unit/component in jsdom (the WebGL canvas is NOT unit-tested; scene logic is tested as pure functions); Playwright e2e drives Configure→Run→Result against the built dist via vite preview; the code-split gate (`node scripts/check-code-split.mjs`) keeps three out of the entry chunk.
11. "Tech stack" — concise list/table: React 19.2.7, Vite 8, TypeScript 6, three 0.184 / @react-three/fiber 9 / @react-three/drei 10, @tanstack/react-query, react-hook-form + zod + @hookform/resolvers, Tailwind v4, react-router 7 (SPA mode), nanoid.
12. "Project structure" — a brief high-level tree of src/: api, components (viewer, result), features/config, features/loading, hooks, lib, routes, types.
13. "Deployment" — single static Docker image; nginx SPA fallback (try_files → /index.html) is MANDATORY for deep links like /result; non-root on 8080; long-cached hashed assets (max-age=31536000, immutable); runtime API-URL override is out of scope (rebuild to reconfigure).
14. "Contributing" — link to CONTRIBUTING.md.
15. "License" — MIT, link to LICENSE.
Do NOT add badges (no git remote configured — see STATE.md Deferred Items).

Create .env.example (root) with a comment block: VITE_API_URL is build-time only, baked into the static bundle, REQUIRED for production builds, NOT needed for `npm run dev` (the Vite proxy handles it). Include the example line `VITE_API_URL=https://packerapi.anzozulia.xyz`. (.gitignore ignores `*.local`, NOT `.env.example`, so this file is committable — confirmed.)

Create CONTRIBUTING.md (root), concise and practical: Prereqs (Node 22, `npm ci`); dev loop (`npm run dev`); the FULL local gate before a PR as a single fenced command: `npm run typecheck && npm run lint && npm run test && npm run build && node scripts/check-code-split.mjs && npm run test:e2e`; the code-split rule (config/non-viewer files must NEVER import three/r3f/drei or any viewer module — those live only in the lazy /result chunk; scripts/check-code-split.mjs enforces it); conventional-commit-style messages; husky + lint-staged auto-run `eslint --fix` + prettier on staged files (per package.json lint-staged config).

Create docker-compose.yml (root): a single service that BUILDS the image with the VITE_API_URL build-arg defaulting to https://packerapi.anzozulia.xyz and overridable via shell env, i.e. `build.args.VITE_API_URL: ${VITE_API_URL:-https://packerapi.anzozulia.xyz}`; map `8080:8080`; `restart: unless-stopped`; add a comment noting VITE_API_URL is build-time. OMIT the obsolete top-level `version:` key.
  </action>
  <verify>
    <automated>test -f README.md && test -f .env.example && test -f CONTRIBUTING.md && test -f docker-compose.yml && grep -q "docker build --build-arg VITE_API_URL=https://packerapi.anzozulia.xyz -t pallet-packer ." README.md && grep -q "docker compose up --build" README.md && grep -q "check-code-split" CONTRIBUTING.md && grep -q "VITE_API_URL=https://packerapi.anzozulia.xyz" .env.example && grep -q "8080:8080" docker-compose.yml && ! grep -q "^version:" docker-compose.yml</automated>
  </verify>
  <done>All four doc files exist. README contains the exact Docker build/run commands and the compose command. CONTRIBUTING references the code-split gate. .env.example has the example VITE_API_URL line. docker-compose.yml maps 8080:8080, uses the VITE_API_URL build-arg with a default, and has NO `version:` key. No badges added.</done>
</task>

<task type="auto">
  <name>Task 2: Brand the favicon + head meta, rebrand LICENSE and package.json</name>
  <files>public/favicon.svg, index.html, LICENSE, package.json</files>
  <action>
Create public/favicon.svg: a minimal SVG favicon mirroring the header logo glyph in src/features/config/ConfigForm.tsx. Render a rounded square (viewBox roughly 0 0 32 32, rounded corners) filled with the brand gradient from #6d63f5 to #4f46e5 (a linearGradient at ~150deg orientation, e.g. x1/y1 top-left to x2/y2 bottom-right), and a smaller inset white square/border (white, ~90% opacity, inset from the edges with a small corner radius) echoing the `after:inset-[5px] after:border-[1.5px] after:border-white/90` inner border. Keep it tiny and dependency-free (hand-authored SVG, no external refs).

Edit index.html: keep the existing `<meta charset>`, `<meta name="viewport">`, and `<title>Pallet Packer</title>`. Add inside `<head>`: `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />`, a `<meta name="description" content="...">` with a one-line tool description (free, open-source, self-hostable 3D pallet packing tool), and `<meta name="theme-color" content="#4f46e5" />`.

Edit LICENSE: change the single line `Copyright (c) 2026 Palletize contributors` to `Copyright (c) 2026 Pallet Packer contributors`. Keep the rest of the MIT text byte-for-byte.

Edit package.json: change top-level `"name": "palletize"` to `"name": "pallet-packer"`. Optionally add `"description"` (one-line tool description) and `"license": "MIT"`. DO NOT touch dependencies, devDependencies, scripts, version, or lint-staged. DO NOT run npm install (leave package-lock.json untouched).
  </action>
  <verify>
    <automated>test -f public/favicon.svg && grep -qi "<svg" public/favicon.svg && grep -qi "6d63f5" public/favicon.svg && grep -qi "4f46e5" public/favicon.svg && grep -q 'href="/favicon.svg"' index.html && grep -q 'name="theme-color"' index.html && grep -q 'name="description"' index.html && grep -q "Pallet Packer contributors" LICENSE && ! grep -q "Palletize contributors" LICENSE && grep -q '"name": "pallet-packer"' package.json && grep -q "MIT License" LICENSE</automated>
  </verify>
  <done>public/favicon.svg exists with the brand gradient (#6d63f5/#4f46e5) and white inset. index.html links the favicon and has description + theme-color meta while keeping charset/viewport/title. LICENSE attributes "Pallet Packer contributors" and retains the MIT body. package.json name is "pallet-packer"; dependencies/scripts/versions unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Harden build context (.dockerignore) and CI (code-split gate)</name>
  <files>.dockerignore, .github/workflows/ci.yml</files>
  <action>
Edit .dockerignore: KEEP the existing entries (node_modules, dist, .git, playwright-report, coverage, e2e, *.md) and ADD: .planning, .claude, design, test-results, .husky, .github, *.tsbuildinfo, .DS_Store. (Note: *.md is already ignored, which keeps README/CONTRIBUTING out of the build context — correct, the image is a static build that does not need docs.)

Edit .github/workflows/ci.yml: in the `build-and-test` job, add a new step IMMEDIATELY AFTER the existing `- run: npm run build` step: `- run: node scripts/check-code-split.mjs`. Do not change any other step, the `e2e` job, triggers, or the node version.
  </action>
  <verify>
    <automated>grep -q "^.planning$" .dockerignore && grep -q "^.claude$" .dockerignore && grep -q "^design$" .dockerignore && grep -q "^test-results$" .dockerignore && grep -q "^.husky$" .dockerignore && grep -q "^.github$" .dockerignore && grep -q "tsbuildinfo" .dockerignore && grep -q ".DS_Store" .dockerignore && grep -q "node_modules" .dockerignore && grep -q "check-code-split.mjs" .github/workflows/ci.yml && grep -A1 "npm run build" .github/workflows/ci.yml | grep -q "check-code-split"</automated>
  </verify>
  <done>.dockerignore retains all original entries and adds .planning, .claude, design, test-results, .husky, .github, *.tsbuildinfo, .DS_Store. ci.yml's build-and-test job runs `node scripts/check-code-split.mjs` directly after `npm run build`; the e2e job and all other config are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → self-hoster | README/compose commands are copy-pasted and run by operators; wrong commands cause failed/insecure deploys |
| build context → Docker image | .dockerignore decides what leaves the host into the build image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-eg4-01 | Information Disclosure | .dockerignore / build context | mitigate | Add .planning, .claude, .github, .husky, design, test-results, *.tsbuildinfo to .dockerignore so planning notes, CI secrets context, and tooling never enter the build context or shipped image. |
| T-eg4-02 | Tampering | README Docker commands | mitigate | Document the REQUIRED build-time VITE_API_URL and the fail-loud guard so operators cannot ship a misconfigured (URL-less) production image silently; state the CORS requirement so the deploy is reachable, not a broken/open fallback. |
| T-eg4-03 | Repudiation | CI pipeline | mitigate | Add the code-split gate to CI build-and-test so a three-in-entry-chunk regression cannot merge unnoticed; the gate's exit-non-zero fails the run. |
| T-eg4-SC | Tampering | npm/pip/cargo installs | accept | No package installs, no dependency changes in this plan (DOCS + CONFIG only). package-lock.json is intentionally left untouched. No supply-chain surface introduced. |
</threat_model>

<verification>
Run the FULL local gate (must all pass, no app-logic changes expected to break anything):

```
npm run typecheck
npm run lint
npm run test          # 224 tests pass
npm run build
node scripts/check-code-split.mjs    # ✓ code-split check PASSED
npm run test:e2e
```

Docker smoke build (proves the documented command works; remove the image after):

```
docker build --build-arg VITE_API_URL=https://packerapi.anzozulia.xyz -t pallet-packer:ci-check .
docker image rm pallet-packer:ci-check
```

Brand consistency — `palletize` must appear ONLY in the STORAGE_KEY, its test, e2e localStorage usage, and frozen design/*.html mockups (case-insensitive, brand-facing scan):

```
grep -rin "palletize" \
  --include="*.ts" --include="*.tsx" --include="*.html" --include="*.json" \
  --include="*.md" --include="*.yml" --include="*.yaml" \
  . --exclude-dir=node_modules --exclude-dir=.planning --exclude-dir=.git
```

Expected hits ONLY (no brand-facing hits remain):
- src/lib/config-persist.ts (STORAGE_KEY 'palletize:config:v1')
- src/lib/config-persist.test.ts (asserts the key)
- e2e/*.spec.ts (localStorage key usage)
- design/*.html (frozen mockups — title + .brand wordmark)
- package-lock.json (build metadata "name" — out of scope, not brand-facing)
- CLAUDE.md (project-instruction config, not a brand-facing deliverable — leave as-is)

There must be NO new brand-facing "Palletize" in README.md, CONTRIBUTING.md, .env.example, docker-compose.yml, index.html, LICENSE, or package.json (all must say "Pallet Packer" / "pallet-packer").
</verification>

<success_criteria>
- README.md, .env.example, CONTRIBUTING.md, docker-compose.yml, public/favicon.svg created; index.html, LICENSE, package.json, .dockerignore, .github/workflows/ci.yml edited.
- README has all 15 sections in order with the exact Docker build/run + compose commands; no badges.
- docker-compose.yml builds with the VITE_API_URL build-arg (default + override), maps 8080:8080, restart unless-stopped, no `version:` key.
- Favicon renders the indigo brand glyph; index.html links it and has description + theme-color meta; title stays "Pallet Packer".
- LICENSE = "Pallet Packer contributors" (MIT body intact); package.json name = "pallet-packer".
- .dockerignore adds the 8 new entries and keeps the originals; ci.yml runs the code-split gate after build.
- Full local gate green (typecheck, lint, 224 tests, build, code-split PASS, e2e) and the Docker smoke build succeeds.
- `grep -rin palletize` shows NO brand-facing hits outside STORAGE_KEY + its test + e2e localStorage usage + design/*.html (and non-brand package-lock.json/CLAUDE.md metadata).
- src/ runtime code, Dockerfile, nginx.conf, STORAGE_KEY, and dependency versions are all UNCHANGED.
</success_criteria>

<output>
Create `.planning/quick/260618-eg4-demo-deploy-readiness-open-source-self-h/260618-eg4-SUMMARY.md` when done.
</output>
