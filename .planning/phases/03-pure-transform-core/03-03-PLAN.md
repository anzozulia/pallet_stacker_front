---
phase: 03-pure-transform-core
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/result-mapper.ts
  - src/lib/result-mapper.test.ts
autonomous: true
requirements: [PACK-02, BOX-04]
must_haves:
  truths:
    - "Given the captured done response, the mapper regroups packed boxes by type AND by pallet in a single pass (SC-3)"
    - "Every packed item's type is recovered O(1) via an optional idToType map, falling back to typeKeyOf for the captured fixture (SC-1 round-trip)"
    - "The mapper surfaces per-pallet cog and per-box support_ratio RAW (no Three.js mapping, no tint bucketing) (D-08)"
    - "byType keys equal what colorForType consumes, keeping the Phase 6 legend stable"
    - "Multi-pallet (2) and the 7 unpacked items are surfaced correctly without mutating the input"
  artifacts:
    - path: "src/lib/result-mapper.ts"
      provides: "mapDoneResponse(done, idToType?) -> {summary, pallets, byType, unpacked}; ResultView/MappedPallet/TypeAggregate types"
      exports: ["mapDoneResponse"]
      contains: "export function mapDoneResponse"
      min_lines: 30
    - path: "src/lib/result-mapper.test.ts"
      provides: "SC-1 round-trip, SC-3 dual grouping + raw diagnostics, multi-pallet + unpacked tests"
      contains: "mapDoneResponse"
      min_lines: 35
  key_links:
    - from: "src/lib/result-mapper.ts"
      to: "src/lib/mapping.ts"
      via: "imports typeKeyOf as the parse-fallback recovery channel"
      pattern: "typeKeyOf"
    - from: "src/lib/result-mapper.ts"
      to: "src/types/pack-contract.ts"
      via: "type-only import of DoneResponse, PlacementOut, PalletResult, UnpackedItem, InputSummary, Cog"
      pattern: "from '@/types/pack-contract'"
    - from: "src/lib/result-mapper.test.ts"
      to: "src/lib/__fixtures__/pack-done-response.json"
      via: "imports the captured done response as the round-trip target"
      pattern: "pack-done-response.json"
---

<objective>
Slice B of the pure transform core: the result-mapper vertical. A complete, independently-verifiable transform path — response types (from Plan 01) → `mapDoneResponse` logic → co-located tests consuming the captured `pack-done-response.json`. Delivers SC-3 (regroup by type + by pallet, surface raw cog + support_ratio) and the consuming end of the SC-1 round-trip (O(1) id→type recovery, map-primary / `typeKeyOf`-fallback).

This is a vertical MVP slice independent of Plan 02: after this plan, Phase 6's result page could feed a real `done` response through `mapDoneResponse` and get its grouped view model, even if the request-builder were absent. Test-first — the round-trip contract lands as a failing test before the implementation.

Purpose: The mapper is the data-shaping boundary; Phase 6 owns coordinate/visual derivation. Keeping `cog`/`support_ratio` raw and keeping `byType` keys compatible with `colorForType` is what lets the Phase 6 viewer + legend stay stable (D-08).
Output: `src/lib/result-mapper.ts` (`mapDoneResponse` + `ResultView`/`MappedPallet`/`TypeAggregate`) + `src/lib/result-mapper.test.ts`.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pure-transform-core/03-CONTEXT.md
@.planning/phases/03-pure-transform-core/03-RESEARCH.md
@.planning/phases/03-pure-transform-core/03-PATTERNS.md

@src/lib/mapping.ts
@src/lib/palette.ts
@src/lib/mapping.test.ts
@src/lib/palette.test.ts
@src/lib/__fixtures__/pack-done-response.json
</context>

<artifacts_this_phase_produces>
This plan (03-03) creates these NEW symbols and files (exclude from drift verification):

**New transform module — `src/lib/result-mapper.ts`:**
- `TypeAggregate` (interface: `{ typeId; count; totalWeight; itemIds }`)
- `MappedPallet` (interface: `{ palletId; utilisation; cog; totalWeight; items }`)
- `ResultView` (interface: `{ summary; pallets; byType; unpacked }`)
- `mapDoneResponse(done: DoneResponse, idToType?: Map<string, string>): ResultView` (function)

**New test file — `src/lib/result-mapper.test.ts`** (no exported symbols)

(Types `DoneResponse`/`PlacementOut`/`PalletResult`/`UnpackedItem`/`InputSummary`/`Cog` are created/consolidated in Plan 01; `typeKeyOf` is reused from Phase 2's `mapping.ts`.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing result-mapper test suite (RED) — fixture round-trip centerpiece</name>
  <files>src/lib/result-mapper.test.ts</files>
  <read_first>
    - src/lib/mapping.test.ts (test wiring to copy: `{ describe, expect, it } from 'vitest'`, `@/` alias, fixture JSON import + `as DoneResponse` cast, jsdom-WebGL-free; the AABB-over-fixture iteration at lines 49-72 shows reading pallets[].items[])
    - src/lib/palette.test.ts (colorForType key-compatibility: it dedupes+sorts its input — the mapper's byType keys must feed it cleanly)
    - src/lib/__fixtures__/pack-done-response.json (the INPUT + round-trip target — VERIFIED counts: input_summary.items_packed=31, items_unpacked=7, pallets_used=2; pallets P001/P002; per-type packed counts D=11, T=12, F=8; per-pallet cog present; per-item support_ratio/supported_by/supports present)
    - src/types/pack-contract.ts (from Plan 01 — DoneResponse and the response interfaces to type the cast)
    - .planning/phases/03-pure-transform-core/03-RESEARCH.md § Pattern 3 + § Code Examples (round-trip test skeleton) + § Validation Architecture (SC-1 / SC-3 test rows)
  </read_first>
  <behavior>
    - Test "round-trip recovers every type (SC-1)": mapDoneResponse(done) with NO idToType map (captured fixture) recovers each packed item's type via typeKeyOf; `[...view.byType.keys()].sort()` deep-equals `['D','F','T']`.
    - Test "round-trip O(1) per item": every packed item id appears in exactly one byType aggregate; total of byType counts equals summary.items_packed (31).
    - Test "groups by type and pallet (SC-3)": view.byType.get('T').count === 12, get('D').count === 11, get('F').count === 8; view.pallets has length 2 with palletIds ['P001','P002'].
    - Test "surfaces raw cog + support_ratio (SC-3 / D-08)": view.pallets[0].cog deep-equals done.result.pallets[0].cog (unchanged numbers, NOT remapped to Three.js space); a sampled mapped item's support_ratio equals the source item's support_ratio verbatim.
    - Test "multi-pallet": each MappedPallet exposes utilisation + totalWeight + items[]; pallet item counts sum to 31.
    - Test "unpacked": view.unpacked has length 7 and passes through item_id/dimensions/weight/reason raw.
    - Test "map-primary recovery beats parse": when an explicit idToType map is supplied that maps a fixture id to a DIFFERENT typeId than its prefix, the mapper uses the map (proves map is primary, parse is fallback — D-07 / Pitfall 2).
    - Test "non-mutating": after mapping, the source fixture items have no injected `typeId` property (spread, not assign — Pitfall 5).
  </behavior>
  <action>
    Create `src/lib/result-mapper.test.ts` importing `mapDoneResponse` from `@/lib/result-mapper` (does not exist yet — RED), `doneResponse from '@/lib/__fixtures__/pack-done-response.json'`, and `type { DoneResponse } from '@/types/pack-contract'`; cast `const done = doneResponse as DoneResponse`. Use golden-literal assertions for the VERIFIED counts (31 packed, 7 unpacked, 2 pallets, byType D=11/T=12/F=8, pallet ids P001/P002) per the mapping.test.ts literal style — do NOT re-derive counts from the fixture at test time, hard-code them so a regrouping bug fails loudly. Name tests with `-t`-matchable substrings from 03-VALIDATION.md: `round-trip`, `groups by type and pallet`, `multi-pallet`, `unpacked`. For the map-primary test, construct a small `Map<string,string>` overriding one known fixture id (e.g. `'D000' -> 'OVERRIDE'`) and assert the override typeId shows up in byType. NO fenced implementation of the mapper — tests only.

    Run the suite and confirm it FAILS (module not found) before Task 2.
  </action>
  <verify>
    <automated>npm run test -- src/lib/result-mapper.test.ts 2>&1 | tail -15; echo "EXPECT: failing because @/lib/result-mapper does not exist yet (RED)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/result-mapper.test.ts` exists and imports from `@/lib/result-mapper` (which does not exist yet) and from `@/lib/__fixtures__/pack-done-response.json`.
    - Test names include literal substrings `round-trip`, `groups by type and pallet`, `multi-pallet`, `unpacked` — matchable by the `-t` commands in 03-VALIDATION.md.
    - Golden literals present: `items_packed`→31, unpacked length 7, pallets length 2, byType counts D=11/T=12/F=8, sorted keys `['D','F','T']`.
    - A map-primary test supplies an explicit idToType override and asserts the map wins over `typeKeyOf`.
    - `npm run test -- src/lib/result-mapper.test.ts` exits NON-zero (RED — implementation absent).
    - The test mounts no Canvas and imports no three (`grep -c "from 'three'\|@react-three\|Canvas" src/lib/result-mapper.test.ts` returns 0).
  </acceptance_criteria>
  <done>A failing, fixture-grounded test suite exists encoding the SC-1 round-trip (map-primary, parse-fallback), SC-3 dual grouping, raw-diagnostic pass-through, multi-pallet + unpacked, and non-mutation; it fails only because the implementation is not yet written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement mapDoneResponse (single-pass dual-axis regrouping) to pass the suite (GREEN)</name>
  <files>src/lib/result-mapper.ts</files>
  <read_first>
    - src/lib/result-mapper.test.ts (the contract just written — implement exactly to satisfy it)
    - src/lib/mapping.ts (purity-header wording at lines 14-15 to mirror; `typeKeyOf` at lines 55-58 — import and use as the FALLBACK recovery channel only; do NOT call `mapPlacement` — coordinate mapping is out of scope, D-08)
    - src/lib/palette.ts (colorForType at lines 15-25 — confirm byType keys are plain strings that dedupe+sort cleanly so the Phase 6 legend stays stable; A4 key-compatibility invariant)
    - src/lib/__fixtures__/pack-done-response.json (field names the mapper reads: result.input_summary, result.pallets[].{pallet_id,utilisation,cog,total_weight,items[]}, item.{item_id,weight,support_ratio,...}, result.unpacked_items[])
    - .planning/phases/03-pure-transform-core/03-RESEARCH.md § Pattern 3 (the single-pass implementation + key-compatibility note) + § Anti-Patterns (no three import, no cog remap, no support_ratio bucketing, no input mutation)
  </read_first>
  <action>
    Create `src/lib/result-mapper.ts` opening with the purity-header comment (pure; no runtime `three`; no React; no IO). Import TYPE-ONLY from `@/types/pack-contract`: `DoneResponse`, `PlacementOut`, `PalletResult`, `UnpackedItem`, `InputSummary`, `Cog`. Import the runtime function `typeKeyOf` from `./mapping` (sibling relative import, matching mapping.ts convention).

    Declare and export the output interfaces (D-08 shape):
    - `TypeAggregate` — `{ typeId: string; count: number; totalWeight: number; itemIds: string[] }`.
    - `MappedPallet` — `{ palletId: string; utilisation: number; cog: Cog; totalWeight: number; items: Array<PlacementOut & { typeId: string }> }` (raw placement + recovered type; cog passed RAW).
    - `ResultView` — `{ summary: InputSummary; pallets: MappedPallet[]; byType: Map<string, TypeAggregate>; unpacked: UnpackedItem[] }`.

    Export `function mapDoneResponse(done: DoneResponse, idToType?: Map<string, string>): ResultView`. Implementation: build a `byType` Map; define `recoverType = (id) => idToType?.get(id) ?? typeKeyOf(id)` (map PRIMARY, parse FALLBACK — D-07). Single pass over `done.result.pallets`, mapping each pallet to a `MappedPallet` (palletId=p.pallet_id, utilisation, cog passed raw, totalWeight=p.total_weight) and each item to `{ ...it, typeId }` (SPREAD, never assign onto the input — Pitfall 5); simultaneously fold each item into its `byType` aggregate (count+1, totalWeight+=it.weight, itemIds.push(it.item_id)). Return `{ summary: done.result.input_summary, pallets, byType, unpacked: done.result.unpacked_items }`. Do NOT remap `cog` into Three.js space, do NOT bucket `support_ratio` into tiers, do NOT import or call `mapPlacement` — those are Phase 6 (D-08).
  </action>
  <verify>
    <automated>npm run test -- src/lib/result-mapper.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/result-mapper.ts` contains `export function mapDoneResponse(` and exports `ResultView`, `MappedPallet`, `TypeAggregate`.
    - `npm run test -- src/lib/result-mapper.test.ts` exits 0 (all Task-1 tests now GREEN).
    - `[...view.byType.keys()].sort()` equals `['D','F','T']` for the captured fixture via the `typeKeyOf` fallback (SC-1 round-trip); byType counts are D=11/T=12/F=8; view.pallets length 2; view.unpacked length 7.
    - cog and support_ratio are passed through unchanged (deep-equal the source) — no Three.js remap, no tint bucketing (D-08).
    - An explicit idToType map overrides the `typeKeyOf` fallback (map-primary proven).
    - The transform is non-mutating: the source fixture items gain no `typeId` property.
    - Purity: `grep -c "from 'three'\|@react-three\|from 'react'\|from 'fs'\|from 'path'\|mapPlacement" src/lib/result-mapper.ts` returns 0.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>mapDoneResponse exists, regroups the captured fixture by type and by pallet in one pass, recovers types map-primary/parse-fallback, surfaces raw cog + support_ratio, handles multi-pallet + 7 unpacked non-mutatively, imports zero React/IO/three, and passes the full Task-1 suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 5 client → mapDoneResponse | The `done` response will (in Phase 5) originate from the network. In Phase 3 the only input is the committed, trusted fixture JSON — no live deserialization occurs. The malformed-response trust boundary is owned by Phase 5's zod validation (D-04), tracked below. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | malformed/adversarial `done`-response shape reaching the mapper | accept (Phase-3 scope) / transfer (to Phase 5) | This phase uses hand-written TS interfaces and trusts the typed contract (D-04). Runtime validation (zod at the GET /jobs/{id} trust boundary) is DEFERRED to Phase 5 — recorded here as an intentional, tracked gap, NOT silently dropped. Mitigation eased now by keeping the mapper non-mutating and tolerant (`idToType?.get(id) ?? typeKeyOf(id)`), which the Phase 5 hardening builds on. |
| T-03-07 | Tampering / Integrity | id→type recovery (map vs parse) | mitigate | Wrong type recovery would mis-group boxes and destabilize the legend (data integrity). Mitigated by map-primary recovery (format-independent) with `typeKeyOf` only as fallback, plus the explicit map-primary test that proves the map overrides the parse. |
| T-03-08 | Information Disclosure | pure module import surface | accept | No IO, no network, no secrets, no DOM. The mapper shapes an in-memory object. grep-0 purity criterion is the backstop and protects the code-split gate. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed this phase (RESEARCH § Package Legitimacy Audit). No supply-chain surface. |

This is a pure, offline, IO-free, network-free transform. No `high`-severity threats. The one real forward-looking item (Phase 5 must zod-validate network `done` responses) is explicitly tracked here so the gap is intentional.
</threat_model>

<verification>
Per 03-VALIDATION.md sampling:
- Per task commit: `npm run test -- src/lib/result-mapper.test.ts` (< 5s).
- Per wave merge: `npm run test` (full suite — confirms byType keys stay compatible with the existing palette/legend; no grouping-key regression).
- SC-4 purity backstop (phase gate): `npm run build && node scripts/check-code-split.mjs` must stay green (proves result-mapper never pulled three / never leaked into the entry chunk).
</verification>

<success_criteria>
- `mapDoneResponse` regroups the captured fixture by type AND by pallet in one pass, surfacing raw cog + support_ratio (SC-3).
- The SC-1 round-trip holds: every packed item recovers to its type (map-primary, `typeKeyOf` fallback) and byType keys equal `['D','F','T']`.
- Multi-pallet (2) and 7 unpacked items surface correctly; the transform is non-mutating.
- Co-located tests pass; module imports zero React/IO/three (SC-4 contribution).
</success_criteria>

<output>
Create `.planning/phases/03-pure-transform-core/03-03-SUMMARY.md` when done.
</output>
