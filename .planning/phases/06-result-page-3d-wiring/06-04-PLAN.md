---
phase: 06-result-page-3d-wiring
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - src/components/result/PlacementList.tsx
  - src/components/result/PlacementList.test.tsx
  - src/components/result/UnpackedPanel.tsx
  - src/components/result/UnpackedPanel.test.tsx
  - src/components/viewer/Boxes.tsx
  - src/routes/ResultPage.tsx
  - e2e/api-poll.spec.ts
autonomous: true
requirements: [RESULT-05, RESULT-06]
must_haves:
  truths:
    - "The placement list shows every box in the selected pallet with id, type, size, position, orientation, weight, and support%"
    - "Hovering a placement row highlights the matching box mesh (emissive glow) keyed by item_id (one-way, D-11)"
    - "The unpacked panel appears only when unpacked_items.length > 0, each row showing id, type, dims, weight, and reason"
    - "When everything packed, the unpacked block is omitted and an 'All items packed ✓' affordance shows"
  artifacts:
    - path: "src/components/result/PlacementList.tsx"
      provides: "per-selected-pallet placement cards + hover→mesh wiring (RESULT-05)"
      contains: "onMouseEnter"
    - path: "src/components/result/UnpackedPanel.tsx"
      provides: "conditional whole-job unpacked panel (RESULT-06)"
      contains: "reason"
    - path: "src/components/viewer/Boxes.tsx"
      provides: "hoveredId emissive prop (declarative)"
      contains: "emissiveIntensity"
  key_links:
    - from: "src/components/result/PlacementList.tsx"
      to: "src/routes/ResultPage.tsx"
      via: "onMouseEnter -> setHoveredId(item_id)"
      pattern: "setHoveredId"
    - from: "src/routes/ResultPage.tsx"
      to: "src/components/viewer/Boxes.tsx"
      via: "hoveredId prop -> emissiveIntensity"
      pattern: "hoveredId"
---

<objective>
The placement + unpacked vertical slice (RESULT-05 + RESULT-06): add the per-selected-pallet
Placement list (id, type, size, position, orientation, weight, AND the always-shown
support%) with one-way hover→mesh highlighting (D-11), and the conditional whole-job
Unpacked panel (id, type, dims, weight, reason — shown only when items are unpacked, else
an "All items packed ✓" affordance, D-06). Wire the declarative `hoveredId` emissive prop
into `Boxes` (no imperative `material.emissive.set`, no remount, keyed by `item_id`).

After this slice a user can browse the per-box placement list and watch the matching mesh
glow on hover, and can see exactly which items could not be packed and why.

Purpose: deliver SC-3 (placement list + hover) and SC-4 (unpacked panel + reasons).
Output: `PlacementList`, `UnpackedPanel` (+ tests), the `Boxes` `hoveredId` emissive prop,
the rail wiring in `ResultPage`, and the hover→emissive e2e.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-result-page-3d-wiring/06-PATTERNS.md
@.planning/phases/06-result-page-3d-wiring/06-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
See 06-01-PLAN.md for the full phase artifact list. This plan adds: `PlacementList`,
`UnpackedPanel`, and the `Boxes` `hoveredId` prop (declarative `emissiveIntensity`). These
are NEW symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Placement list cards + hover state wiring (RESULT-05, D-11)</name>
  <files>src/components/result/PlacementList.tsx, src/components/result/PlacementList.test.tsx</files>
  <read_first>
    - src/features/config/BoxRow.tsx (THE visual template: the swatch lines 85-89, the mono id-tag lines 95-97, the multi-field grid lines 128-137 — the grid cells to mirror for Size / Position / Support)
    - src/lib/result-mapper.ts (MappedPallet.items are `PlacementOut & {typeId}` — already carry item_id, position, dimensions, orientation.name, weight, support_ratio, typeId; do NOT re-derive)
    - src/lib/palette.ts (colorForType — the shared swatch colour keyed by typeId; the SAME map as legend/box-tint, passed in from ResultPage's buildPalette)
    - src/components/Card.tsx + src/components/SectionLabel.tsx (block chrome + the `Placement` label)
    - .planning/phases/06-result-page-3d-wiring/06-UI-SPEC.md (§Copywriting "Placement block label" 152, "Placement note" 153 corrected to box min-corner, "Placement card" 154 incl. the third Support datum + §Color accent #3 hovered card + §Accessibility "Placement row → mesh hover" focus=hover parity)
    - .planning/phases/06-result-page-3d-wiring/06-PATTERNS.md (§PlacementList.tsx)
  </read_first>
  <behavior>
    - Renders one card per item in the SELECTED pallet's `items`: a type swatch (colorForType by typeId), mono `{item_id}`, a rotation tag `{orientation.name}`, right-aligned mono `{weight} kg`, a sub-line `{typeId}`, and a field grid with `Size L·W·H` → `{l}·{w}·{h}` (mm, post-orientation dims), `Position x,y,z` → `{x}, {y}, {z}` (the API min-corner), and `Support` → `{(support_ratio*100).toFixed(0)}%` (always shown — DIAG-02)
    - The block label is `Placement` with a right mono `{palletLabel} · {N} items` (per-selected-pallet, D-03)
    - onMouseEnter on a card calls onHover(item_id); onMouseLeave calls onHover(null) — one-way (D-11); the hovered card gets accent border + accent-weak bg
    - The placement note reads `positions are box min-corner · mm · origin = pallet corner` (corrected from the mockup "box-centre", C-01)
    - Every datum is readable without hovering (hover is progressive enhancement, a11y); imports no three/r3f/drei
  </behavior>
  <action>
    Create `src/components/result/PlacementList.tsx` taking `items: (PlacementOut & {typeId})[]`,
    `palette: Map<string,string>`, `palletLabel: string`, and `onHover: (id: string|null) => void`. Head it with
    a `Placement` SectionLabel + a right mono `{palletLabel} · {items.length} items`. Render the placement note
    hint `positions are box min-corner · mm · origin = pallet corner` (C-01 correction — do NOT copy the mockup's
    "box-centre"). For each item render a card mirroring `BoxRow.tsx`: the 13px swatch from `palette.get(item.typeId)`,
    the mono id-tag `{item.item_id}`, the rotation tag `{item.orientation.name}` (accent-text on accent-weak), the
    right mono `{item.weight} kg`, the `{item.typeId}` sub-line, and a field grid with three cells —
    `Size L·W·H` → `{item.dimensions.L}·{item.dimensions.W}·{item.dimensions.H}`, `Position x,y,z` →
    `{item.position.x}, {item.position.y}, {item.position.z}`, `Support` → `{(item.support_ratio*100).toFixed(0)}%`
    (always shown, DIAG-02). Each card: `onMouseEnter={() => onHover(item.item_id)}` /
    `onMouseLeave={() => onHover(null)}`; the hovered card gets accent border + `--accent-weak` bg. All API strings
    rendered as React text only (V5). Imports ONLY React, clsx, palette/types, Card/SectionLabel — NO three/r3f/drei
    (C-04). Create `src/components/result/PlacementList.test.tsx` (jsdom, RTL): render the fixture P001 items and
    assert a representative card shows its item_id, size, position, orientation name, weight, and `support` %;
    fire `mouseEnter`/`mouseLeave` on a card and assert `onHover` is called with the item_id then `null`.
  </action>
  <verify>
    <automated>npx vitest run src/components/result/PlacementList.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/result/PlacementList.tsx` renders cards with id, size, position, orientation name, weight, and a `Support` %
    - The placement note text is `positions are box min-corner · mm · origin = pallet corner` (not "box-centre")
    - Cards call `onHover(item_id)` on mouseEnter and `onHover(null)` on mouseLeave
    - `npx vitest run src/components/result/PlacementList.test.tsx` exits 0 covering the datum render + the hover callbacks
    - `grep -E "three|@react-three|@/components/viewer" src/components/result/PlacementList.tsx` returns nothing
  </acceptance_criteria>
  <done>The placement list renders every per-pallet box's fields incl. support%, and fires one-way hover callbacks; three-free; tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unpacked panel (conditional whole-job, reasons, RESULT-06, D-06)</name>
  <files>src/components/result/UnpackedPanel.tsx, src/components/result/UnpackedPanel.test.tsx</files>
  <read_first>
    - src/features/config/BoxRow.tsx (the head row swatch + id + meta lines 84-117)
    - src/features/config/BoxCatalogCard.tsx (the empty-state pattern lines 59-65)
    - src/lib/result-mapper.ts (ResultView.unpacked: UnpackedItem[] — item_id, dimensions, weight, reason)
    - src/lib/mapping.ts (typeKeyOf — the parse-fallback type recovery)
    - src/types/pack-contract.ts (UnpackedItem lines 114-119)
    - .planning/phases/06-result-page-3d-wiring/06-UI-SPEC.md (§Copywriting "Unpacked block label" 155, "Unpacked row" 156, "All-packed affordance" 157 using --color-pos + §Color "Semantic positive")
    - .planning/phases/06-result-page-3d-wiring/06-PATTERNS.md (§UnpackedPanel.tsx)
  </read_first>
  <behavior>
    - When `unpacked.length > 0`: renders a `Could not pack` block with a right mono `{N} items`; each row shows mono `{item_id}`, the recovered `{type}` (idToType ?? typeKeyOf), dims `{l}·{w}·{h} mm`, `{weight} kg`, and the `{reason}` rendered as PLAIN TEXT (V5 — never dangerouslySetInnerHTML)
    - When `unpacked.length === 0`: the block is OMITTED and an inline `All items packed ✓` affordance shows using the `--color-pos` token
    - Whole-job scope: the panel content does NOT change when the selected pallet changes; non-interactive (no mesh)
    - Imports no three/r3f/drei
  </behavior>
  <action>
    Create `src/components/result/UnpackedPanel.tsx` taking `unpacked: UnpackedItem[]` and
    `idToType?: Map<string,string>`. If `unpacked.length === 0`, render a calm single-line `All items packed ✓`
    affordance using the `--color-pos` token (text/checkmark, NOT a fill) and render NO block (D-06). Otherwise
    render a `Could not pack` SectionLabel + right mono `{unpacked.length} items`, then one row per item mirroring
    the `BoxRow` head: mono `{item_id}`, the recovered type `idToType?.get(item.item_id) ?? typeKeyOf(item.item_id)`
    (C-03), dims `{item.dimensions.L}·{item.dimensions.W}·{item.dimensions.H} mm`, `{item.weight} kg`, and the
    `{item.reason}` as PLAIN React text (V5 / T-06-07 — never dangerouslySetInnerHTML/innerHTML). Non-interactive
    (no hover/mesh). Imports ONLY React, clsx, typeKeyOf, types, Card/SectionLabel — NO three/r3f/drei (C-04).
    Create `src/components/result/UnpackedPanel.test.tsx` (jsdom, RTL): (a) with the fixture's 7 unpacked items,
    assert the `Could not pack` label, the `7 items` count, and a representative row's id + reason text;
    (b) with an empty array, assert the block is absent and `All items packed ✓` is present.
  </action>
  <verify>
    <automated>npx vitest run src/components/result/UnpackedPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/result/UnpackedPanel.tsx` renders a row per unpacked item with id, type, dims, weight, and `{reason}` as text
    - With an empty unpacked array it renders `All items packed ✓` and no `Could not pack` block
    - `grep -c dangerouslySetInnerHTML src/components/result/UnpackedPanel.tsx` returns 0
    - `npx vitest run src/components/result/UnpackedPanel.test.tsx` exits 0 covering both the populated and empty cases
    - `grep -E "three|@react-three|@/components/viewer" src/components/result/UnpackedPanel.tsx` returns nothing
  </acceptance_criteria>
  <done>The unpacked panel renders conditionally with reasons as escaped text, or the all-packed affordance; three-free; tested.</done>
</task>

<task type="auto">
  <name>Task 3: Boxes hoveredId emissive prop + wire placement/unpacked into the rail (D-11)</name>
  <files>src/components/viewer/Boxes.tsx, src/routes/ResultPage.tsx</files>
  <read_first>
    - src/components/viewer/Boxes.tsx (current per-mesh material lines 59-66; BoxesProps lines 31-35 — add `hoveredId?: string|null`; the mapped useMemo lines 43-54)
    - src/routes/ResultPage.tsx (the Plan-02/03 shell: `hoveredId`/`setHoveredId` state already declared in Plan 02; `sel`/`selPallet`; the rail aside with SummaryBlock + PalletSwitcher from Plan 03 — append PlacementList + UnpackedPanel; the Boxes usage to pass hoveredId)
    - .planning/phases/06-result-page-3d-wiring/06-RESEARCH.md (§Pattern 5 — declarative emissiveIntensity prop, NO imperative material.emissive.set, no ref, no remount; r3f patches the live material in place)
    - .planning/phases/06-result-page-3d-wiring/06-PATTERNS.md (§Boxes.tsx — the EXTEND props + the declarative emissive block)
  </read_first>
  <action>
    (1) `Boxes.tsx`: add `hoveredId?: string | null` to `BoxesProps`. In the per-mesh `<meshStandardMaterial>`,
    add `emissive={b.color}` and `emissiveIntensity={hoveredId === b.id ? 0.45 : 0}` (declarative — r3f diffs the
    prop and patches the material in place; NO imperative `material.emissive.set()`, NO ref, NO remount; keyed by
    the box id which is `item_id`). Keep `roughness={0.62} metalness={0.04}` and the existing `<Edges>`. Do NOT
    switch to InstancedMesh (D-12 — individual meshes make per-box emissive trivial; max pallet is 19 boxes).
    (2) `ResultPage.tsx`: pass `hoveredId` to `<Boxes>`. Append `<PlacementList items={selPallet.items-with-typeId}
    palette={palette} palletLabel={selectedLabel} onHover={setHoveredId} />` and `<UnpackedPanel unpacked={view.unpacked}
    idToType={navState?.idToType} />` to the rail aside (after Summary + Switcher). The PlacementList items come
    from `view.pallets[sel].items` (the `PlacementOut & {typeId}` array — already typed). The UnpackedPanel is
    whole-job scope (does not change on switch).
  </action>
  <verify>
    <automated>npx vitest run && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/viewer/Boxes.tsx` `BoxesProps` includes `hoveredId?: string | null` and the material uses `emissiveIntensity={hoveredId === b.id ? 0.45 : 0}`
    - Boxes.tsx contains NO `material.emissive.set` and NO InstancedMesh
    - `src/routes/ResultPage.tsx` renders `<PlacementList` and `<UnpackedPanel` in the rail and passes `hoveredId` to `<Boxes` and `setHoveredId` as PlacementList `onHover`
    - `npm run typecheck` clean; `npx vitest run` full unit suite green
  </acceptance_criteria>
  <done>Boxes highlights the hovered item declaratively; the rail shows the placement list + unpacked panel wired to selected-pallet/whole-job state.</done>
</task>

<task type="auto">
  <name>Task 4: e2e — hovering a placement row glows the matching mesh (RESULT-05)</name>
  <files>e2e/api-poll.spec.ts</files>
  <read_first>
    - e2e/api-poll.spec.ts (the stubbed Configure→Run→Result flow reaching a populated /result)
    - e2e/result-viewer.spec.ts (the canvas-screenshot readback pattern lines 64-69 — compare canvas pixels before/after hover to prove the emissive glow rendered)
    - .planning/phases/06-result-page-3d-wiring/06-VALIDATION.md (§Per-Task map RESULT-05 — e2e emissive glow; the WebGL half is Playwright-only, never jsdom)
  </read_first>
  <action>
    Add a Playwright test (in `e2e/api-poll.spec.ts` or the result-viewer spec) that reaches a populated
    `/result` via the stubbed flow, captures the canvas screenshot, hovers the first placement card (e.g. by its
    item_id text), waits a beat for the emissive material to repaint, re-captures the canvas, and asserts the
    pixels DIFFER (the matching mesh glowed). Then move the mouse off the card and assert the glow clears (pixels
    return toward the baseline, or simply that an off-hover capture differs from the hovered one). All API routes
    stubbed; never the live API.
  </action>
  <verify>
    <automated>npm run build && npx playwright test e2e/api-poll.spec.ts e2e/result-viewer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - A Playwright test hovers a placement card and asserts the canvas pixels change (emissive glow rendered)
    - The test asserts the glow clears on mouse-leave
    - `npx playwright test` passes; no live-API call
  </acceptance_criteria>
  <done>An e2e proves hovering a placement row highlights the matching mesh in the real scene.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| react-query cache → rail | API-sourced `item_id`, `reason`, `orientation.name`, `typeId` rendered into the placement + unpacked rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering / Information Disclosure | API `reason`/`item_id`/`orientation.name` rendered into placement + unpacked rows (XSS) | mitigate | Rendered as React text children only (auto-escaped); never `dangerouslySetInnerHTML`/`innerHTML`. Verified by `grep -c dangerouslySetInnerHTML == 0` on both new rail files. ASVS V5 / mirrors Phase-5 ErrorCard rule. |
| T-06-08 | Denial of Service | A placement item with a missing/NaN field rendering crash | accept | The body is zod-parsed upstream; fields are read defensively (`.toFixed`/text). No client crash for a single-user tool; low risk. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed (RESEARCH Package Legitimacy Audit — N/A). `npm ci` against the pinned lockfile only. |
</threat_model>

<verification>
- `npx vitest run` full unit suite green (PlacementList + UnpackedPanel tests + unaffected)
- `npm run typecheck` clean
- `npm run build && node scripts/check-code-split.mjs` — the two new rail components stay three-free; Boxes' emissive prop adds no new three import (already in the lazy chunk)
- `npx playwright test e2e/api-poll.spec.ts e2e/result-viewer.spec.ts` green (incl. the hover→emissive test)
</verification>

<success_criteria>
- Placement list shows every per-pallet box's fields incl. support% (RESULT-05)
- Hovering a row highlights the matching mesh declaratively (D-11) — proven by e2e
- Unpacked panel appears conditionally with reasons; all-packed affordance otherwise (RESULT-06/D-06)
- API strings rendered as escaped text (T-06-07)
- code-split gate green
</success_criteria>

<output>
Create `.planning/phases/06-result-page-3d-wiring/06-04-SUMMARY.md` when done.
</output>
