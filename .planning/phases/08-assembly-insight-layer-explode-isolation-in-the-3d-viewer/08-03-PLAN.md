---
phase: 08-assembly-insight-layer-explode-isolation-in-the-3d-viewer
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/components/viewer/Boxes.tsx
  - src/components/viewer/LayerControls.tsx
  - src/components/result/PlacementList.tsx
  - src/routes/ResultPage.tsx
  - e2e/assembly-insight.spec.ts
autonomous: true
requirements: [RESULT-07]

must_haves:
  truths:
    - "The Layers slider in Build-up mode hides upper not-yet-revealed layers (show 1..k); in Isolate mode it dims non-focused layers to translucent ghosts and shows layer k solid"
    - "Default (All / Build-up-full) leaves every layer visible and opaque — byte-identical to the assembled view (no transparency)"
    - "Clicking a placement-list row sets Isolate mode focused on that box's layer (revealing it if hidden) and shows a persistent selected cue distinct from hover"
    - "Switching pallets resets explode to 0 and layer-focus to default, while the camera is still preserved"
    - "Layer-focus composes with explode, presets, CoG/heatmap toggles, and the existing one-way hover highlight without regression"
  artifacts:
    - path: "src/components/viewer/Boxes.tsx"
      provides: "Per-layer visibility (build-up hide) + ghost opacity (isolate dim) as derived props; transparent only when opacity<1"
      contains: "transparent"
    - path: "src/components/viewer/LayerControls.tsx"
      provides: "Layers native range slider (0..N) + Build-up/Isolate mode toggle following the dark-overlay pattern"
      contains: "Isolate"
    - path: "src/components/result/PlacementList.tsx"
      provides: "Row onClick -> onIsolate(item_id) + a persistent selectedId cue distinct from the transient hover cue"
      exports: ["default"]
    - path: "src/routes/ResultPage.tsx"
      provides: "focusMode + focusIndex state, reset-on-switch, row-click->isolate wiring, layerModel-driven layer count"
      contains: "focusMode"
  key_links:
    - from: "src/routes/ResultPage.tsx"
      to: "src/components/result/PlacementList.tsx"
      via: "onIsolate maps item_id -> layerModel.itemToLayer.get(id)+1, sets Isolate focus; selectedId cue"
      pattern: "onIsolate"
    - from: "src/components/viewer/Boxes.tsx"
      to: "src/lib/computeLayers.ts (LayerModel)"
      via: "per-layer visible/opacity derived from focusMode + focusIndex + layerIndex"
      pattern: "focusMode"
    - from: "src/routes/ResultPage.tsx"
      to: "setSel reset path"
      via: "wrap pallet switch to reset explode=0 + focus default; camera preserved (measureNonce only)"
      pattern: "setFocusIndex"
---

<objective>
Deliver the Layers control end-to-end as the second vertical slice: a single Layers slider (0..N,
floor-up) plus a Build-up/Isolate mode toggle (D-08) that reveals layers cumulatively (build-up HIDES
upper layers) or isolates one layer (isolate DIMS the rest to translucent ghosts, D-09), wired so that
clicking a placement-list row jumps the viewer to that box's isolated layer (D-12), pallet switches reset
both controls while preserving the camera (D-11), and everything composes simultaneously with explode and
the existing diagnostics/hover (D-10/SC-4) — with the All/Build-up-full default keeping the scene
byte-identical to assembled (SC-3).

Purpose: Complete RESULT-07 — after this plan a user can read a dense pallet layer by layer (build it up
from the floor, or isolate-and-ghost any single layer), and the placement list becomes a "layer remote".
This is the final slice; it closes SC-3 (default no-op), SC-4 (full compose matrix), and the D-11/D-12
integration. Implements RESULT-07 (SC-3, SC-4; D-08/09/10/11/12).
Output: per-layer visibility/opacity in Boxes, the Layers slider + mode toggle in LayerControls, the
row-click->isolate seam in PlacementList, ResultPage focus state + reset-on-switch + row-click wiring,
and the isolate/build-up/compose/reset e2e cases appended to the assembly-insight spec.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-assembly-insight-layer-explode-isolation-in-the-3d-viewer/08-CONTEXT.md
@.planning/phases/08-assembly-insight-layer-explode-isolation-in-the-3d-viewer/08-RESEARCH.md
@.planning/phases/08-assembly-insight-layer-explode-isolation-in-the-3d-viewer/08-PATTERNS.md
@.planning/phases/08-assembly-insight-layer-explode-isolation-in-the-3d-viewer/08-UI-SPEC.md
@.planning/phases/08-assembly-insight-layer-explode-isolation-in-the-3d-viewer/08-VALIDATION.md
@.planning/phases/08-assembly-insight-layer-explode-isolation-in-the-3d-viewer/08-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New / extended symbols in THIS plan (created here — exclude from undefined-symbol drift checks):

- `Boxes` new props: `focusMode: 'buildup' | 'isolate'`, `focusIndex: number | null` + a pure
  `layerAppearance(layerIndex, focusMode, focusIndex)` helper returning `{ visible, opacity }` — `src/components/viewer/Boxes.tsx`
- `LayerControls` extension: Layers `<input type="range">` (0..N) + Build-up/Isolate `role="switch"` toggle
  + props `focusMode`, `onFocusMode`, `focusIndex`, `onFocusIndex` — `src/components/viewer/LayerControls.tsx`
- `PlacementList` new props: `onIsolate?: (itemId: string) => void`, `selectedId?: string | null` — `src/components/result/PlacementList.tsx`
- `ResultPage` new state: `focusMode`, `focusIndex`, a wrapped `selectPallet` that resets explode +
  focus, the row-click->isolate handler + `selectedId` derivation — `src/routes/ResultPage.tsx`
- `GHOST_OPACITY` constant (~0.15) — `src/components/viewer/Boxes.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Per-layer build-up hide + isolate ghost opacity in Boxes (derived props)</name>
  <files>src/components/viewer/Boxes.tsx</files>
  <read_first>
    - src/components/viewer/Boxes.tsx (as left by Plan 02) — the per-layer wrapper groups + the
      explode useFrame damp. This task adds visibility + opacity to those SAME groups/meshes; do NOT
      restructure the explode work.
    - 08-RESEARCH.md Pattern 2 (lines 227-246) — the EXACT layerAppearance(layerIndex, focusMode,
      focusIndex) logic: focusIndex==null -> {visible:true,opacity:1}; buildup -> {visible: layerIndex<=k0,
      opacity:1} (k0 = focusIndex-1, HIDE above); isolate -> focused {visible:true,opacity:1}, others
      {visible:true,opacity:~0.15}. Pitfall 4 (lines 317-321 ghost must NOT recolor — only transparent +
      opacity, leave color/emissive to heatmap/hover). Pitfall 5 (lines 323-327 transparent=false at
      opacity 1 to keep the default byte-identical + avoid sort artifacts).
    - 08-PATTERNS.md "Boxes.tsx (EDIT)" (b) (lines 105-119) — the declarative material props block: set
      ONLY transparent={opacity<1} + opacity={opacity}; leave color/emissive/emissiveIntensity unchanged.
    - 08-UI-SPEC.md "Color" (ghost opacity ~0.12-0.18 is a 3D-material constant, not a token) +
      "Net-New Interaction Contract" (Layers slider + mode toggle row).
  </read_first>
  <action>
    Add props focusMode: 'buildup' | 'isolate' and focusIndex: number | null to BoxesProps and a
    GHOST_OPACITY constant (~0.15). Add a pure helper layerAppearance(layerIndex /*0-based*/, focusMode,
    focusIndex) -> { visible: boolean; opacity: number } implementing RESEARCH Pattern 2 exactly:
    focusIndex == null -> { visible: true, opacity: 1 } (default All); buildup -> show layers with
    layerIndex <= focusIndex-1 (visible:true), HIDE the rest (visible:false), opacity 1; isolate -> the
    focused layer (layerIndex === focusIndex-1) is { visible:true, opacity:1 }, all others { visible:true,
    opacity:GHOST_OPACITY }. Apply visible to the per-layer wrapper group (a hidden build-up layer drops
    out entirely). Apply the ghost opacity to the per-box meshStandardMaterial by setting ONLY
    transparent={opacity < 1} and opacity={opacity} (Pitfall 5: transparent must be FALSE when
    opacity===1 so the default is byte-identical and there are no transparency sort artifacts). Leave the
    existing color / emissive / emissiveIntensity (hover) / heatmap colour + the <Edges> UNTOUCHED so
    heatmap colour and the hover glow remain visible THROUGH the ghost (Pitfall 4 / SC-4). Optionally fade
    opacity via maath easing (discretion — not load-bearing); instant is acceptable. Add focusMode +
    focusIndex to the relevant useMemo/useFrame deps. Default (focusIndex null) MUST reproduce the Plan-02
    behavior exactly.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Boxes.tsx declares focusMode + focusIndex props and a GHOST_OPACITY constant.
    - A layerAppearance helper returns {visible,opacity}; buildup hides layerIndex>focusIndex-1; isolate ghosts non-focused layers to GHOST_OPACITY.
    - The material sets transparent={opacity < 1} (transparent is FALSE at opacity 1): `grep -n "transparent" src/components/viewer/Boxes.tsx` shows the opacity<1 guard.
    - color/emissive/emissiveIntensity remain set as in Plan 02 (hover + heatmap unchanged): the existing emissiveIntensity hover line is still present.
    - `npx tsc --noEmit` exits 0 and `npx vitest run` stays green.
  </acceptance_criteria>
  <done>Boxes hides upper layers in build-up and ghosts non-focused layers in isolate via derived props; default (null focus) is byte-identical; hover/heatmap compose through the ghost.</done>
</task>

<task type="auto">
  <name>Task 2: Layers slider + mode toggle in LayerControls; PlacementList row-click -> isolate</name>
  <files>src/components/viewer/LayerControls.tsx, src/components/result/PlacementList.tsx</files>
  <read_first>
    - src/components/viewer/LayerControls.tsx (as left by Plan 02) — the Explode portion + the bar
      container/pill conventions. Add the Layers slider + the mode toggle to the SAME bar.
    - src/components/viewer/ViewerOverlay.tsx (lines 113-135) — the role=switch toggle button + the exact
      active/inactive class pair to copy for the Build-up/Isolate two-way toggle (accent = active mode).
    - src/components/result/PlacementList.tsx (full) — the one-way hover seam (onMouseEnter/Leave ->
      onHover, lines 80-94) + the hovered-card accent cue (line 93). Add onClick alongside, and a
      PERSISTENT selected cue distinct from the transient hover cue.
    - 08-PATTERNS.md "PlacementList.tsx (EDIT)" (lines 185-207) — the exact onClick + selectedId props +
      the border-accent ring-1 ring-accent persistent cue vs the hover border-accent bg-accent-weak.
    - 08-UI-SPEC.md "Net-New Interaction Contract" (Layers slider + mode toggle; placement row click ->
      isolate with a persistent selected cue) + "Copywriting Contract" (Layers readout: All default,
      Layer {k} / {N} when focused; mode pills Build-up/Isolate; Single layer disabled at N===1; No boxes
      at N===0) + "Accessibility" (Layers slider aria-label "Layer focus", aria-valuetext mirrors the
      readout; mode toggle role=switch/radio with aria state).
  </read_first>
  <action>
    LayerControls.tsx: Add to the bar (a) a Build-up/Isolate mode toggle — two role=switch pills using the
    ViewerOverlay active/inactive class pair (accent only on the active mode), bound to props focusMode +
    onFocusMode; (b) a Layers native <input type="range"> (min 0, max = layerCount, step 1) bound to props
    focusIndex (treat 0/null as "All") + onFocusIndex, with a readout that reads All at 0/null and
    Layer {k} / {layerCount} when focused (k floor-up, 1-based per D-08). a11y: aria-label "Layer focus",
    aria-valuetext mirrors the readout. Disabled states: layerCount===1 -> disable the Layers slider + mode
    toggle, readout Single layer, pointer-events-none + reduced opacity (UI-SPEC); layerCount===0 -> the
    whole bar disabled, readout No boxes. Keep the Explode portion from Plan 02 unchanged. (Build-up + All
    = focusIndex null/0 is the no-op default, D-02/SC-3.)

    PlacementList.tsx: Add props onIsolate?: (itemId: string) => void and selectedId?: string | null
    (mirror the onHover prop doc). Add onClick={() => onIsolate?.(item.item_id)} to the card alongside the
    existing onMouseEnter/Leave (hover stays the one-way emissive seam, unchanged — D-11/SC-4). Add a
    PERSISTENT selected cue for the row whose item_id === selectedId, distinct from the transient hover cue:
    use border-accent ring-1 ring-accent for selected vs the existing border-accent bg-accent-weak for
    hover (so "this row drove the current isolation" is legible). Keep the C-04 import discipline (no three;
    React/clsx + types + SectionLabel only). Render every string as React text children only (V5 — no
    raw-HTML sink). Optionally add a small Layer {k} chip reusing the Field micro-label style
    (font-mono text-[9.5px] uppercase) — planner discretion, not required.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run src/components/result/PlacementList.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LayerControls.tsx contains a Layers <input type="range" with aria-label "Layer focus" and a Build-up/Isolate role=switch toggle; source contains the literals Build-up, Isolate, and All.
    - LayerControls renders Single layer when layerCount===1 and No boxes when layerCount===0 (both literals present).
    - PlacementList.tsx declares onIsolate + selectedId props and an onClick calling onIsolate with item.item_id.
    - PlacementList applies a persistent selected cue (ring-accent or equivalent) distinct from the hover bg-accent-weak: `grep -n "selectedId" src/components/result/PlacementList.tsx` shows the cue.
    - The existing onHover/onMouseEnter hover seam is unchanged (still present).
    - `npx tsc --noEmit` exits 0; `npx vitest run src/components/result/PlacementList.test.tsx` stays green (extend the test if the new props change rendering).
  </acceptance_criteria>
  <done>The Layers slider + Build-up/Isolate toggle drive focus; placement rows are clickable to isolate with a persistent selected cue; hover unchanged; disabled states correct.</done>
</task>

<task type="auto">
  <name>Task 3: ResultPage focus state, reset-on-switch, row-click wiring; isolate/build-up/compose/reset e2e + phase gate</name>
  <files>src/routes/ResultPage.tsx, e2e/assembly-insight.spec.ts</files>
  <read_first>
    - src/routes/ResultPage.tsx (as left by Plan 02) — the explode state + layerModel + the <Boxes>,
      <LayerControls>, <PalletSwitcher onSelect={setSel}> (line 334), <PlacementList ... onHover=
      {setHoveredId}> (lines 337-343) wiring. This task adds focus state, the reset-on-switch wrapper, and
      the row-click handler.
    - 08-PATTERNS.md "ResultPage.tsx (EDIT)" (lines 215-238) — the focus state idiom, the computeLayers
      memo, the reset-on-switch wrapper, and the onIsolate handler (setFocusMode('isolate');
      setFocusIndex(layerModel.itemToLayer.get(id)+1)).
    - 08-CONTEXT.md D-11 (reset explode->0 + focus->default on switch, camera preserved) and D-12 (row
      click isolates that box's layer, revealing if hidden; hover unchanged).
    - e2e/assembly-insight.spec.ts (as left by Plan 02) — the harness + the existing explode tests to
      append to (reuse the same route-interception helpers + __cameraState hook + data hooks).
    - 08-VALIDATION.md per-task map (SC-3 default byte-identical, SC-4 full compose matrix, D-05 camera
      unchanged on switch) + the phase-gate sampling (full unit + Playwright + code-split before verify).
  </read_first>
  <action>
    ResultPage.tsx: Add const [focusMode, setFocusMode] = useState<'buildup'|'isolate'>('buildup') and
    const [focusIndex, setFocusIndex] = useState<number | null>(null) (null = All, the no-op default).
    Pass focusMode + focusIndex to <Boxes> and focusMode/onFocusMode + focusIndex/onFocusIndex + layerCount
    to <LayerControls>. Wrap the pallet-switch path: replace onSelect={setSel} on <PalletSwitcher> with a
    selectPallet(i) that calls setSel(i) AND resets explode to 0 (setExplode(0); bump explodeNonce so the
    camera re-fits back to the assembled frame) AND resets focus (setFocusMode('buildup');
    setFocusIndex(null)) — D-11. The camera is still preserved across the switch because CameraPresets only
    re-frames on presetNonce/explodeNonce and re-measures (without animating) on measureNonce={selIndex};
    resetting explode to 0 sets explodeExtraHeight to 0 so any re-fit targets the assembled frame, NOT a
    snap toward a preset on the new pallet — confirm the camera-unchanged e2e still passes. Wire the row
    click: pass onIsolate={(id) => { const li = layerModel.itemToLayer.get(id); if (li != null) {
    setFocusMode('isolate'); setFocusIndex(li + 1); setSelectedId(id); } }} (track the clicked id in a
    selectedId state) and selectedId to <PlacementList> (D-12 — isolate shows the focused layer, revealing
    it if it was hidden). Keep onHover unchanged. Also clear selectedId in the selectPallet reset.

    e2e/assembly-insight.spec.ts: APPEND tests against the same stubbed flow:
    (1) BUILD-UP HIDES: set the Layers slider to k<N in Build-up mode; assert the canvas differs from All
    (upper layers gone) and from Isolate-of-the-same-k.
    (2) ISOLATE DIMS: switch mode to Isolate at layer k; assert the canvas differs from build-up (rest
    ghosted, not hidden) and from assembled.
    (3) ROW CLICK -> ISOLATE (D-12): click a PlacementList card (data-placement-card), assert the clicked
    row shows the persistent selected cue (ring-accent class or a selected attribute) AND the canvas changes
    to an isolated view; assert the existing hover still lights a mesh (hover unchanged).
    (4) RESET ON SWITCH (D-11): set explode>0 + a focus, switch pallets, assert the Explode readout returns
    to Assembled and the Layers readout to All, and (D-05/Pitfall 1) the camera position is unchanged by the
    switch itself (within epsilon).
    (5) FULL COMPOSE (SC-4): build-up to k AND explode>0 simultaneously, then press a preset + toggle CoG
    and heatmap; assert zero console errors and a rendered canvas.
    Keep all tests route-intercepted (never the live API). SC-5 (interactive FPS) remains the documented
    manual check, not asserted here.
  </action>
  <verify>
    <automated>npx playwright test e2e/assembly-insight.spec.ts</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - ResultPage.tsx declares focusMode + focusIndex (+ selectedId) state and passes them to <Boxes>/<LayerControls>/<PlacementList>.
    - PalletSwitcher onSelect is wrapped so a switch resets explode to 0 and focus to default (setFocusIndex(null)): `grep -n "setFocusIndex" src/routes/ResultPage.tsx` shows the reset path.
    - onIsolate maps item_id via layerModel.itemToLayer and sets Isolate focus: `grep -n "onIsolate" src/routes/ResultPage.tsx` present.
    - The appended e2e covers build-up-hides, isolate-dims, row-click->isolate, reset-on-switch (readouts return to Assembled/All; camera unchanged), and full compose with zero console errors.
    - `npx playwright test e2e/assembly-insight.spec.ts` passes and `npx vitest run` stays green.
  </acceptance_criteria>
  <done>ResultPage owns focus state with reset-on-switch and row-click->isolate; the e2e proves build-up/isolate/compose/reset; phase behavior complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver output -> viewer focus state | Already-validated `done` placements + the Plan-01 layer model drive visibility/opacity. No new network, no new persistence. |
| discrete control / row click -> focus state | A bounded Layers slider, a two-way mode toggle, and a row click set numeric focus indices; no value reaches a DOM/HTML sink. |

## STRIDE Threat Register

Client-only 3D-viewer enhancement: NO new network call, NO auth, NO new persistence, NO user-supplied
text reaching a sink. The realistic threat surface is minimal — stated explicitly per the security gate.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-XSS | Tampering/Info-disclosure | LayerControls readouts + PlacementList selected cue / optional Layer chip | mitigate | Readouts/chips are numbers + static literals (All, Layer k / N, Build-up, Isolate, Single layer, No boxes); rendered as React text children only — never dangerouslySetInnerHTML (locked PlacementList/ResultPage escaped-text rule). |
| T-08-DOS | Denial of Service | layerAppearance / focus indexing | mitigate | focusIndex is bounded to 0..layerCount; layerAppearance is pure with a null no-op; itemToLayer lookup guarded (li != null) before setting focus. No unbounded loop. |
| T-08-REG | Tampering (behavior regression) | reset-on-switch vs camera preservation | mitigate | Reset sets explode 0 (explodeExtraHeight 0) so the switch re-frame targets the assembled frame, never a preset snap on the new pallet (D-02/D-11); asserted by the camera-unchanged-on-switch e2e. |
</threat_model>

<verification>
- `npx vitest run` — full unit suite green (no regression in the baseline).
- `npx tsc --noEmit` — type-clean across the new focus props + wiring.
- `npx playwright test e2e/assembly-insight.spec.ts` — explode + build-up + isolate + row-click + reset +
  compose all green.
- `npm run build && node scripts/check-code-split.mjs` — exits 0; three stays out of index-*.js (the
  LayerControls + PlacementList changes stay three-free / inside the lazy chunk; computeLayers three-free).
- Manual (SC-5, documented in 08-VALIDATION.md): load a demo preset, drag Explode 0->max and scrub the
  Layers control — confirm interactive frame rate with no visible stutter.
</verification>

<success_criteria>
- Build-up hides upper layers; Isolate ghosts the rest; default (All/Build-up) byte-identical to assembled (SC-3).
- Row click isolates the box's layer with a persistent selected cue; hover unchanged (D-12/SC-4).
- Pallet switch resets explode + focus, camera preserved (D-11/D-05).
- Full compose matrix (explode + focus + preset + CoG + heatmap + hover) renders with no console errors (SC-4).
- Code-split gate green; full unit suite + the assembly-insight e2e green.
</success_criteria>

<output>
Create `.planning/phases/08-assembly-insight-layer-explode-isolation-in-the-3d-viewer/08-03-SUMMARY.md` when done.
</output>
