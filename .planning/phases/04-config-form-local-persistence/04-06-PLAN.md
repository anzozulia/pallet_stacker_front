---
phase: 04-config-form-local-persistence
plan: 06
type: execute
wave: 4
depends_on: [04-01, 04-04]
files_modified:
  - src/features/config/PalletCard.tsx
  - src/features/config/PalletCard.test.tsx
autonomous: true
requirements: [PALLET-01, PALLET-02, PACK-03]

must_haves:
  truths:
    - "A user can set pallet length, width, and max stack height in mm"
    - "A user can set pallet max weight (kg) and max overhang (mm)"
    - "A user can set how many pallets the solver may use (maxPallets)"
    - "The pallet card renders the Dimensions and Limits sections over the light tokens, with no CoG envelope field"
  artifacts:
    - path: "src/features/config/PalletCard.tsx"
      provides: "pallet dims + limits + maxPallets fields card"
      contains: "maxPallets"
    - path: "src/features/config/PalletCard.test.tsx"
      provides: "pallet field binding test"
      contains: "Max pallets"
  key_links:
    - from: "src/features/config/PalletCard.tsx"
      to: "react-hook-form register"
      via: "register('pallet.length') etc. + register('maxPallets')"
      pattern: "register"
---

<objective>
Build the Pallet configuration card: a `Dimensions` section (length, width, max stack height — mm) and a `Limits` section (max weight — kg, max overhang — mm, and `Max pallets` — the integer solver budget that replaces the mockup's CoG-envelope field). Bind every field through RHF `register`, composed from the Plan-04 primitives. Component-test the field bindings.

Purpose: This is the pallet/limits half of the user story (PALLET-01/02, PACK-03). It honors C-04 (NO CoG-envelope input) and D-10 (maxPallets lives in the pallet/limits group, the other three API options stay baked in the request-builder). It runs parallel with the box-catalog slice (Plan 05) — no shared files.

Output: `src/features/config/PalletCard.tsx` + test.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-config-form-local-persistence/04-CONTEXT.md
@.planning/phases/04-config-form-local-persistence/04-RESEARCH.md
@.planning/phases/04-config-form-local-persistence/04-PATTERNS.md
@.planning/phases/04-config-form-local-persistence/04-UI-SPEC.md
@src/types/config.ts
@src/components/Hello.test.tsx
@design/config.html
</context>

## Phase Goal

**As a** self-hosting user describing a packing job, **I want to** set the target pallet's dimensions and physical limits and how many pallets the solver may use, **so that** the packer respects my real envelope and constraints.

## Artifacts this phase produces

- **`src/features/config/PalletCard.tsx`:** wraps the `Card` primitive (title "Pallet configuration", desc "— the physical envelope and constraints"). `Dimensions` section: `Length`/`Width`/`Max stack height` NumberFields (mm). `Limits` section: `Max weight` (kg), `Max overhang` (mm), `Max pallets` (integer). Binds `pallet.length`/`pallet.width`/`pallet.height`/`pallet.maxWeight`/`pallet.maxOverhang` and `maxPallets` via RHF `register`. NO CoG-envelope field (C-04); the mockup's "Allow overhang" boolean is omitted (numeric `maxOverhang` covers it — RESEARCH Open Question 2).

<tasks>

<task type="auto">
  <name>Task 1: Build + test PalletCard (Dimensions + Limits incl. Max pallets, no CoG)</name>
  <files>src/features/config/PalletCard.tsx, src/features/config/PalletCard.test.tsx</files>
  <read_first>
    - .planning/phases/04-config-form-local-persistence/04-UI-SPEC.md §"Copywriting Contract" (Card 1 title/desc; pallet section labels Dimensions/Limits; pallet field labels — "replace the mockup's CoG envelope field with the Max pallets integer field"), §"Color"/§"Typography"/§"Spacing Scale" (cols-3 grid, section-label spacing)
    - .planning/phases/04-config-form-local-persistence/04-CONTEXT.md §"C-04" (drop CoG-envelope field) + §"D-10" (maxPallets in the pallet/limits group; other 3 options baked)
    - .planning/phases/04-config-form-local-persistence/04-RESEARCH.md §"Pattern 1" (register number fields; mm integer vs kg decimal), §"Open Questions" #2 (omit the separate Allow-overhang boolean)
    - src/types/config.ts (PalletConfig: length/width/height/maxWeight/maxOverhang; PackConfig.maxPallets)
    - src/components/NumberField.tsx, src/components/Card.tsx, src/components/SectionLabel.tsx (the Plan-04 primitive prop contracts)
    - design/config.html (the Pallet card markup lines ~186-231 — port Dimensions/Limits grid; DROP the CoG field at lines ~218-221 and the Allow-overhang switch at ~225-227)
  </read_first>
  <action>
    Create `src/features/config/PalletCard.tsx` (`export default function PalletCard`). Receive RHF `register` (or use `useFormContext`). Wrap in `Card` (title "Pallet configuration", desc "— the physical envelope and constraints"). Render a `SectionLabel` "Dimensions" over a `cols-3` grid of NumberFields: `Length` / `Width` / `Max stack height` bound to `pallet.length`/`pallet.width`/`pallet.height` (unit `mm`). Render a `SectionLabel` "Limits" (xl/32px top margin) over a `cols-3` grid: `Max weight` (`pallet.maxWeight`, unit `kg`), `Max overhang` (`pallet.maxOverhang`, unit `mm`), `Max pallets` (`maxPallets`, integer — D-10/PACK-03). Do NOT render a CoG-envelope field (C-04) and do NOT render a separate Allow-overhang boolean (numeric maxOverhang covers it). Surface RHF `formState.errors` for each field into the NumberField `error` prop. Import only `@/components/*`, `@/types/config`, React, RHF, `clsx` — NEVER three/viewer (C-05).

    Write `src/features/config/PalletCard.test.tsx` (render inside an RHF wrapper seeded from DEFAULT_CONFIG): assert the six fields render with their labels (Length/Width/Max stack height/Max weight/Max overhang/Max pallets); assert NO "CoG envelope" text is present (C-04 guard); assert editing a field updates the form value (or that the inputs are registered). @testing-library/react + user-event; jsdom-WebGL-free.
  </action>
  <verify>
    <automated>npx vitest run src/features/config/PalletCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/config/PalletCard.test.tsx` passes: the six pallet/limits fields render and bind (PALLET-01/02, PACK-03).
    - The test asserts NO "CoG envelope" field exists (C-04) — `screen.queryByText(/CoG/i)` is null.
    - `Max pallets` is present in the Limits group (D-10), bound to `maxPallets`.
    - `grep -c "from 'three'\|/viewer/" src/features/config/PalletCard.tsx` is 0.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>
    The pallet card edits length/width/max-stack-height (mm), max-weight (kg), max-overhang (mm), and max-pallets (integer), with NO CoG field — composed from the Plan-04 primitives, three-free (PALLET-01/02, PACK-03, C-04, D-10).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user numeric input → form state | Pallet/limits numbers are user-entered; validated by the zod resolver (Plan 07 wiring) at the form boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-IV | Tampering | invalid pallet numbers (negative/zero/non-integer) | mitigate | Inputs are validated by `packConfigSubmitSchema` (Plan 01) via the resolver wired in Plan 07; the Run gate blocks on invalid (D-02/BOX-06). This card only renders + registers fields. |
| T-4-06 | (quality) | CoG field accidentally ported | mitigate | The test asserts no "CoG envelope" field exists (C-04) — pinned, not just documented. |
</threat_model>

<verification>
- `npx vitest run src/features/config/PalletCard.test.tsx` passes (incl. the no-CoG assertion).
- `npm run test` + `npm run typecheck` pass at wave merge.
- `npm run build && node scripts/check-code-split.mjs` passes (pallet card three-free).
</verification>

<success_criteria>
- Pallet length/width/max-stack-height (mm), max-weight (kg), max-overhang (mm), and max-pallets (integer) are all editable (PALLET-01/02, PACK-03).
- No CoG-envelope input field (C-04); maxPallets in the limits group (D-10).
- The card is three-free.
</success_criteria>

<output>
Create `.planning/phases/04-config-form-local-persistence/04-06-SUMMARY.md` when done.
</output>
