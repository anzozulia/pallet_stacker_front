---
phase: 02-coordinate-mapping-fixture-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/__fixtures__/pack-request.json
  - src/lib/__fixtures__/pack-done-response.json
  - src/lib/fixture-types.ts
  - src/lib/mapping.ts
  - src/lib/mapping.test.ts
  - src/lib/palette.ts
  - src/lib/palette.test.ts
autonomous: true
requirements: [RESULT-01]
must_haves:
  truths:
    - 'A real multi-pallet `done` response (with a 3-cycle rotated box) is committed in the repo as a fixture'
    - 'mapPlacement returns the exact mapped centre+size for the non-rotated golden case (T000)'
    - 'mapPlacement returns the exact mapped centre+size for the rotated golden case (D003), using post-orientation dimensions and NOT re-applying perm'
    - 'Every mapped pallet-0 box sits within the pallet envelope (AABB invariant holds, 0 violations)'
    - 'Box-type colours are deterministic: the whole-fixture type set {D,F,T} maps to the three seed colours in stable sorted order'
  artifacts:
    - path: 'src/lib/__fixtures__/pack-done-response.json'
      provides: 'Committed golden done-response (2 pallets, 7 unpacked, types D/F/T, 3-cycle perm)'
      contains: '"status": "done"'
    - path: 'src/lib/__fixtures__/pack-request.json'
      provides: 'Committed capture request for reproducibility (D-03)'
    - path: 'src/lib/mapping.ts'
      provides: 'Pure API-placement to Three.js mesh-transform mapping (D-05)'
      exports: ['mapPlacement', 'assertWithinEnvelope', 'MappedBox']
      min_lines: 25
    - path: 'src/lib/mapping.test.ts'
      provides: 'Golden non-rotated + rotated + AABB-envelope tests'
      contains: 'non-rotated'
    - path: 'src/lib/palette.ts'
      provides: 'Deterministic box-type colour palette (D-09)'
      exports: ['colorForType']
    - path: 'src/lib/palette.test.ts'
      provides: 'Determinism + whole-fixture type-set colour test'
  key_links:
    - from: 'src/lib/mapping.test.ts'
      to: 'src/lib/mapping.ts'
      via: '@/lib import + golden assertions on captured numbers'
      pattern: "from '@/lib/mapping'"
    - from: 'src/lib/palette.test.ts'
      to: 'src/lib/palette.ts'
      via: '@/lib import'
      pattern: "from '@/lib/palette'"
---

<objective>
Resolve and lock the single highest-risk piece of the whole product: the pure function that maps the API's coordinate space (z-up, min-corner positions in mm) into Three.js mesh transforms. Commit the already-captured real `done` response as the golden fixture, then build the pure `src/lib/` mapping and palette functions against it with golden-value Vitest tests — including the load-bearing rotated case that proves `dimensions` is consumed post-orientation and `orientation.perm` is NOT re-applied.

This is the inside-out foundation: the pure transforms must exist and be golden-tested BEFORE the Plan 02 viewer consumes them (`depends_on` ordering). Per MVP vertical-slice rules, this slice ends with a provably-correct, tested mapping — the next slice renders it.

Purpose: Eliminate the locked-risk (RESEARCH § "Coordinate Mapping" resolved it empirically; this plan pins the resolution with executable golden tests so no future change can silently regress axis order, half-extent, recentre, or the z→y swap).
Output: `src/lib/__fixtures__/{pack-request,pack-done-response}.json`, `src/lib/fixture-types.ts`, `src/lib/mapping.ts` (+ test), `src/lib/palette.ts` (+ test). Delivers the data + math half of RESULT-01.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coordinate-mapping-fixture-viewer/02-RESEARCH.md
@.planning/phases/02-coordinate-mapping-fixture-viewer/02-PATTERNS.md
@.planning/phases/02-coordinate-mapping-fixture-viewer/02-CONTEXT.md
</context>

<artifacts_this_phase_produces>
Symbols and files this plan creates (source-grounding allowlist — newly-created, not drift):

**Files:**

- `src/lib/__fixtures__/pack-request.json` — committed capture request
- `src/lib/__fixtures__/pack-done-response.json` — committed golden done-response
- `src/lib/fixture-types.ts` — hand-written TS interfaces for the fixture shape
- `src/lib/mapping.ts` — pure mapping module
- `src/lib/mapping.test.ts` — golden + AABB tests
- `src/lib/palette.ts` — deterministic palette module
- `src/lib/palette.test.ts` — palette tests

**Functions / values:**

- `mapPlacement(placement, pallet, typeKey): MappedBox` — pure geometry mapping
- `assertWithinEnvelope(placement, pallet): void` — dev-only AABB sanity assertion (`import.meta.env.DEV`-gated)
- `typeKeyOf(itemId): string` — leading-alpha-prefix extractor (e.g. `T000` → `T`)
- `colorForType(typeKeys: string[]): Map<string, string>` — deterministic type→hex palette
- `DECK_TOP_Y` — module constant (`100`)
- `SEED_COLORS` — module constant (`['#6d63f5', '#0ea5a3', '#e0892b']`)

**Types / interfaces:**

- `MappedBox` — `{ id: string; typeKey: string; size: [number,number,number]; center: [number,number,number] }`
- `PlacementOut`, `PalletDims`, `DoneResponse`, `Vec3`, `BoxDims`, `Orientation` (in `fixture-types.ts`)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Commit the captured golden fixture + hand-write its TS types</name>
  <files>src/lib/__fixtures__/pack-request.json, src/lib/__fixtures__/pack-done-response.json, src/lib/fixture-types.ts</files>
  <read_first>
    - .planning/phases/02-coordinate-mapping-fixture-viewer/captured-fixture/pack-request.json (the source to copy verbatim)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/captured-fixture/pack-done-response.json (the source to copy verbatim — the golden data)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-RESEARCH.md § "Fixture Capture Procedure" (verify-before-locking checklist) + § "Architecture Patterns" Pattern 1 (field semantics: position = MIN CORNER, dimensions = POST-orientation, perm = scatter/diagnostic-only)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-PATTERNS.md § "src/lib/__fixtures__" (exact-copy directive + verified golden anchors)
    - src/components/Hello.tsx (file-header comment convention to copy)
  </read_first>
  <action>
Copy the two captured files verbatim from `.planning/phases/02-coordinate-mapping-fixture-viewer/captured-fixture/` into `src/lib/__fixtures__/` (create the dir). Per D-01/D-02/D-03/A4 these are the richest real capture (2 pallets, 7 unpacked, types D/F/T, 3-cycle perm `[2,0,1]`) — DO NOT re-capture; the live API is not needed (no auth, already captured). Before locking, assert the verify-before-locking checklist holds: `status: "done"`, `result.pallets.length === 2`, `result.unpacked_items.length === 7`, ≥3 distinct request box types, and ≥1 placement with `orientation.perm` ≠ `[0,1,2]` (D003 has `[2,0,1]`).

Create `src/lib/fixture-types.ts` with hand-written named-export interfaces typing the fixture shape (zod is NOT used this phase per RESEARCH § "Standard Stack" — defer to Phase 5). Match the REAL committed JSON exactly: top-level `DoneResponse { job_id: string; status: string; result: { input_summary; pallets: PalletResult[]; unpacked_items: UnpackedItem[] } }`; `PalletResult { pallet_id: string; dimensions: PalletDims; utilisation: number; cog; total_weight: number; items: PlacementOut[] }`; `PlacementOut { item_id: string; position: Vec3; dimensions: BoxDims; orientation: Orientation; weight: number; support_ratio: number; supported_by: string[]; supports: string[] }`; `Vec3 { x: number; y: number; z: number }`; `BoxDims { L: number; W: number; H: number }`; `PalletDims { L: number; W: number; H: number; max_weight: number }`; `Orientation { perm: [number,number,number]; name: string }`. Note the item array key is `items` (NOT `placements`). Use `import type` only or zero three imports — this file must be safe to import from anywhere (code-split discipline, Pitfall 3). Open the file with a terse why-comment per house style.
</action>
<acceptance_criteria> - `src/lib/__fixtures__/pack-done-response.json` exists and contains the string `"status": "done"` and `"perm": [2, 0, 1]` (or `[2,0,1]`) - `src/lib/__fixtures__/pack-request.json` exists (byte-identical to the captured source) - `src/lib/fixture-types.ts` exists and exports `DoneResponse`, `PlacementOut`, `PalletDims`, `Vec3`, `BoxDims`, `Orientation` - `node -e "const r=require('./src/lib/__fixtures__/pack-done-response.json'); if(r.status!=='done'||r.result.pallets.length!==2||r.result.unpacked_items.length!==7) process.exit(1)"` exits 0 - `npx tsc -b --noEmit` exits 0 (fixture-types compiles)
</acceptance_criteria>
<verify>
<automated>node -e "const r=require('/Users/anzozulia/Desktop/pallet_stacker_front/src/lib/**fixtures**/pack-done-response.json'); const ok=r.status==='done'&&r.result.pallets.length===2&&r.result.unpacked_items.length===7&&r.result.pallets[0].items.some(i=>JSON.stringify(i.orientation.perm)==='[2,0,1]'); process.exit(ok?0:1)"</automated>
</verify>
<done>Both fixture JSONs committed under src/lib/**fixtures**/; fixture-types.ts compiles and exports the named interfaces; the done-response passes the verify-before-locking checklist (2 pallets, 7 unpacked, 3-cycle perm present).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure coordinate-mapping function + golden + AABB tests</name>
  <files>src/lib/mapping.ts, src/lib/mapping.test.ts</files>
  <read_first>
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-RESEARCH.md § "Architecture Patterns" Pattern 1 (the verified mapping formula, lines ~160-206), Pattern 2 (perm is diagnostic-only — do NOT re-apply), Pattern 5 (dev-mode AABB assertion), § "Code Examples → Golden-value test" (exact expected numbers), § "Common Pitfalls" 1+2 (min-corner vs centre; double-applying orientation)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-PATTERNS.md § "src/lib/mapping.ts" (no-analog → use RESEARCH; code-split discipline; verified golden anchors) + § "src/lib/mapping.test.ts" (test wiring from Hello.test.tsx)
    - src/lib/fixture-types.ts (the types created in Task 1)
    - src/lib/__fixtures__/pack-done-response.json (the golden source — read pallet 0 T000 + D003)
    - src/components/Hello.test.tsx (vitest wiring + @/ alias + jsdom-WebGL-free comment to copy)
  </read_first>
  <behavior>
    - Test "non-rotated": `mapPlacement({item_id:'T000', position:{x:0,y:0,z:0}, dimensions:{L:250,W:250,H:700}}, {L:1000,W:800}, 'T')` → `size === [250,700,250]` (API L→x, H→y, W→z) and `center === [-375, 450, -275]`
    - Test "rotated": `mapPlacement({item_id:'D003', position:{x:0,y:0,z:700}, dimensions:{L:150,W:600,H:300}}, {L:1000,W:800}, 'D')` → `size === [150,300,600]` (uses post-orientation dims verbatim — perm `[2,0,1]` is NOT applied) and `center === [-425, 950, -100]`
    - Test "envelope": for every item in fixture pallet 0, `assertWithinEnvelope(item, pallet0.dimensions)` produces 0 violations (each box's AABB sits within `[0,L]×[0,W]×[0,H]` in API space pre-recentre)
  </behavior>
  <action>
Write `src/lib/mapping.ts` as a pure, IO-free module (D-05). Export `interface MappedBox { id: string; typeKey: string; size: [number,number,number]; center: [number,number,number] }`. Export `function mapPlacement(p, pallet, typeKey)` implementing the VERIFIED formula: `size = [dims.L, dims.H, dims.W]`; `center = [position.x + dims.L/2 - pallet.L/2, DECK_TOP_Y + position.z + dims.H/2, position.y + dims.W/2 - pallet.W/2]` with `const DECK_TOP_Y = 100` (blockH 78 + deckH 22; tunable). Consume ONLY `position` + `dimensions` — do NOT read or apply `orientation.perm` (it is already baked into `dimensions`; re-applying double-rotates — Pitfall 2). Export `function typeKeyOf(itemId: string): string` returning the leading non-digit prefix (e.g. `T000`→`T`, `D003`→`D`, `F011`→`F`). Export `function assertWithinEnvelope(p, pallet)` guarded by `if (!import.meta.env.DEV) return;` so it tree-shakes from prod (Pattern 5) — emit `console.error('[AABB] ...', p.item_id)` on any escape past the pallet `[0,L]×[0,W]×[0,H]` envelope (eps 1e-6). Type params via `src/lib/fixture-types.ts` (`PlacementOut`, `PalletDims`); import three only as `import type` or not at all (Pitfall 3 — keep three out of the entry chunk). Open with a why-comment stating the locked-risk resolution (position=min-corner, dims=post-orientation, perm NOT applied).

Write `src/lib/mapping.test.ts` co-located, copying the wiring from `Hello.test.tsx` (`import { describe, expect, it } from 'vitest'`, `@/lib/mapping` import, jsdom-WebGL-free — never import a Canvas). Three `describe`/`it` blocks named so the RESEARCH `-t` filters hit: `non-rotated`, `rotated`, `envelope`. The non-rotated and rotated tests assert the exact tuples above (use literal numbers, not re-derived from the formula, so a formula bug fails loudly). The envelope test loops `result.pallets[0].items` from the imported fixture JSON, calls a pure envelope-check helper (or asserts the inline AABB predicate) and expects 0 violations. The rotated case is the load-bearing proof — add a comment marking it so.
</action>
<acceptance_criteria> - `src/lib/mapping.ts` exports `mapPlacement`, `assertWithinEnvelope`, `typeKeyOf`, and the `MappedBox` interface - `src/lib/mapping.ts` contains NO non-type `three` import (grep: no `import .* from 'three'` without `type`) so the code-split gate is not threatened - `npx vitest run src/lib/mapping.test.ts -t "non-rotated"` exits 0 - `npx vitest run src/lib/mapping.test.ts -t "rotated"` exits 0 and the test asserts `size [150,300,600]` + `center [-425,950,-100]` - `npx vitest run src/lib/mapping.test.ts -t "envelope"` exits 0 with 0 violations over all pallet-0 items
</acceptance_criteria>
<verify>
<automated>cd /Users/anzozulia/Desktop/pallet_stacker_front && npx vitest run src/lib/mapping.test.ts</automated>
</verify>
<done>mapping.ts is pure (no runtime three import), exports the named API, and all three golden/AABB test groups pass with the exact captured numbers; the rotated case proves post-orientation dims are used and perm is not re-applied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deterministic box-type palette + test</name>
  <files>src/lib/palette.ts, src/lib/palette.test.ts</files>
  <read_first>
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-RESEARCH.md § "Code Examples → Deterministic box-type palette" (seed colours + spinHue scheme) + § "Common Pitfalls" 5 (legend from whole-fixture type set, not pallet-0)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-UI-SPEC.md § "Box-type colour palette (D-09)" (seed hex table + extend-harmoniously rule)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-PATTERNS.md § "src/lib/palette.ts" (no-analog → use RESEARCH; test wiring identical to mapping.test.ts)
    - src/lib/mapping.ts (reuse typeKeyOf for type derivation; same export/header convention)
    - src/lib/__fixtures__/pack-done-response.json (whole-fixture type set is {D,F,T})
  </read_first>
  <behavior>
    - Test "deterministic": `colorForType(['F','T','D'])` and `colorForType(['D','T','F'])` return Maps with identical entries (sort-stable regardless of input order)
    - Test "seed assignment": the whole-fixture type set sorted `['D','F','T']` maps to the three seeds in order — `D`→`#6d63f5`, `F`→`#0ea5a3`, `T`→`#e0892b`
    - Test "extension": a 4th type beyond the 3 seeds gets a distinct deterministic hex (not equal to any seed), proving spinHue extension
  </behavior>
  <action>
Write `src/lib/palette.ts` as a pure module (D-09) exporting `const SEED_COLORS = ['#6d63f5', '#0ea5a3', '#e0892b']` (UI-SPEC indices 0/1/2: indigo/teal/amber) and `function colorForType(typeKeys: string[]): Map<string,string>`: dedupe + `.sort()` for deterministic order, assign `SEED_COLORS[i]` for `i < 3`, else a harmonious extension via `spinHue(SEED_COLORS[i % 3], i)` (parse hex→HSL, add `(i*47)deg` hue, keep S/L near the seed, return hex). Keep S/L close so the legend reads as one family. Pure — no three, no React, no IO. Open with a why-comment (deterministic so a type always gets the same colour across renders + the legend).

Write `src/lib/palette.test.ts` co-located, wiring copied from `mapping.test.ts`/`Hello.test.tsx`. Assert the three behaviours above. For the seed-assignment test, derive the type set from the imported fixture (`typeKeyOf` over all pallets' items + unpacked) to prove the whole-fixture set is `{D,F,T}` (Pitfall 5 recommendation (a) — legend uses the whole fixture so 3 swatches show even though pallet 0 carries only D+T).
</action>
<acceptance_criteria> - `src/lib/palette.ts` exports `colorForType` and `SEED_COLORS` - `npx vitest run src/lib/palette.test.ts` exits 0 - the determinism test proves `colorForType(['F','T','D'])` deep-equals `colorForType(['D','T','F'])` - the seed-assignment test asserts `D→#6d63f5`, `F→#0ea5a3`, `T→#e0892b` from the sorted whole-fixture type set - `src/lib/palette.ts` contains no `three` / `react` import (pure)
</acceptance_criteria>
<verify>
<automated>cd /Users/anzozulia/Desktop/pallet_stacker_front && npx vitest run src/lib/palette.test.ts</automated>
</verify>
<done>palette.ts is a pure deterministic type→colour map seeded by the mockup's three colours with a harmonious extension for extra types; the whole-fixture type set {D,F,T} maps to the three seeds in stable sorted order; tests pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                                                                                               |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none active this plan) | No runtime input crosses a boundary. The fixture is a committed, PR-reviewed static file (build-time data, not runtime fetch). Pure functions have no IO. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                         | Disposition | Mitigation Plan                                                                                                                                          |
| --------- | ---------------------- | ------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Tampering              | committed fixture JSON (`src/lib/__fixtures__/*`) | accept      | Fixed file reviewed in PR; not attacker-controlled. The golden tests + dev-mode AABB assertion catch malformed geometry at test/build time, not runtime. |
| T-02-02   | Information Disclosure | fixture contents                                  | accept      | The capture contains no PII, no secrets, no auth tokens (API needs no auth — RESEARCH § "Runtime State Inventory"). It is synthetic packing geometry.    |
| T-02-SC   | Tampering              | npm installs                                      | accept      | No new packages installed this plan — the locked quartet + vitest/playwright were supply-chain-approved in Phase 1 (T-1-SC). slopcheck N/A.              |

**ASVS L1 net:** Phase 2 has a negligible security surface (pure math + committed trusted static fixture, no auth, no runtime user input, no runtime network, no secrets — RESEARCH § "Security Domain"). No high-severity threats identified. Forward note (out of scope here): Phase 5's live client must zod-validate `GET /jobs/{id}` responses at the boundary.
</threat_model>

<verification>
- `npx vitest run src/lib/` — all golden (non-rotated + rotated), AABB-envelope, and palette tests pass (~1s)
- `npx tsc -b --noEmit` — fixture-types, mapping, palette all typecheck
- grep confirms `src/lib/mapping.ts` and `src/lib/palette.ts` contain no runtime (non-`type`) `three` import — code-split discipline preserved for Plan 02's build gate
- the rotated golden (D003 → size `[150,300,600]`, center `[-425,950,-100]`) is the load-bearing proof the locked risk is resolved
</verification>

<success_criteria>

- A real multi-pallet `done` response with a 3-cycle rotated box is committed under `src/lib/__fixtures__/` (SC-1)
- Golden-value Vitest tests assert exact mapped position+size for the non-rotated AND rotated fixture items, and the dev-mode AABB sanity invariant passes over all pallet-0 boxes (SC-2)
- `mapPlacement` and `colorForType` are pure, IO-free, jsdom-testable without a Canvas, and import three only as a type (if at all)
- RESULT-01 (data + mapping + colour-by-type half) is proven by tests; the viewer half lands in Plan 02
  </success_criteria>

<output>
Create `.planning/phases/02-coordinate-mapping-fixture-viewer/02-01-SUMMARY.md` when done
</output>
