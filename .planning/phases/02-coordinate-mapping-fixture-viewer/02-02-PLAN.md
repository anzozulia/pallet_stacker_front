---
phase: 02-coordinate-mapping-fixture-viewer
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/styles.css
  - src/routes/ResultPage.tsx
  - src/components/viewer/Pallet.tsx
  - src/components/viewer/Boxes.tsx
  - src/components/viewer/CameraPresets.tsx
  - src/components/viewer/ViewerOverlay.tsx
  - src/lib/camera-presets.ts
  - src/lib/camera-presets.test.ts
  - e2e/result-viewer.spec.ts
autonomous: false
requirements: [RESULT-01, RESULT-02]
user_setup: []
must_haves:
  truths:
    - "The /result route renders the committed fixture's pallet 0 as a 3D wood pallet with per-type-coloured boxes"
    - 'A legend (top-right) shows a swatch + name row per box type, derived from the whole-fixture type set (3 rows: D/F/T)'
    - 'A dark overlay header (top-left) shows the pallet name + a static dimensions tag from fixture pallet 0'
    - 'A user can orbit, zoom, and pan the scene (drei OrbitControls with damping + polar clamp)'
    - 'A user can click ISO / TOP / FRONT and the camera visibly reframes (TOP straight down, FRONT along an axis), auto-fit to the fixture bbox'
    - 'three/r3f/drei stay in the lazy /result chunk — the code-split build gate passes'
  artifacts:
    - path: 'src/routes/ResultPage.tsx'
      provides: 'The viewer scene replacing the empty Canvas body (D-07/D-13)'
      contains: 'data-testid="r3f-canvas"'
    - path: 'src/components/viewer/Boxes.tsx'
      provides: 'Per-box meshes from mapPlacement + colorForType + drei <Edges>'
    - path: 'src/components/viewer/CameraPresets.tsx'
      provides: 'ISO/TOP/FRONT buttons + animated bbox-fit camera presets (D-11/D-12)'
    - path: 'src/components/viewer/ViewerOverlay.tsx'
      provides: 'Dark header + legend + control hints (static chrome, D-13)'
    - path: 'src/lib/camera-presets.ts'
      provides: 'Pure bbox → ISO/TOP/FRONT camera vector math (jsdom-testable)'
      exports: ['presetFromBbox']
    - path: 'src/styles.css'
      provides: 'Dark 3D-overlay @theme token group (D-08)'
      contains: '--color-d-bg'
    - path: 'e2e/result-viewer.spec.ts'
      provides: 'Playwright Canvas-mount smoke + ISO/TOP/FRONT preset assertions'
  key_links:
    - from: 'src/components/viewer/Boxes.tsx'
      to: 'src/lib/mapping.ts'
      via: 'import mapPlacement + map over fixture pallet-0 items'
      pattern: 'mapPlacement'
    - from: 'src/components/viewer/Boxes.tsx'
      to: 'src/lib/palette.ts'
      via: 'import colorForType for per-type tint'
      pattern: 'colorForType'
    - from: 'src/routes/ResultPage.tsx'
      to: 'src/lib/__fixtures__/pack-done-response.json'
      via: 'import the committed fixture and render pallet index 0 (D-04)'
      pattern: 'pack-done-response'
    - from: 'e2e/result-viewer.spec.ts'
      to: '/result'
      via: 'page.goto + canvas visible + preset button clicks'
      pattern: "goto\\('/result'\\)"
---

<objective>
Render the locked, golden-tested mapping as a faithful, explorable 3D scene on the existing lazy `/result` route — completing the MVP vertical slice: a committed real fixture now renders as a preset-switchable 3D pallet that matches `design/result.html`. Build the wood pallet, lighting/shadows/fog/grid, per-type-coloured boxes with edge-lines, the dark overlay chrome (header + legend + control hints + ISO/TOP/FRONT buttons), and bbox-derived animated camera presets. Prove it with the Playwright preview-build smoke (Canvas mounts, ≥1 box mesh, no WebGL errors) and a preset-switch e2e.

Purpose: Deliver the visible, interactive half of RESULT-01 + all of RESULT-02. After Plan 01 proved the math, this plan makes it explorable — the slice a real user can actually use.
Output: extended `src/styles.css` (dark tokens), the viewer scene in `ResultPage.tsx` + `src/components/viewer/*`, pure `src/lib/camera-presets.ts` (+ test), and `e2e/result-viewer.spec.ts`. Consumes Plan 01's `mapPlacement`, `colorForType`, and the committed fixture.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coordinate-mapping-fixture-viewer/02-RESEARCH.md
@.planning/phases/02-coordinate-mapping-fixture-viewer/02-UI-SPEC.md
@.planning/phases/02-coordinate-mapping-fixture-viewer/02-PATTERNS.md
@.planning/phases/02-coordinate-mapping-fixture-viewer/02-01-SUMMARY.md
@design/result.html
</context>

<artifacts_this_phase_produces>
Symbols and files this plan creates (source-grounding allowlist — newly-created, not drift):

**Files:**

- `src/components/viewer/Pallet.tsx` — wood pallet model (slats + blocks), ground plane, grid
- `src/components/viewer/Boxes.tsx` — per-box meshes from the mapping + palette
- `src/components/viewer/CameraPresets.tsx` — ISO/TOP/FRONT buttons + camera-preset driver
- `src/components/viewer/ViewerOverlay.tsx` — dark header + legend + control hints (DOM chrome)
- `src/lib/camera-presets.ts` — pure bbox→camera-vector math
- `src/lib/camera-presets.test.ts` — preset math unit test
- `e2e/result-viewer.spec.ts` — Playwright Canvas + preset e2e

**Functions / values:**

- `presetFromBbox(bbox, which): { position: [number,number,number]; target: [number,number,number] }` — pure ISO/TOP/FRONT vector math from a bounding box
- React components: `Pallet`, `Boxes`, `CameraPresets`, `ViewerOverlay` (default or named per house convention)

**@theme tokens (new in `src/styles.css`):**

- `--color-d-bg: #0c0f17`
- `--color-d-border: #222838`
- `--color-d-text: #e6e8ee`
- `--color-d-text-2: #838b9e`

**Scene module constants (NOT tokens):** wood/light/grid/fog/material values per UI-SPEC § "Scene material / lighting constants".
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Port dark-overlay @theme tokens + pure camera-preset math</name>
  <files>src/styles.css, src/lib/camera-presets.ts, src/lib/camera-presets.test.ts</files>
  <read_first>
    - src/styles.css (the existing @theme block to EXTEND in place — do not duplicate --font-sans/--font-mono/--color-accent)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-UI-SPEC.md § "Token additions to src/styles.css (D-08)" (exact 4 token names + hex) + § "Interaction Contract" (ISO/TOP/FRONT behaviour, polar clamp, distances from bbox)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-RESEARCH.md § "Architecture Patterns" Pattern 4 (presets from scene bbox: ISO dir normalize(1,0.8,1.1), TOP (0,r*k,0.0001), FRONT (0,r*0.25,r*k); minDistance/maxDistance from radius) + Anti-Pattern "Hardcoding camera positions"
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-PATTERNS.md § "src/lib/camera-presets.ts" + § "src/styles.css"
    - src/lib/mapping.test.ts (test wiring convention to copy for camera-presets.test.ts)
  </read_first>
  <behavior>
    - Test "top-is-overhead": `presetFromBbox(bbox, 'TOP').position` is directly above the bbox centre (x≈cx, z≈cz, y > cy) so it looks straight down
    - Test "front-is-axis-aligned": `presetFromBbox(bbox, 'FRONT').position` is offset primarily along +z from centre at near-eye-level (looks along an axis)
    - Test "iso-is-elevated-3q": `presetFromBbox(bbox, 'ISO').position` is offset positively on all three of x, y, z from centre (three-quarter view)
    - Test "distance-scales-with-bbox": a larger bbox yields a proportionally larger camera distance (no hardcoded magic vector) and `target` equals the bbox centre for every preset
  </behavior>
  <action>
Extend the existing `@theme` block in `src/styles.css` by ADDING the D-08 dark-overlay group verbatim from UI-SPEC: `--color-d-bg: #0c0f17;`, `--color-d-border: #222838;`, `--color-d-text: #e6e8ee;`, `--color-d-text-2: #838b9e;`. Do NOT duplicate the existing font/accent tokens; do NOT add scene-material colours as tokens (they are module constants in the viewer per UI-SPEC).

Write `src/lib/camera-presets.ts` as a pure, IO-free module exporting `function presetFromBbox(bbox: { center:[number,number,number]; size:[number,number,number] }, which: 'ISO'|'TOP'|'FRONT'): { position:[number,number,number]; target:[number,number,number] }`. Compute radius `r = length(size)/2`; `target = center`; per RESEARCH Pattern 4: ISO `position = center + normalize(1,0.8,1.1)*(r*k)`, TOP `position = center + (0, r*k, 0.0001)`, FRONT `position = center + (0, r*0.25, r*k)` (k a tunable framing factor ~2.0). Also export the derived `minDistance`/`maxDistance` from `r` (e.g. `r*0.4`…`r*4`) as a helper or part of the return — used by OrbitControls in Task 2. Use plain number tuples (no three import) so this stays jsdom-testable and entry-chunk-safe. Open with a why-comment (presets derived from the fixture bbox, NOT the mockup's hardcoded vectors — D-11).

Write `src/lib/camera-presets.test.ts` co-located, wiring copied from `mapping.test.ts`. Use a synthetic bbox and assert the four behaviours above (relational assertions: TOP above centre, FRONT along +z, ISO positive on all axes, distance scales with bbox size, target===centre).
</action>
<acceptance_criteria> - `src/styles.css` contains `--color-d-bg: #0c0f17` and the other three d-\* tokens inside the @theme block, with the existing 3 tokens still present (not duplicated) - `src/lib/camera-presets.ts` exports `presetFromBbox` and contains no `three`/`react` runtime import - `npx vitest run src/lib/camera-presets.test.ts` exits 0 - the distance-scaling test proves a 2× bbox yields a larger camera distance (no hardcoded vector)
</acceptance_criteria>
<verify>
<automated>cd /Users/anzozulia/Desktop/pallet_stacker_front && npx vitest run src/lib/camera-presets.test.ts && grep -q -- '--color-d-bg' src/styles.css</automated>
</verify>
<done>The four dark-overlay tokens are added to the @theme block (existing tokens intact); presetFromBbox is a pure, tested bbox→camera-vector function whose presets scale with the fixture bounding box and never use the mockup's hardcoded vectors.</done>
</task>

<task type="auto">
  <name>Task 2: Build the drei viewer scene (pallet, boxes, controls, presets) on /result</name>
  <files>src/routes/ResultPage.tsx, src/components/viewer/Pallet.tsx, src/components/viewer/Boxes.tsx, src/components/viewer/CameraPresets.tsx, src/components/viewer/ViewerOverlay.tsx</files>
  <read_first>
    - src/routes/ResultPage.tsx (PRESERVE the 100dvh wrapper + Canvas data-testid="r3f-canvas"; REPLACE the empty body)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-RESEARCH.md § "Architecture Patterns" Pattern 3 (drei scene recipe: Canvas shadows, fog, lights, Bounds fit clip observe, OrbitControls makeDefault enableDamping maxPolarAngle, per-box mesh + Edges) + Pattern 4 (preset re-targeting) + § "Common Pitfalls" 3 (three must stay in lazy chunk) + Assumptions A1 (verify drei prop names at pmndrs.github.io/drei)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-UI-SPEC.md § "Scene material / lighting constants" (all wood/light/grid/fog/material values) + § "Box-type colour palette" (MeshStandardMaterial roughness 0.62/metalness 0.04; edge tint offsetHSL(0,-0.04,+0.18) opacity 0.55) + § "Interaction Contract"
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-PATTERNS.md § "src/routes/ResultPage.tsx" + § "viewer subcomponents"
    - design/result.html lines ~286-352 (VISUAL intent only — scene/material/light recipe + ISO/TOP/FRONT feel; do NOT copy hardcoded camera vectors or the wrong "box-centre" note)
    - src/lib/mapping.ts, src/lib/palette.ts, src/lib/camera-presets.ts, src/lib/__fixtures__/pack-done-response.json (the Plan-01 + Task-1 artifacts this consumes)
    - src/router.tsx (confirms /result is lazy — keep all three/r3f/drei imports inside this subtree)
  </read_first>
  <action>
Replace the empty body of `src/routes/ResultPage.tsx` (keep the `100dvh` wrapper + `<Canvas data-testid="r3f-canvas">`; keep `export default function ResultPage`). Import the committed fixture JSON and render `result.pallets[0]` only (D-04). Build the scene from RESEARCH Pattern 3 + UI-SPEC constants:
- `<Canvas shadows>` with PCF soft shadows, antialias, pixelRatio capped at 2; `<color attach="background">` or radial-gradient container for `--color-d-bg`; `<fog>` (`#0c0f17`, near/far tuned to bbox, not hardcoded).
- Lights (UI-SPEC values): ambient `#9fb0d0`@0.55, hemisphere sky `#cdd7f0`/ground `#0b0d14`@0.5, key directional `#fff6e8`@1.15 castShadow 2048² shadow map, fill directional `#6d7cff`@0.35.
- `src/components/viewer/Pallet.tsx`: the wood pallet model (slats + blocks) at `DECK_TOP_Y` baseline matching the mapping's deck (wood top `#caa06a`, sides/blocks `#b08a55`, roughness 0.85), the ground plane (`#0e1320`, receiveShadow), and the grid (drei `<Grid>` lines `#243049`/`#19202f` opacity 0.5, or `<gridHelper>` JSX).
- `src/components/viewer/Boxes.tsx`: derive the whole-fixture type set via `typeKeyOf` over ALL pallets' items + unpacked (Pitfall 5 → 3 types), build the palette once with `colorForType`. For each pallet-0 item, call `mapPlacement(item, pallet0.dimensions, typeKeyOf(item.item_id))` → render a `<mesh castShadow receiveShadow position={center}>` with `<boxGeometry args={size}>`, `<meshStandardMaterial color roughness={0.62} metalness={0.04}>`, and drei `<Edges>` tinted via offsetHSL(0,-0.04,+0.18) opacity 0.55 (D-10 — individual meshes, no InstancedMesh). Call `assertWithinEnvelope` per item (dev-only, tree-shaken). Wrap the boxes group in drei `<Bounds fit clip observe margin={1.2}>` so the real fixture auto-frames.
- `src/components/viewer/CameraPresets.tsx`: drei `<OrbitControls makeDefault enableDamping dampingFactor={0.08} maxPolarAngle={Math.PI*0.495}>` with min/maxDistance from `presetFromBbox`'s radius; on ISO/TOP/FRONT selection, animate the camera/target toward `presetFromBbox(bbox, which)` (lerp ~520ms cubic ease, or drei `useBounds().to(...)`). Initial frame = ISO. Compute the scene bbox via `THREE.Box3().setFromObject` of the boxes group (this is the one place three runtime is used — inside the lazy subtree).
- `src/components/viewer/ViewerOverlay.tsx`: absolute-positioned DOM over the Canvas (RESEARCH Open Question 2 recommendation — not drei `<Html>`), `pointer-events:none` except the preset buttons. Header (top-left, inset 24px): title (Inter 16/600 `--color-d-text`) = pallet name (fallback `Pallet A`) + dims tag (Mono 12/400 `--color-d-text-2`, rgba(255,255,255,0.05) bg, `--color-d-border` border) reading `{L} × {W} × {H} mm` from pallet0 dimensions (× = U+00D7). Legend (top-right, inset 24px): one row per whole-fixture type — 9×9px swatch (palette colour) + Mono 12/400 label (D/F/T). Control hints (bottom-left): `drag` orbit · `scroll` zoom · `right-drag` pan (verb 600 in `--color-d-text`, rest 400 in `--color-d-text-2`). The ISO/TOP/FRONT buttons render bottom-right (gap 8px, Mono 12px, the active button uses the accent state per UI-SPEC § Color); use `clsx` for the `.on`/active toggle. NO computed stats, NO right rail (D-14/D-15).
All three/r3f/drei imports MUST live in this `ResultPage` subtree (Pitfall 3). Each new file opens with a why-comment per house style.
  </action>
  <acceptance_criteria>
    - `src/routes/ResultPage.tsx` still contains `data-testid="r3f-canvas"` and `export default function ResultPage`
    - `src/components/viewer/Boxes.tsx` imports and calls `mapPlacement` and `colorForType` (grep both present)
    - `ResultPage` (or Boxes) imports the committed fixture and renders `result.pallets[0]` (grep `pack-done-response`)
    - `npx tsc -b --noEmit` exits 0 (scene typechecks against @types/three 0.184.1 + drei 10)
    - `npm run build` succeeds AND `node scripts/check-code-split.mjs` exits 0 (three/r3f/drei in the lazy chunk only, absent from index-*)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/anzozulia/Desktop/pallet_stacker_front && npm run build && node scripts/check-code-split.mjs</automated>
  </verify>
  <done>The /result route renders fixture pallet 0 as a wood pallet with per-type-coloured edged boxes, dark overlay header + legend + control hints + ISO/TOP/FRONT buttons, drei OrbitControls (damping + polar clamp), and bbox-fit presets; the build passes and the code-split gate confirms three stays in the lazy chunk.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright Canvas-mount + ISO/TOP/FRONT preset e2e</name>
  <files>e2e/result-viewer.spec.ts</files>
  <read_first>
    - e2e/smoke.spec.ts (copy structure: console-error collector registered BEFORE goto, goto('/result'), canvas visible, zero webgl/three errors; runs against preview build)
    - playwright.config.ts (webServer = npm run build && npm run preview — preview-build, not dev)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-RESEARCH.md § "Validation Architecture → Phase Requirements → Test Map" (the two e2e rows: Canvas+≥1 box mesh; ISO/TOP/FRONT change camera / button .on toggles; the `-g presets` group name)
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-VALIDATION.md § "Per-Task Verification Map" (the two viewer rows) + § "Manual-Only Verifications"
    - .planning/phases/02-coordinate-mapping-fixture-viewer/02-PATTERNS.md § "e2e/result-viewer.spec.ts"
    - src/components/viewer/ViewerOverlay.tsx / CameraPresets.tsx (the rendered ISO/TOP/FRONT button copy + active-state class to assert against)
  </read_first>
  <action>
Write `e2e/result-viewer.spec.ts`, copying the wiring from `e2e/smoke.spec.ts`. Test 1 (Canvas smoke, RESULT-01): register the console-error collector before `page.goto('/result')`, assert `page.locator('canvas')` is visible, and assert zero `webgl|three`-matching console errors — proving the viewer mounts the populated scene (≥1 box mesh) without WebGL errors. Test 2 (grouped so `-g presets` selects it, RESULT-02): locate the three buttons by their `ISO`/`TOP`/`FRONT` text, click each, and assert the camera visibly reframed — assert the clicked button gains the active (`.on`/accent) state AND/OR the canvas pixels change (screenshot-diff or a readback). Prefer the deterministic button-active-state assertion (the visual reframe + animation easing is a human judgment captured in VALIDATION § "Manual-Only Verifications"). Both tests run against the preview build via the existing webServer config — no dev server, no new Playwright config.
  </action>
  <acceptance_criteria>
    - `e2e/result-viewer.spec.ts` contains `goto('/result')` and a console-error collector registered before navigation
    - a test (or describe) named so `-g presets` matches it exists and clicks ISO/TOP/FRONT, asserting the active-state toggle
    - `npx playwright test e2e/result-viewer.spec.ts` exits 0 (Canvas mounts, no WebGL errors)
    - `npx playwright test e2e/result-viewer.spec.ts -g presets` exits 0 (preset switching asserted)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/anzozulia/Desktop/pallet_stacker_front && npx playwright test e2e/result-viewer.spec.ts</automated>
  </verify>
  <done>The Playwright preview-build e2e proves the /result viewer mounts a populated Canvas with no WebGL errors and that ISO/TOP/FRONT clicking toggles the active preset; runs green via the existing webServer (no new config).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual + camera-feel verification of the /result viewer</name>
  <what-built>The full static 3D fixture viewer on /result: wood pallet + per-type-coloured edged boxes, dark overlay header (pallet name + dims tag) + top-right legend + bottom-left control hints + bottom-right ISO/TOP/FRONT buttons, orbit/zoom/pan with damping + polar clamp, and animated bbox-fit camera presets. Mapping, palette, and preset math are golden/unit-tested; the Canvas mount + preset switching are e2e-tested. This checkpoint confirms the qualitative visual + camera "feel" that automation cannot judge.</what-built>
  <how-to-verify>
    1. Run `npm run build && npm run preview` and open `http://localhost:4173/result`.
    2. Compare side-by-side with `design/result.html` (viewer pane): confirm coloured boxes sit on a wood pallet, soft shadows + grid + fog present, a top-right legend shows 3 swatch+name rows (D / F / T), and a dark top-left overlay header shows the pallet name + a `1000 × 800 × 1000 mm` dims tag, with bottom-left control hints.
    3. Left-drag to orbit, scroll to zoom, right-drag to pan — confirm damping feels smooth and the camera cannot orbit beneath the ground plane.
    4. Click TOP (should look straight down — a plan view), FRONT (an axis-aligned elevation), then ISO (a three-quarter view) — confirm each visibly reframes with a smooth animated transition and the active button is highlighted.
    5. Confirm boxes sit ON the deck (the corner box at position {0,0,0} sits in the pallet corner, not floating or half-sunk) — the min-corner-vs-centre sanity check from RESEARCH Pitfall 1.
  </how-to-verify>
  <action>Gate task — no code change. After Tasks 1-3 are green (vitest + Playwright + code-split build gate all pass), present the running preview build for human visual + camera-feel sign-off per the verification steps below. This is the qualitative judgment (mockup fidelity, preset framing, animation easing, boxes-on-deck sanity) that the automated e2e cannot make. Block until "approved".</action>
  <resume-signal>Type "approved" or describe any visual/camera issues (e.g. boxes floating, legend sparse, presets not reframing).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                                                                                                                                                 |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none active this plan) | The viewer renders a committed, build-time-bundled fixture. No runtime network call, no runtime user input beyond camera gestures (which only move the client-side camera). The live API client is Phase 5. |

## STRIDE Threat Register

| Threat ID | Category          | Component                         | Disposition | Mitigation Plan                                                                                                                                            |
| --------- | ----------------- | --------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-03   | Denial of Service | three/WebGL render of the fixture | accept      | Single-pallet box count (19 meshes) is far below the InstancedMesh ~100–200 threshold; the fixture is fixed and reviewed. No attacker-controlled geometry. |
| T-02-04   | Tampering         | three/r3f/drei untrusted input    | accept      | No untrusted runtime input — the only data is the bundled fixture; camera gestures cannot inject data.                                                     |
| T-02-SC   | Tampering         | npm installs                      | accept      | No new packages installed this plan (drei/r3f/three/clsx + playwright were Phase-1 supply-chain-approved, T-1-SC). slopcheck N/A.                          |

**ASVS L1 net:** Negligible surface (client-side 3D render of a trusted committed fixture; no auth, no runtime network, no secrets — RESEARCH § "Security Domain"). No high-severity threats. The forward CORS note (production serving origin must be added to the API allowlist) is a Phase 5/7 concern already tracked in STATE.md — out of scope here.
</threat_model>

<verification>
- `npm run build && node scripts/check-code-split.mjs` — build succeeds and three/r3f/drei stay in the lazy /result chunk (absent from index-*)
- `npx vitest run src/lib/` — camera-preset math (plus Plan 01's mapping/palette) all green
- `npx playwright test e2e/result-viewer.spec.ts` — Canvas mounts with no WebGL errors; ISO/TOP/FRONT preset switching asserted
- `grep -- '--color-d-bg' src/styles.css` — dark-overlay tokens ported
- Human checkpoint confirms visual fidelity to the mockup + qualitative camera feel (the parts automation cannot judge — VALIDATION § "Manual-Only Verifications")
</verification>

<success_criteria>

- The /result viewer renders the committed fixture's pallet 0 as a 3D wood pallet with boxes coloured by type and a legend, visually matching design/result.html (SC-3, RESULT-01)
- A user can orbit, zoom, pan and switch ISO/TOP/FRONT camera presets, auto-fit to the fixture (SC-4, RESULT-02)
- Camera presets are computed from the scene bounding box (D-11) — not the mockup's hardcoded vectors
- three/r3f/drei remain isolated in the lazy chunk (code-split gate green); the dark-overlay @theme tokens are ported (D-08)
  </success_criteria>

<output>
Create `.planning/phases/02-coordinate-mapping-fixture-viewer/02-02-SUMMARY.md` when done
</output>
