---
phase: 01-scaffolding-version-lock
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02-scaffolding-version-lock
files_modified:
  - vitest.config.ts
  - src/test/setup.ts
  - src/components/Hello.tsx
  - src/components/Hello.test.tsx
  - playwright.config.ts
  - e2e/smoke.spec.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "npm run test runs a passing Vitest sample in jsdom with no Canvas imported (SC-3a)"
    - "npm run test:e2e boots the real preview build, navigates to /result, and asserts the canvas element is visible with zero webgl/three console errors (SC-2 live + SC-3b)"
    - "The @/ path alias resolves inside Vitest as well as Vite (vite-tsconfig-paths registered in both, the #1 alias-breaks-in-tests gotcha)"
    - "Vitest never runs the Playwright specs (e2e/** excluded) and no jsdom test imports the WebGL Canvas (Pitfall 2)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "jsdom env, globals, setupFiles, exclude e2e/**, tsconfigPaths plugin"
      contains: "jsdom"
    - path: "src/test/setup.ts"
      provides: "jest-dom matchers wired into Vitest"
      contains: "@testing-library/jest-dom/vitest"
    - path: "src/components/Hello.test.tsx"
      provides: "Passing sample unit test (no Canvas)"
    - path: "playwright.config.ts"
      provides: "webServer boots vite preview on 4173, Chromium, testDir e2e"
      contains: "webServer"
    - path: "e2e/smoke.spec.ts"
      provides: "Navigates /result, asserts canvas visible + no WebGL console errors"
      contains: "/result"
  key_links:
    - from: "playwright.config.ts"
      to: "vite preview (port 4173)"
      via: "webServer.command + url"
      pattern: "vite preview|4173"
    - from: "e2e/smoke.spec.ts"
      to: "src/routes/ResultPage.tsx canvas"
      via: "page.goto('/result') + locator('canvas')"
      pattern: "locator\\(.canvas"
    - from: "vitest.config.ts"
      to: "tsconfig paths @/*"
      via: "tsconfigPaths() plugin in vitest config"
      pattern: "tsconfigPaths"
---

<objective>
Stand up the full test harness — the Wave 0 test infrastructure the VALIDATION.md contract requires — so the walking skeleton has a passing unit test and a passing end-to-end smoke test through the real preview build. Vitest (jsdom) runs a trivial sample component test and a jest-dom-augmented setup; Playwright boots `vite preview` (the production static build, not the dev server), navigates to `/result`, and asserts the r3f `<Canvas>` mounts with zero WebGL console errors — the live proof of SC-2 that jsdom cannot give.

Purpose: Delivers SC-3a (Vitest sample passes), SC-3b (Playwright smoke passes), and the LIVE half of SC-2 (Canvas mounts without WebGL errors in real Chromium). It closes the Wave 0 gaps enumerated in 01-VALIDATION.md so every later wave/phase has a working feedback loop. Correctly honors the jsdom-has-no-WebGL boundary (Canvas tested only in Playwright) and the `@/`-alias-in-both-configs gotcha.
Output: `npm run test` and `npm run test:e2e` both exit 0 — the test scripts Plan 04's CI pipeline runs as its quality gate.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-scaffolding-version-lock/01-CONTEXT.md
@.planning/phases/01-scaffolding-version-lock/01-RESEARCH.md
@.planning/phases/01-scaffolding-version-lock/01-VALIDATION.md
@.planning/phases/01-scaffolding-version-lock/01-02-PLAN.md
</context>

<artifacts_this_phase_produces>
New files + symbols created by THIS plan (excluded from drift verification):

- `vitest.config.ts` — `plugins: [react(), tsconfigPaths()]`, `test: { environment: 'jsdom', globals: true, setupFiles: ['./src/test/setup.ts'], exclude: [..., 'e2e/**'] }`
- `src/test/setup.ts` — `import '@testing-library/jest-dom/vitest'`
- `src/components/Hello.tsx` — trivial sample component (`<h1>Palletize</h1>`), default export
- `src/components/Hello.test.tsx` — passing render-and-assert test (NO Canvas import)
- `playwright.config.ts` — `testDir: './e2e'`, `use.baseURL: 'http://localhost:4173'`, `webServer` = `npm run build && npm run preview`, `reuseExistingServer: !process.env.CI`
- `e2e/smoke.spec.ts` — `page.goto('/result')`, asserts `locator('canvas')` visible + filters console errors for `/webgl|three/i` === 0
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vitest harness (jsdom) + jest-dom setup + passing sample unit test</name>
  <files>vitest.config.ts, src/test/setup.ts, src/components/Hello.tsx, src/components/Hello.test.tsx</files>
  <behavior>
    - `npm run test` discovers and runs `src/components/Hello.test.tsx` and it PASSES
    - The sample test renders `<Hello/>` and asserts the heading "Palletize" is in the document (proves @testing-library/react + jest-dom matchers work)
    - Vitest uses the jsdom environment and does NOT attempt to run any spec under `e2e/`
    - A `@/`-aliased import inside a test resolves (alias works in Vitest, not just Vite)
  </behavior>
  <read_first>
    - .planning/phases/01-scaffolding-version-lock/01-RESEARCH.md — "Vitest config (jsdom env, jest-dom setup)" and "Pattern 5: Path aliases in BOTH Vite and Vitest" (the #1 alias-breaks-in-tests gotcha) and "Pitfall 2: Canvas tested in jsdom" and "Pitfall 4: Alias resolves in app, breaks in tests"
    - .planning/phases/01-scaffolding-version-lock/01-VALIDATION.md — "Wave 0 Requirements" (vitest.config.ts, src/test/setup.ts, Hello.test.tsx) and the SC-3a row
    - CLAUDE.md — "Do not unit-test the WebGL canvas in jsdom" guidance; the vitest / @testing-library / jsdom rows
  </read_first>
  <action>
    Create `vitest.config.ts` per the RESEARCH.md snippet: `defineConfig` from `vitest/config`, `plugins: [react(), tsconfigPaths()]` (tsconfigPaths MUST be present here too — without it `@/` imports resolve in `npm run dev` but break in `npm run test`), and a `test` block with `environment: 'jsdom'`, `globals: true`, `setupFiles: ['./src/test/setup.ts']`, and `exclude: ['**/node_modules/**', 'e2e/**']` so Vitest never runs the Playwright specs. Create `src/test/setup.ts` importing `@testing-library/jest-dom/vitest`. Create `src/components/Hello.tsx` as a trivial default-export component rendering `<h1>Palletize</h1>`, and `src/components/Hello.test.tsx` that imports Hello via the `@/` alias (to exercise the alias-in-tests seam), renders it with `@testing-library/react`, and asserts the heading is in the document using a jest-dom matcher. Do NOT import `ResultPage`, `Canvas`, or any three/r3f module into any jsdom test — jsdom has no WebGL and the test would throw or hang (Pitfall 2).
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` (`vitest run`) exits 0 with the Hello sample test passing (SC-3a)
    - `grep -q "jsdom" vitest.config.ts && grep -q "tsconfigPaths" vitest.config.ts && grep -q "e2e/" vitest.config.ts` succeed (jsdom env, alias plugin, e2e excluded)
    - `grep -q "@testing-library/jest-dom/vitest" src/test/setup.ts` succeeds
    - `grep -q "@/components/Hello" src/components/Hello.test.tsx` succeeds (alias used in a test, proving Pitfall 4 is handled)
    - `! grep -rEq "Canvas|@react-three" src/components/Hello.test.tsx src/test/setup.ts` — no WebGL/Canvas import in any jsdom test (Pitfall 2)
  </acceptance_criteria>
  <done>`npm run test` passes the sample unit test in jsdom; the `@/` alias resolves in Vitest; `e2e/**` is excluded from Vitest; no jsdom test imports the WebGL Canvas (SC-3a; Pitfalls 2 + 4 handled).</done>
</task>

<task type="auto">
  <name>Task 2: Playwright smoke harness — preview-build webServer + /result Canvas-mount assertion</name>
  <files>playwright.config.ts, e2e/smoke.spec.ts</files>
  <read_first>
    - .planning/phases/01-scaffolding-version-lock/01-RESEARCH.md — "Playwright config + smoke spec" (the `playwright.config.ts` and `e2e/smoke.spec.ts` snippets), "Pitfall 5: Playwright browsers not installed in CI", and the note to use `vite preview` (port 4173) not the dev server
    - .planning/phases/01-scaffolding-version-lock/01-CONTEXT.md — D-04/D-05 (Canvas on lazy /result; smoke navigates to /result) and the <specifics> note (verify Canvas in Playwright, jsdom has no WebGL)
    - .planning/phases/01-scaffolding-version-lock/01-VALIDATION.md — the SC-2 / SC-3b rows and "Wave 0 Requirements" (playwright.config.ts, e2e/smoke.spec.ts)
    - src/routes/ResultPage.tsx (from Plan 02) — the page under test (the `<Canvas data-testid="r3f-canvas">`)
  </read_first>
  <action>
    Ensure the Chromium browser binary is installed (`npx playwright install --with-deps chromium`) before running — the browser is a separate download from the npm package (Pitfall 5; CI does this in Plan 04). Create `playwright.config.ts` per the RESEARCH.md snippet: `testDir: './e2e'`, `use: { baseURL: 'http://localhost:4173' }`, and a `webServer` block whose `command` is `npm run build && npm run preview` (test the REAL static production build that Docker serves, NOT the dev server), `url: 'http://localhost:4173'`, `reuseExistingServer: !process.env.CI` (fresh server in CI, reuse locally), and a generous `timeout` (~120000). Create `e2e/smoke.spec.ts` that registers a `page.on('console', ...)` error collector BEFORE navigation, calls `page.goto('/result')`, asserts `expect(page.locator('canvas')).toBeVisible()` (the lazy chunk loads + the r3f Canvas mounts), and asserts that console errors matching `/webgl|three/i` have length 0 (no WebGL errors — the live half of SC-2). This deep-link nav to `/result` also exercises the route boundary Plan 04's nginx fallback must serve.
  </action>
  <verify>
    <automated>npx playwright install chromium >/dev/null 2>&1; npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e` (`playwright test`) exits 0 with the smoke test passing (SC-3b)
    - The smoke test navigates to `/result`, the `canvas` element is visible, and no `webgl`/`three` console errors are collected (live SC-2)
    - `grep -q "webServer" playwright.config.ts && grep -Eq "vite preview|npm run preview" playwright.config.ts && grep -q "4173" playwright.config.ts` succeed (preview build under test, not dev server)
    - `grep -q "reuseExistingServer" playwright.config.ts` succeeds
    - `grep -q "/result" e2e/smoke.spec.ts && grep -Eq "locator\(.canvas" e2e/smoke.spec.ts` succeed
    - `grep -Eq "webgl|three" e2e/smoke.spec.ts` succeeds (the no-WebGL-error assertion is present)
  </acceptance_criteria>
  <done>`npm run test:e2e` boots the real preview build, navigates to `/result`, and confirms the empty r3f Canvas mounts with zero WebGL console errors — the live proof of SC-2 plus SC-3b; the deep-link nav also pre-validates the route Plan 04's nginx fallback serves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → app code | Vitest/Playwright execute app code; no untrusted external input in Phase 1 |
| Playwright browser → real WebGL context | Real Chromium creates a WebGL context to validate the Canvas mount (jsdom cannot) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-06 | Tampering | Canvas tested in the wrong environment (jsdom) | mitigate | jsdom tests cover pure DOM/logic only; the Canvas-mounts assertion runs exclusively in Playwright (real Chromium); `e2e/**` excluded from Vitest so the boundary cannot be crossed accidentally |
| T-1-07 | Denial of Service | Playwright browsers missing in CI | accept (mitigated in Plan 04) | `npx playwright install --with-deps chromium` is run before E2E locally and is a dedicated CI step in Plan 04 (Pitfall 5) |
</threat_model>

<verification>
- `npm run test` passes the Vitest sample in jsdom (SC-3a); `@/` alias resolves in Vitest; `e2e/**` excluded
- `npm run test:e2e` boots `vite preview` and passes the `/result` Canvas-mount smoke with no WebGL errors (SC-2 live + SC-3b)
- No jsdom test imports the Canvas (Pitfall 2); preview build under test, not dev server
</verification>

<success_criteria>
- SC-3a: `npm run test` runs a passing Vitest sample
- SC-3b: `npm run test:e2e` runs a passing Playwright smoke test
- SC-2 (live): the `/result` Canvas mounts in real Chromium with zero WebGL console errors
</success_criteria>

<output>
Create `.planning/phases/01-scaffolding-version-lock/01-03-SUMMARY.md` when done.
</output>
