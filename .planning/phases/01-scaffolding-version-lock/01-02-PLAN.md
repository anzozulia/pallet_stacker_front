---
phase: 01-scaffolding-version-lock
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-scaffolding-version-lock
files_modified:
  - src/main.tsx
  - src/router.tsx
  - src/App.tsx
  - src/routes/ConfigurePage.tsx
  - src/routes/ResultPage.tsx
  - src/components/.gitkeep
  - src/features/.gitkeep
  - src/lib/.gitkeep
  - src/api/.gitkeep
  - src/types/.gitkeep
  - scripts/check-code-split.mjs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "A developer can run npm run dev, load / (ConfigurePage, eager), navigate to /result, and see an empty react-three-fiber <Canvas> mount with no WebGL errors (SC-2)"
    - "three / r3f / drei land ONLY in the lazy /result chunk and are absent from the initial Configure (main) bundle (code-split assertion, D-04)"
    - "Routing uses createBrowserRouter from react-router (v7, SPA/library mode) with / eager and /result React.lazy-loaded behind Suspense (D-03)"
    - "The signposted directory skeleton (components/features/lib/api/types/routes) exists with .gitkeep placeholders and no speculative logic files (D-01/D-02)"
  artifacts:
    - path: "src/router.tsx"
      provides: "createBrowserRouter with / (eager ConfigurePage) and /result (lazy ResultPage behind Suspense)"
      contains: "createBrowserRouter"
    - path: "src/main.tsx"
      provides: "createRoot + RouterProvider + styles.css import"
      contains: "createRoot"
    - path: "src/routes/ResultPage.tsx"
      provides: "Lazy-loaded page hosting the empty r3f <Canvas> in a full-height wrapper"
      contains: "Canvas"
    - path: "src/routes/ConfigurePage.tsx"
      provides: "Eager placeholder page for / (no three import)"
    - path: "scripts/check-code-split.mjs"
      provides: "Build-time assertion that three is in the lazy chunk, absent from entry chunk"
  key_links:
    - from: "src/router.tsx"
      to: "src/routes/ResultPage.tsx"
      via: "React.lazy(() => import('@/routes/ResultPage'))"
      pattern: "lazy\\(.*ResultPage"
    - from: "src/main.tsx"
      to: "src/router.tsx"
      via: "RouterProvider router={router}"
      pattern: "RouterProvider"
    - from: "src/routes/ResultPage.tsx"
      to: "@react-three/fiber"
      via: "import { Canvas }"
      pattern: "from ['\"]@react-three/fiber['\"]"
---

<objective>
Build the thinnest real end-to-end UI slice of the walking skeleton: the app shell, react-router 7 SPA-mode routing with an eager Configure page at `/` and a `React.lazy`-loaded Result page at `/result` that hosts an empty react-three-fiber `<Canvas>`, plus the signposted directory skeleton. This is the one real user interaction that proves the full client stack: navigating to `/result` mounts a WebGL canvas with no errors, AND the heavy three/r3f/drei trio lands only in the `/result` chunk (out of the initial Configure bundle).

Purpose: Delivers SC-2 (dev server renders an empty `<Canvas>` without WebGL errors) and the code-split assertion (D-04) — the single highest-value tracer through the client stack. It also deliberately exercises the route boundary that Plan 04's nginx SPA fallback (D-05) must handle on deep-link refresh. Establishes the directory architecture (D-01/D-02) every later phase populates.
Output: A running SPA where `/` and `/result` both work, three is code-split into the lazy chunk, and the empty Canvas mounts cleanly — verified end-to-end by Plan 03's Playwright smoke test.
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-scaffolding-version-lock/01-CONTEXT.md
@.planning/phases/01-scaffolding-version-lock/01-RESEARCH.md
@.planning/phases/01-scaffolding-version-lock/01-01-PLAN.md
</context>

<artifacts_this_phase_produces>
New files + symbols created by THIS plan (excluded from drift verification):

- `src/main.tsx` — `createRoot(...).render(<StrictMode><RouterProvider router={router}/></StrictMode>)`, imports `@/styles.css`
- `src/router.tsx` — `export const router = createBrowserRouter([...])`; `/` -> eager `<ConfigurePage/>`, `/result` -> `<Suspense><ResultPage/></Suspense>` with `ResultPage = lazy(() => import('@/routes/ResultPage'))`
- `src/App.tsx` — optional shell (or folded into router); no three import
- `src/routes/ConfigurePage.tsx` — eager placeholder, default export, NO three import (must stay out of main chunk)
- `src/routes/ResultPage.tsx` — default export; `<Canvas data-testid="r3f-canvas">` with `<ambientLight/>` inside a `100dvh` wrapper
- `src/components/.gitkeep`, `src/features/.gitkeep`, `src/lib/.gitkeep`, `src/api/.gitkeep`, `src/types/.gitkeep` — signposted dirs (D-01/D-02; NO logic files)
- `scripts/check-code-split.mjs` — node script asserting three is only in the lazy chunk after `vite build`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Signposted directory skeleton + app shell + react-router SPA routing</name>
  <files>src/main.tsx, src/router.tsx, src/App.tsx, src/routes/ConfigurePage.tsx, src/components/.gitkeep, src/features/.gitkeep, src/lib/.gitkeep, src/api/.gitkeep, src/types/.gitkeep</files>
  <read_first>
    - .planning/phases/01-scaffolding-version-lock/01-RESEARCH.md — "Recommended Project Structure (D-01 thin/signposted)" and "Pattern 1: react-router 7 SPA mode + lazy 3D route" (the `src/router.tsx` and `src/main.tsx` snippets)
    - .planning/phases/01-scaffolding-version-lock/01-CONTEXT.md — D-01 (thin signposted dirs), D-02 (NO placeholder logic files — .gitkeep only), D-03 (createBrowserRouter, two routes), D-04 (lazy /result keeps three out of main chunk)
    - CLAUDE.md — react-router row ("import from `react-router`", SPA/library mode, NOT framework/SSR) and the "What NOT to Use" `react-router-dom` warning
    - src/styles.css (created in Plan 01) — imported by main.tsx
  </read_first>
  <action>
    Create the signposted directory skeleton per D-01 with `.gitkeep` ONLY (no logic files, D-02): `src/components/.gitkeep`, `src/features/.gitkeep`, `src/lib/.gitkeep`, `src/api/.gitkeep`, `src/types/.gitkeep`. Create `src/routes/ConfigurePage.tsx` as an eager placeholder default-export component (a heading using Tailwind utilities such as `text-accent` / `font-sans` to prove the styling seam; it MUST NOT import three/r3f/drei so it stays out of the lazy chunk). Create `src/router.tsx` with `createBrowserRouter` imported from `react-router` (NOT `react-router-dom` — v7 merged package), defining `/` to `<ConfigurePage/>` (eager import) and `/result` to the lazy element (completed in Task 2). Create `src/main.tsx` using `createRoot` from `react-dom/client`, importing `@/styles.css` and `RouterProvider` from `react-router`, rendered inside `<StrictMode>`. Optionally add a thin `src/App.tsx` shell or fold it into the router (no three import either way). Use the `@/` alias for intra-src imports to exercise the path-alias seam from Plan 01.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` and `npm run lint` both exit 0
    - `grep -q "createBrowserRouter" src/router.tsx` succeeds and `grep -Eq "from ['\"]react-router['\"]" src/router.tsx` succeeds (v7 import surface)
    - `! grep -rq "react-router-dom" src/` — the wrong package is never imported (D-03)
    - `grep -q "createRoot" src/main.tsx && grep -q "RouterProvider" src/main.tsx` succeed
    - `grep -q "styles.css" src/main.tsx` succeeds (styles wired)
    - `for d in components features lib api types; do test -f src/$d/.gitkeep || exit 1; done` succeeds (signposted dirs, D-01)
    - `! grep -rq "three" src/routes/ConfigurePage.tsx` — ConfigurePage has no three import (stays in main chunk, D-04)
    - No stray logic files in signposted dirs: `[ -z "$(find src/lib src/api src/types src/features -type f ! -name '.gitkeep')" ]` (D-02)
  </acceptance_criteria>
  <done>The signposted directory skeleton exists (.gitkeep only); `createBrowserRouter` from `react-router` wires `/` (eager ConfigurePage) and `/result`; `main.tsx` mounts `RouterProvider` with styles imported; typecheck + lint clean; no `react-router-dom` import and no speculative logic files (D-01/D-02/D-03).</done>
</task>

<task type="auto">
  <name>Task 2: Lazy /result route hosting the empty r3f Canvas + verify code-split</name>
  <files>src/routes/ResultPage.tsx, src/router.tsx, scripts/check-code-split.mjs</files>
  <read_first>
    - .planning/phases/01-scaffolding-version-lock/01-RESEARCH.md — "Pattern 2: Empty r3f Canvas on the lazy route" (the `ResultPage.tsx` snippet, the explicit-height requirement), the "Verify the split worked" note after Pattern 1, and "Pitfall 2: Canvas tested in jsdom"
    - .planning/phases/01-scaffolding-version-lock/01-CONTEXT.md — D-04 (empty Canvas on lazy /result keeps three out of the main bundle), D-05 (this exercises the nginx SPA fallback early), and the <specifics> note (Canvas verified in Playwright, never jsdom)
    - src/router.tsx (from Task 1) — the route definition this task completes
  </read_first>
  <action>
    Create `src/routes/ResultPage.tsx` as a default-export component per the RESEARCH.md Pattern 2 snippet: a wrapper div with an EXPLICIT height (`100dvh`) — required because `<Canvas>` fills its parent and a zero-height parent yields a 0x0 canvas — containing `<Canvas data-testid="r3f-canvas">` from `@react-three/fiber` with a single `<ambientLight intensity={0.5}/>` inside (empty scene; Phase 6 adds content). In `src/router.tsx`, define `ResultPage` via `lazy(() => import('@/routes/ResultPage'))` and wrap it in `<Suspense fallback={...}>` AT THE ROUTE ELEMENT so three/r3f/drei resolve only when `/result` is visited (this is what creates the separate chunk). Do NOT import ResultPage or Canvas anywhere eager (no top-level import in router.tsx, no jsdom test) — that would defeat the split and break unit tests. Create `scripts/check-code-split.mjs`: a node script that reads `dist/assets/*.js`, confirms three/r3f markers (e.g. `BufferGeometry`/`THREE`) appear in a lazy chunk and are ABSENT from the `index-*` entry chunk, prints which chunk holds three, and exits non-zero (failing the build assertion) if three leaked into the entry chunk. Run `npm run build` then the script to prove the code-split boundary is correct.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-code-split.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -Eq "lazy\(.*ResultPage" src/router.tsx` succeeds (lazy import of ResultPage)
    - `grep -q "Suspense" src/router.tsx` succeeds (Suspense wraps the lazy route element)
    - `grep -Eq "from ['\"]@react-three/fiber['\"]" src/routes/ResultPage.tsx` succeeds and `grep -q "100dvh" src/routes/ResultPage.tsx` succeeds (explicit height)
    - `! grep -Eq "^import .*ResultPage" src/router.tsx` — ResultPage is NOT statically imported (only via `lazy(() => import(...))`)
    - `node scripts/check-code-split.mjs` exits 0: three appears in a lazy chunk and is ABSENT from the `index-*` entry chunk; fails loudly if three leaked into the entry chunk (D-04)
  </acceptance_criteria>
  <done>`/result` lazy-loads a page rendering an empty `<Canvas>` in a 100dvh wrapper; `npm run build` produces a separate chunk containing three (r3f/drei) while the entry/Configure chunk is three-free, proven by `scripts/check-code-split.mjs` (SC-2 build path, D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM → react-three-fiber WebGL context | The Canvas creates a real WebGL rendering context in the browser; no untrusted external input crosses here in Phase 1 (empty scene) |
| route boundary (`/` vs `/result`) | Client-side route changes; deep-link refresh of `/result` is handled by nginx SPA fallback (Plan 04, D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Denial of Service | three/r3f/drei in the initial bundle | mitigate | Code-split via `React.lazy` keeps the heaviest dep out of the Configure (main) chunk; build-time assertion (`scripts/check-code-split.mjs`) fails the build if three leaks into the entry chunk |
| T-1-04 | Information Disclosure | deep-link refresh of `/result` | accept (mitigated downstream) | In Phase 1 the route renders only an empty Canvas (no data); the nginx `try_files ... /index.html` fallback that makes deep-links safe is delivered in Plan 04 (D-05) |
</threat_model>

<verification>
- `npm run typecheck` and `npm run lint` exit 0 (quality gate)
- `npm run build` exits 0 and `scripts/check-code-split.mjs` confirms three is only in the lazy chunk (D-04)
- Routing imports from `react-router` (not `react-router-dom`); `/` eager, `/result` lazy behind Suspense (D-03)
- ResultPage hosts an empty `<Canvas>` in a 100dvh wrapper (SC-2 build path; Plan 03 asserts the live mount)
- Signposted dirs exist with `.gitkeep` only; no speculative logic files (D-01/D-02)
</verification>

<success_criteria>
- SC-2 (build path): `/result` renders an empty r3f `<Canvas>`; live no-WebGL-error mount is asserted by Plan 03's Playwright smoke
- Code-split (D-04): three/r3f/drei are absent from the entry chunk and present in the lazy `/result` chunk
- D-03: `createBrowserRouter` (react-router 7, SPA mode); `/` eager, `/result` lazy
- D-01/D-02: signposted directory skeleton with `.gitkeep` placeholders only
</success_criteria>

<output>
Create `.planning/phases/01-scaffolding-version-lock/01-02-SUMMARY.md` when done.
</output>
