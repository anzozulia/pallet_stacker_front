---
phase: 01-scaffolding-version-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - src/vite-env.d.ts
  - src/styles.css
  - public/fonts/Inter.woff2
  - public/fonts/JetBrainsMono.woff2
  - eslint.config.js
  - .prettierrc
  - .prettierignore
  - .gitignore
  - index.html
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "npm ci installs React 19.2.x, @react-three/fiber 9, @react-three/drei 10, three 0.184.0 (exact, no caret) with zero peer-dependency conflicts (SC-1)"
    - "npm run typecheck and npm run lint both exit 0 on the scaffold (quality gate)"
    - "The Vite dev server proxies /api to https://packerapi.anzozulia.xyz and VITE_API_URL is readable at build time via import.meta.env (SC-5)"
    - "Tailwind v4 is wired CSS-first (no tailwind.config.js, no postcss.config.js) with self-hosted Inter + JetBrains Mono fonts and the #4f46e5 accent token (D-06/D-07/D-08)"
  artifacts:
    - path: "package.json"
      provides: "Exact version pins for the locked quartet + scripts (dev/build/preview/typecheck/lint/format/test/test:e2e)"
      contains: "\"three\": \"0.184.0\""
    - path: "package-lock.json"
      provides: "Authoritative committed lockfile (supply-chain pin)"
    - path: "vite.config.ts"
      provides: "react + tailwindcss + tsconfigPaths plugins; server.proxy /api -> API"
      contains: "packerapi.anzozulia.xyz"
    - path: "src/styles.css"
      provides: "Tailwind import + @theme tokens + @font-face for self-hosted fonts"
      contains: "@import \"tailwindcss\""
    - path: "eslint.config.js"
      provides: "Flat ESLint config (eslint 10 / typescript-eslint 8) with prettier last"
    - path: "src/vite-env.d.ts"
      provides: "Typed VITE_API_URL on ImportMetaEnv (the build-time API seam)"
      contains: "VITE_API_URL"
  key_links:
    - from: "vite.config.ts"
      to: "https://packerapi.anzozulia.xyz"
      via: "server.proxy['/api'].target"
      pattern: "target:.*packerapi"
    - from: "src/styles.css"
      to: "public/fonts/Inter.woff2"
      via: "@font-face src url(/fonts/Inter.woff2)"
      pattern: "/fonts/Inter"
    - from: "vite.config.ts"
      to: "tsconfig.app.json paths @/*"
      via: "vite-tsconfig-paths plugin"
      pattern: "tsconfigPaths"
---

<objective>
Stand up the version-locked toolchain foundation for Palletize: a hand-built `package.json` with EXACT pins for the React 19.2.x / r3f 9 / drei 10 / three 0.184.0 quartet (three with no caret), a clean `npm ci`, the TypeScript project-references config, the Vite config (React plugin + Tailwind v4 plugin + path aliases + `/api` dev proxy + build-time `VITE_API_URL` seam), Tailwind v4 CSS-first styling with self-hosted fonts, and the flat ESLint + Prettier quality gate.

Purpose: This is the bedrock every later wave and every later phase builds on. The whole risk of Phase 1 is *wiring*, not version choice — getting the pins, the peer matrix, and the eight integration seams exactly right here means waves 2-4 just add files on top. Anchors SC-1 (install resolves the quartet, no peer conflict), SC-5 (dev proxy + build-time env), and the typecheck/lint quality gate.
Output: A repo that `npm ci && npm run typecheck && npm run lint` passes cleanly, with Tailwind, path aliases, the dev proxy, and the build-time env seam all wired — but no app code or routes yet (those are Plan 02).
</objective>

<execution_context>
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anzozulia/Desktop/pallet_stacker_front/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-scaffolding-version-lock/01-CONTEXT.md
@.planning/phases/01-scaffolding-version-lock/01-RESEARCH.md
@.planning/phases/01-scaffolding-version-lock/01-VALIDATION.md
</context>

<artifacts_this_phase_produces>
New files + symbols created by THIS plan (the source-grounding pass excludes these from drift verification):

- `package.json` — exact-pinned deps + scripts (`dev`, `build`, `preview`, `typecheck`, `lint`, `format`, `test`, `test:e2e`, `prepare`), `lint-staged` block
- `package-lock.json` — committed authoritative lockfile
- `tsconfig.json`, `tsconfig.app.json` (with `paths: {"@/*": ["src/*"]}`), `tsconfig.node.json`
- `vite.config.ts` — `react()`, `tailwindcss()`, `tsconfigPaths()` plugins; `server.proxy['/api']` -> `https://packerapi.anzozulia.xyz`
- `src/vite-env.d.ts` — `ImportMetaEnv.VITE_API_URL: string`
- `src/styles.css` — `@import "tailwindcss"`, `@theme` (`--font-sans`, `--font-mono`, `--color-accent: #4f46e5`), two `@font-face` blocks
- `public/fonts/Inter.woff2`, `public/fonts/JetBrainsMono.woff2` — self-hosted variable fonts
- `eslint.config.js` — flat config; `.prettierrc`, `.prettierignore`
- `.gitignore`, `index.html` (root div + module script entry)
- The `@/` path alias (target `src/*`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Hand-build package.json with exact pins and install clean</name>
  <files>package.json, package-lock.json, .gitignore, index.html</files>
  <read_first>
    - CLAUDE.md — the "TL;DR — The Prescriptive Stack" table and "Version Compatibility" matrix (exact versions + peer ranges) and the "What NOT to Use" list
    - .planning/phases/01-scaffolding-version-lock/01-RESEARCH.md — "Installation (single deliberate install set, exact pins)" block, "Supporting packages to add" table, and the `@vitejs/plugin-react@6.0.2` peer trap note
    - .planning/phases/01-scaffolding-version-lock/01-CONTEXT.md — D-13 (Babel plugin-react), D-15 (vite-tsconfig-paths)
  </read_first>
  <action>
    Do NOT run `npm create vite` — it pulls unpinned `^latest` and risks `react@>=19.3`, which violates the r3f 9 `<19.3` peer cap. Hand-author `package.json` from the RESEARCH.md install set with the EXACT versions listed there. Pin `three` to `0.184.0` with NO caret; pin `react`/`react-dom` to `19.2.7`; keep `@types/three` at `0.184.x` in lockstep. Include the supporting packages from the RESEARCH.md table: `@testing-library/dom@10.4.1` (the missing explicit peer of testing-library/react 16 — MUST be installed), `@testing-library/jest-dom@6.9.1`, `@testing-library/user-event@14.6.1`, `@types/react@19.2.16`, `@types/react-dom@19.2.3`, `eslint-plugin-react-hooks@7.1.1`, `eslint-plugin-react-refresh@0.5.2`, `@eslint/js@10.0.1`, `globals@17.6.0`, `jsdom@^26` (honor CLAUDE.md's `~26` deliberate lag), `clsx@2.1.1`, `husky@9.1.7`, `lint-staged@^17`. Do NOT install the optional peers `babel-plugin-react-compiler` or `@rolldown/plugin-babel` (plugin-react 6 marks them `optional: true`; npm will not warn). Add the `scripts` block from RESEARCH.md (`dev`, `build` = `tsc -b && vite build`, `preview` = `vite preview --port 4173`, `typecheck`, `lint`, `format`, `test` = `vitest run`, `test:e2e`, `prepare` = `husky`) and the `lint-staged` config. Set `"type": "module"`, `"private": true`, `"name": "palletize"`. Then run `npm install` once to generate `package-lock.json`, and commit the lockfile (it is the supply-chain pin; CI uses `npm ci` against it). Add `.gitignore` (node_modules, dist, coverage, playwright-report, .DS_Store) and a minimal root `index.html` with a `#root` div and a module `<script src="/src/main.tsx">` (main.tsx is created in Plan 02; that's fine, dev server isn't run in this task).
  </action>
  <verify>
    <automated>npm ci && npm ls react react-dom three @react-three/fiber @react-three/drei</automated>
  </verify>
  <acceptance_criteria>
    - `npm ci` exits 0 with no peer-dependency conflict / `ERESOLVE` errors (SC-1)
    - `npm ls react three @react-three/fiber @react-three/drei` shows react 19.2.x, three 0.184.0, @react-three/fiber 9.x, @react-three/drei 10.x with no `invalid`/`UNMET` markers (SC-1)
    - `node -e "const p=require('./package.json'); if(p.dependencies.three!=='0.184.0') throw new Error('three not exact-pinned')"` exits 0 (three value is exactly `0.184.0`, no caret)
    - `node -e "const p=require('./package.json'); const t=p.devDependencies['@types/three']; if(!/^0\.184\./.test(t)) throw new Error('types/three not in lockstep')"` exits 0
    - `node -e "const p=require('./package.json'); const all={...p.dependencies,...p.devDependencies}; if(all['babel-plugin-react-compiler']||all['@rolldown/plugin-babel']) throw new Error('optional react-compiler peers must NOT be installed')"` exits 0
    - `node -e "const p=require('./package.json'); if(!p.devDependencies['@testing-library/dom']) throw new Error('missing testing-library/dom explicit peer')"` exits 0
    - `package-lock.json` exists and is committed
  </acceptance_criteria>
  <done>`npm ci` resolves the full locked quartet plus supporting libs with zero peer conflicts; three is exact-pinned; the React-Compiler optional peers are absent; `@testing-library/dom` is present; the lockfile is committed (SC-1).</done>
</task>

<task type="auto">
  <name>Task 2: Wire TypeScript project refs, Vite config (proxy + env + Tailwind + aliases), and the env-type seam</name>
  <files>tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, src/vite-env.d.ts</files>
  <read_first>
    - .planning/phases/01-scaffolding-version-lock/01-RESEARCH.md — "Pattern 4: Vite config — proxy + env + tsconfig paths", "Pattern 5: Path aliases in BOTH Vite and Vitest", and the `src/vite-env.d.ts` snippet
    - CLAUDE.md — "Docker / nginx Self-Host Recipe (Outline)" notes on build-time `VITE_API_URL` and "Local dev: use Vite `server.proxy`"; the "Recommended Stack" Vite + plugin-react rows
    - .planning/phases/01-scaffolding-version-lock/01-CONTEXT.md — D-15 (`@/` aliases via vite-tsconfig-paths), D-16 (dev proxy + build-time env are two distinct seams)
  </read_first>
  <action>
    Create `tsconfig.json` (a thin solution file referencing `tsconfig.app.json` + `tsconfig.node.json` via project references), `tsconfig.app.json` (the app compiler options: ESNext module, bundler moduleResolution, `react-jsx`, strict, `baseUrl` ".", `paths` `{"@/*": ["src/*"]}`, noEmit, includes `src`), and `tsconfig.node.json` (covers `vite.config.ts` / config files). Create `vite.config.ts` with `plugins: [react(), tailwindcss(), tsconfigPaths()]` and `server.proxy['/api']` targeting `https://packerapi.anzozulia.xyz` with `changeOrigin: true` and `secure: true` (no `rewrite` — A2: forward `/api` as-is; Phase 5 resolves the exact prefix). Per D-16, do NOT read `VITE_API_URL` in code yet (no client until Phase 5) — only declare its type. Create `src/vite-env.d.ts` with the `/// <reference types="vite/client" />` line and the `ImportMetaEnv { readonly VITE_API_URL: string }` + `ImportMeta` augmentation exactly as in the RESEARCH.md snippet, so the build-time env seam is typed and ready. Honor the two-seam distinction: the dev proxy exists only in `npm run dev`; the baked `VITE_API_URL` is for production — keep them separate, do not conflate.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` (`tsc -b --noEmit`) exits 0
    - `grep -q "target:.*packerapi.anzozulia.xyz" vite.config.ts` succeeds (dev proxy target present)
    - `grep -q "changeOrigin" vite.config.ts && grep -q "secure" vite.config.ts` succeeds
    - `grep -q "tsconfigPaths" vite.config.ts` succeeds (alias plugin registered in Vite)
    - `grep -q "VITE_API_URL" src/vite-env.d.ts` succeeds (build-time env seam typed)
    - `grep -E '"@/\*"\s*:\s*\[\s*"src/\*"\s*\]' tsconfig.app.json` succeeds (the `@/` alias targets `src/*`)
    - No `tailwind.config.js` and no `postcss.config.js` exist: `test ! -e tailwind.config.js && test ! -e postcss.config.js`
  </acceptance_criteria>
  <done>TypeScript project references typecheck clean; `vite.config.ts` registers react + Tailwind + path-alias plugins and proxies `/api` to the HTTPS API target; the `@/` alias resolves to `src/*`; the `VITE_API_URL` build-time seam is typed but not yet consumed; no Tailwind/PostCSS JS config files exist (SC-5, D-15, D-16, D-06).</done>
</task>

<task type="auto">
  <name>Task 3: Tailwind v4 CSS-first styling with self-hosted fonts, flat ESLint + Prettier</name>
  <files>src/styles.css, public/fonts/Inter.woff2, public/fonts/JetBrainsMono.woff2, eslint.config.js, .prettierrc, .prettierignore</files>
  <read_first>
    - .planning/phases/01-scaffolding-version-lock/01-RESEARCH.md — "Pattern 3: Tailwind v4 CSS-first wiring", the "Flat ESLint config" code example, and the anti-pattern note on `tailwind.config.js`/`postcss.config.js`
    - .planning/phases/01-scaffolding-version-lock/01-CONTEXT.md — D-06 (Tailwind v4 CSS-first), D-07 (minimal @theme: fonts + accent ONLY in Phase 1), D-08 (self-host Inter + JetBrains Mono, no Google Fonts link), D-10 (light flat ESLint config)
    - design/config.html — visual source of the accent `#4f46e5`, Inter + JetBrains Mono (visual intent only — do NOT port the full palette per D-07)
  </read_first>
  <action>
    Source the variable `.woff2` files for Inter and JetBrains Mono (both OFL/open-licensed and freely downloadable — A3) and place them at `public/fonts/Inter.woff2` and `public/fonts/JetBrainsMono.woff2`. Create `src/styles.css` with `@import "tailwindcss"` FIRST, then two `@font-face` blocks referencing the fonts via ABSOLUTE `/fonts/...` URLs (variable-weight ranges per the RESEARCH.md snippet, `font-display: swap`), then a `@theme` block with ONLY `--font-sans: 'Inter', sans-serif`, `--font-mono: 'JetBrains Mono', monospace`, and `--color-accent: #4f46e5`. Do NOT port the full mockup palette (surfaces, the dark `--d-bg` 3D-overlay group) — that lands per-phase in Phases 4/6 per D-07. Do NOT add a Google Fonts `<link>` anywhere — self-hosting is the whole point (D-08, offline self-host story). Do NOT create `tailwind.config.js` or `postcss.config.js` (v4 is CSS-first). Create `eslint.config.js` as the flat config from the RESEARCH.md example: `js.configs.recommended`, `tseslint.configs.recommended`, the react-hooks + react-refresh plugins, and `eslint-config-prettier` (`prettier`) LAST so it disables conflicting stylistic rules; ignore `dist`, `playwright-report`, `coverage`. Create `.prettierrc` (project formatting defaults) and `.prettierignore` (dist, coverage, playwright-report, public/fonts, package-lock.json).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` (`eslint .`) exits 0 with no errors on the scaffold (quality gate)
    - `head -1 src/styles.css | grep -q 'tailwindcss'` — `@import "tailwindcss"` is the first line
    - `grep -q -- '--color-accent: #4f46e5' src/styles.css` succeeds (accent token, D-07)
    - `grep -q "/fonts/Inter" src/styles.css && grep -q "/fonts/JetBrainsMono" src/styles.css` succeeds (self-hosted @font-face, absolute URLs)
    - `test -f public/fonts/Inter.woff2 && test -f public/fonts/JetBrainsMono.woff2` succeeds (font files present)
    - NO Google Fonts link anywhere: `! grep -rq "fonts.googleapis.com" src/ index.html`
    - `! grep -q -- '--d-bg' src/styles.css` — full dark-overlay palette NOT ported in Phase 1 (D-07)
    - `eslint-config-prettier` (`prettier`) is the LAST entry in the flat config (disables conflicting stylistic rules)
    - `test ! -e tailwind.config.js && test ! -e postcss.config.js` succeeds
  </acceptance_criteria>
  <done>Tailwind v4 is wired CSS-first with self-hosted Inter + JetBrains Mono and the single accent token (no full palette, no Google Fonts link); `npm run lint` passes on the flat ESLint config with prettier last; no Tailwind/PostCSS JS config files exist (SC-5 styling, D-06/D-07/D-08).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4 (checkpoint): Confirm clean lockfile resolution + correct quartet pinning</name>
  <what-built>
    A hand-built, version-locked `package.json` + committed `package-lock.json` resolving the React 19.2.x / r3f 9 / drei 10 / three 0.184.0 quartet (three exact-pinned, no caret) with zero peer conflicts, plus the TypeScript/Vite/Tailwind/ESLint scaffold. Per the RESEARCH.md Package Legitimacy Audit, slopcheck was unavailable in this environment; all packages were registry-verified as mainstream, but supply-chain integrity (T-1-01) warrants a human confirmation of a clean lockfile resolution before later waves build on it.
  </what-built>
  <how-to-verify>
    1. Run `npm ci` and confirm it completes with NO `ERESOLVE` / peer-dependency errors.
    2. Run `npm ls react three @react-three/fiber @react-three/drei` and confirm: react 19.2.x (NOT >=19.3), three 0.184.0 (exact), r3f 9.x, drei 10.x — no `invalid`/`UNMET PEER` lines.
    3. Confirm `package.json` shows `"three": "0.184.0"` with NO caret, and that `babel-plugin-react-compiler` / `@rolldown/plugin-babel` are absent.
    4. Confirm `package-lock.json` is staged/committed.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the lockfile resolves cleanly and the quartet is correctly pinned, or describe any peer warnings / version drift you see.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine / CI | Package tarballs are untrusted input; resolved versions must be pinned and locked |
| dev browser → Vite dev proxy → packing API | `/api` requests cross from the local dev server to an external HTTPS origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | npm install / dependency resolution | mitigate | Exact version pins (three with no caret, react pinned <19.3), committed `package-lock.json`, registry-verified names per RESEARCH.md Package Legitimacy Audit; CI uses `npm ci` (Plan 04) |
| T-1-02 | Tampering | unpinned/drifting transitive deps | mitigate | Lockfile is authoritative; never `npm install` in CI; the `prepare: husky` script does not fetch network resources |
| T-1-05 | Tampering / Info Disclosure | Vite dev `/api` proxy to packerapi.anzozulia.xyz | mitigate | Proxy target is HTTPS with `secure: true` and `changeOrigin: true` (correct SNI); dev-only, absent from the production image |
| T-1-SC | Tampering | npm package installs (supply chain) | mitigate | slopcheck unavailable → all packages treated per RESEARCH.md registry-verified audit; blocking human-verify checkpoint confirms clean lockfile resolution before later waves |
</threat_model>

<verification>
- `npm ci` resolves the full quartet + supporting libs with zero peer conflicts (SC-1)
- `npm run typecheck` exits 0 (project references valid)
- `npm run lint` exits 0 (flat ESLint config clean)
- `vite.config.ts` proxies `/api` to the HTTPS API and registers Tailwind + path-alias plugins (SC-5, D-15, D-16)
- `src/styles.css` is Tailwind-v4 CSS-first with self-hosted fonts and only the accent token (D-06/D-07/D-08)
- three is exact-pinned; React-Compiler optional peers absent; `@testing-library/dom` present
- No `tailwind.config.js` / `postcss.config.js`; no Google Fonts link
</verification>

<success_criteria>
- SC-1: `npm ci` resolves React 19.2.x / r3f 9 / drei 10 / three 0.184.0 (exact) with no peer conflict
- SC-5 (config half): `/api` dev proxy wired to the HTTPS API; `VITE_API_URL` build-time seam typed via `import.meta.env`
- Quality gate: `npm run typecheck` and `npm run lint` both exit 0
- D-06/D-07/D-08: Tailwind v4 CSS-first, self-hosted fonts, minimal @theme (fonts + accent only)
</success_criteria>

<output>
Create `.planning/phases/01-scaffolding-version-lock/01-01-SUMMARY.md` when done.
</output>
